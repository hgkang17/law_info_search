# 수정기록

이 파일은 저장소에서 무엇을 왜 바꿨는지 이어서 남기는 작업 기록이다.
변경 배경을 놓치면 같은 자리를 되돌리거나 이미 고친 버그를 다시 만들 수
있으므로 모든 작업은 이 파일을 거친다.

## 지켜야 할 것

1. **작업을 시작하기 전에 이 파일의 최근 10개 항목을 읽는다.**
   특히 "주의" 줄은 같은 실수를 막으려고 남긴 것이다.
2. **작업을 끝내면 맨 위(최신이 위)에 항목을 하나 추가한다.**
   커밋과 함께 올린다. 커밋을 나눠도 항목은 하나로 묶어도 된다.
3. **되돌린 작업도 적는다.** 왜 되돌렸는지가 이후 작업에 가장 중요하다.
4. 이 파일은 `버전기록.md`와 다르다. 저기는 **사용자에게 보이는 버전별
   변경 요약**이고, 여기는 **개별 작업의 상세 기록**이다. 코드에 드러나지
   않는 판단ㆍ함정ㆍ보류한 일을 여기 적는다.

## 항목 형식

```markdown
## YYYY-MM-DD · 한 줄 제목

- **커밋**: 짧은 해시 (여러 개면 쉼표로)
- **버전**: 이 작업이 들어간 APP_VERSION (릴리즈 안 했으면 "미배포")
- **무엇을**: 바꾼 내용
- **왜**: 그렇게 한 이유
- **주의**: 이후 작업에서 반복하기 쉬운 실수 (없으면 줄째 생략)
- **남은 일**: 하다 만 것ㆍ보류한 것 (없으면 줄째 생략)
```

---

## 2026-09-03 · 분류 바ㆍ콤보 새 모양과 별표·서식 본문 내 미리보기

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**:
  1. 분류 바를 캡슐 여러 개에서 남색 띠 하나로 바꿨다. 단추가 폭을 고르게
     나누고 고른 분류만 밝게 뜬다. 법령ㆍ행정규칙ㆍ자치법규는 고른 순간
     아래 줄에 ``(법률·대통령령·부령)`` 같은 부제가 함께 나온다.
  2. 왼쪽 주 메뉴와 분류 단추 글꼴을 Pretendard SemiBold 20px로 맞췄다.
  3. 콤보를 회색 바탕에 얇은 ∨ 표시로 바꾸고, 펼친 목록이 칸을 덮지 않고
     칸 아래로 내려오게 했다. 목록은 흰 바탕에 둥근 모서리다.
  4. 별표ㆍ서식 대상 콤보의 ``전체``를 맨 위이자 기본값으로 올렸다.
  5. 본문 하단 별표ㆍ서식 목록을 ``[별표1] 제목`` 형식으로 바꾸고 글자를
     13px로 줄이고 굵기를 없앴다. 세 줄이던 링크는 제목 오른쪽의 HWP·PDF
     표시 두 개로 줄였다. 제목을 누르면 그 자리에서 펼쳐지며 원문이 본문
     안에 그림으로 들어오고, 축소ㆍ확대(60~200%)ㆍ쪽수ㆍ내려받기를 함께
     보여 준다. 쪽이 많으면 8쪽까지 그리고 나머지는 더 보기로 남긴다.
  6. 조문 기록바(38→26px)와 공용 상태줄(26→18px)을 얇게 줄이고, 모든 화면의
     위아래 선을 왼쪽 메뉴 카드에 맞췄다. 메뉴 목록을 감싸던 포커스 테두리를
     없앴다.
  7. 즐겨찾기 목록 글자를 한 포인트 줄이고 줄 높이를 29→23px로 좁혔다.
     본문 제목 옆 약칭을 제목의 절반 크기로 못박고, 조문 옆 별(☆)을 그 줄
     한가운데로 옮겼다.
- **왜**: 분류마다 테두리 상자를 두니 나란히 선 상자들이 서로 경쟁해
  어수선했다. 콤보는 펼치는 순간 목록이 칸을 그대로 덮어 무엇을 고른
  상태인지 사라졌다. 별표ㆍ서식은 항목마다 링크 세 개가 두 줄을 차지해
  목록이 길어졌고, 원문을 보려면 창을 따로 띄워야 했다.
- **주의**:
  - ``_law_annex_entries``는 응답에서 별표 목록을 뽑는 함수다. 화면 상태를
    같은 이름의 속성으로 담았다가 ``self._law_annex_entries(payload)``가
    리스트를 호출하며 저장 본문 열기가 통째로 막혔다. 화면 상태는
    ``_annex_section_entries``에 담는다. 회귀 테스트로 지킨다.
  - Qt 스타일시트의 ``QListWidget::item``에 ``font-family``를 줘도 항목
    글꼴은 바뀌지 않는다. 목록 위젯 자체에 걸어야 한다.
  - Fusion 스타일은 콤보 목록을 창 밖 별도 위젯으로 띄우고 자기 델리게이트로
    그린다. 창 스타일시트의 ``QComboBox QAbstractItemView`` 규칙이 닿지
    않으므로 ``DropdownComboBox``가 목록에 직접 입힌다. 목록을 감싼 틀을
    투명하게 두면 둥근 모서리 바깥이 검은 조각처럼 보인다.
  - 단추 높이를 파이썬 ``setFixedHeight``로 잡으면 스타일시트의 여백 규칙과
    부딪혀 글자가 상자 밖으로 밀린다. 높이는 스타일시트에서만 정한다.
  - 별표 미리보기는 본문 HTML에 PNG를 data URI로 박는다. 쪽수 제한을 없애면
    수십 쪽짜리 별표에서 본문이 그만큼 무거워진다.
- **남은 일**: 미리보기 첫 배율은 100% 고정이다. 본문 폭보다 넓은 별표는
  가로로 스크롤해야 한다. 폭에 맞춰 자동으로 줄일지는 사용자 확인 대기.
- **검증**: 건축법 시행령 별표1 PDF(137KB·12쪽)를 실제로 내려받아 8쪽이
  본문에 그려지고 확대ㆍ축소ㆍ내려받기가 붙는 것을 확인했다. 저장해 둔 자료
  여섯 유형(law·ai_related·law_reference·expc·molitCgmExpc·admrul)을 실제로
  열어 이름 충돌 오류가 사라진 것도 확인했다. 신규 회귀 테스트 1개를 더해
  전체 726개가 통과하고 Ruff F·E9도 통과한다.

## 2026-09-03 · 화면 아래 선 맞춤, 즐겨찾기 목록 압축, 검색줄 콤보 규격 통일

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 왼쪽 메뉴 카드와 본문 카드의 아래 선이 어긋나 있던 것을 맞추고
  창 아래 여백을 절반(14px→7px)으로 줄였다. 즐겨찾기 목록은 글자를 한 포인트
  줄이고 줄 높이를 29px에서 23px로 좁혔다. 스크롤바를 8px 얇은 막대로 바꾸고,
  검색줄 콤보(기관ㆍ검색범위ㆍ별표 대상)의 너비ㆍ높이ㆍ글자 크기를 모두
  ``SEARCH_COMBO_WIDTH``(170px) 한 규격으로 통일했다.
- **왜**: 탭 안쪽 하단 여백(12px) 때문에 결과 카드가 왼쪽 메뉴 카드보다 먼저
  끝나 두 칸의 아래 선이 어긋나 보였다. 콤보는 화면마다 폭이 110ㆍ170ㆍ86으로
  달라 탭을 오갈 때 검색어 칸의 시작 자리가 흔들렸고, 법령검색 범위 콤보만
  8pt로 작아 글자 크기도 튀었다.
- **주의**:
  - 즐겨찾기 트리는 ``setUniformRowHeights(True)``라 스타일시트의
    ``min-height``로는 줄 높이가 바뀌지 않는다. 높이는
    `FavoriteTreeItemDelegate.sizeHint`가 정하므로 거기서 조절한다.
  - 탭 하단 여백을 0으로 둔 자리가 세 곳(각 탭 `_build_ui`, 법령검색과 키워드
    검색의 크게보기 복원부)이다. 크게보기에서 돌아올 때 12px로 되돌리면 다시
    어긋나므로 복원 값도 0이어야 한다.
- **검증**: 즐겨찾기ㆍ법령검색ㆍ질의회신ㆍ해석례ㆍ판례ㆍ저장내역ㆍ지능형 일곱
  화면의 아래 선이 모두 왼쪽 메뉴 카드와 같은 817px에서 끝나는 것을 확인했다.
  콤보 열 개가 모두 170px·40px·9pt로 나오는 것도 확인했다. 전체 테스트 725개
  통과.

## 2026-09-03 · 카테고리 바 재편, 하단 상태줄 통합, 개정 표기 복원, 캐시 파일명 개편

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 사용자가 한 번에 알려준 열한 가지를 함께 반영했다.
  1. 화면마다 따로 있던 하단 상태줄을 창에 하나만 두고(`SharedStatusBar`),
     그 오른쪽 끝에 ``업데이트 확인``ㆍ``프로그램 정보ㆍ라이선스``를 같은 줄로
     옮겼다. 본문 아래에 상태줄과 단추 줄이 두 겹으로 남던 여백이 사라진다.
  2. 카테고리 바를 ``통합검색 | 지능형 법령검색시스템(AI) | 법령 | 행정규칙 |
     자치법규 | 별표·서식`` 여섯 캡슐로 줄였다. 별표·서식 세 API와 연관ㆍ직접
     두 API는 캡슐 하나가 맡고, 어느 것을 고른 상태인지는 그 캡슐의 tabData가
     들고 있다.
  3. 별표·서식 화면에 대상 콤보(법령/행정규칙/자치법규/전체)를 두고, 전체는
     세 API를 차례로 불러 한 표에 합친다(``__annex_all__``).
  4. 연관ㆍ직접검색 검색줄 맨 왼쪽에 모드 스위치(`SegmentedModeSwitch`)를 넣었다.
  5. 닫으면 다시 뜨지 않던 상단 안내 배너를 없애고, 그 문구를 하단 상태줄의
     검색 전 안내로 옮겼다.
  6. 법령ㆍ행정규칙ㆍ자치법규 목록 검색에 ``본문검색``(``search=2``)을 더했다.
     별표·서식의 기존 1/2/3은 그대로다.
  7. 하단 조문 기록바를 항상 한 줄로 두고 휠로 좌우로 넘기게 했다.
  8. 저장내역ㆍ검색목록 캐시 파일명을 제목 기준으로 바꾸고, 기존 파일을
     이름만 바꿔 옮기는 이전 절차를 넣었다.
  9. 즐겨찾기의 ``선택 내역 열기`` 단추를 없앴다(더블클릭이 같은 일을 한다).
  10. 즐겨찾기의 ``표시할 칸`` 라벨을 없앴다.
  11. ``<개정 2009. 7. 16., …>``ㆍ``[전문개정 …]``을 본문에 되살리고, 본문보다
      한 단계 작은 연한 파란색으로 표시한다.
- **왜**: 개정 표기는 API 응답에 분명히 들어오는데도 화면에서 통째로 빠져
  있었다. `json_text`의 HTML 태그 제거(``<[^>]+>``)가 태그가 아닌 이 표기까지
  지우고 있었다. 캡슐이 열 개로 늘어난 카테고리 바와 화면마다 따로 있던
  상태줄도 같은 이유로 정리했다. 캐시 파일명은 ``law_009419``처럼 번호만
  남아 폴더를 열어도 무엇이 담긴 파일인지 알 수 없었다.
- **주의**:
  - `json_text`의 태그 제거는 개정 표기를 마커로 빼돌린 뒤 되돌리는 순서로
    되어 있다. 이 함수에 새 치환을 넣을 때는 마커 복원(``AMENDMENT_NOTE_n``)
    앞뒤 어디에 들어가야 하는지 확인한다. 단독 ``<삭제>``는 예전부터 쓰던
    별도 마커가 그대로 처리한다.
  - 저장내역 폴더에는 사용자가 붙인 메모와 즐겨찾기 구성이 함께 들어 있어
    다시 받을 수 없다. 이름 규칙을 또 바꿀 일이 있으면 반드시
    `LawDocumentCache.migrate_to_named_files`처럼 지우지 말고 옮긴다.
    ``NAMING_VERSION``을 올려야 이전이 다시 돈다.
  - 저장 폴더의 ``_``로 시작하는 파일(``_목록색인.json``ㆍ``_이름규칙.json``)은
    저장 기록이 아니다. 폴더를 훑는 코드에서 반드시 걸러야 한다. 실제로
    표식 파일을 저장 기록으로 읽어 목록 색인 테스트가 깨졌다.
  - 카테고리 바의 캡슐 하나가 여러 대상을 맡으므로, 같은 캡슐 안에서 대상만
    바뀔 때는 `currentChanged`가 오지 않는다. `select_category`가 그때
    `_category_changed`를 직접 부른다.
  - `SegmentedModeSwitch`의 버튼 높이를 파이썬 `setFixedHeight`로 잡으면
    스타일시트의 여백 규칙과 부딪혀 버튼이 캡슐 밖으로 밀려 나온다. 높이는
    스타일시트(``min-height``/``max-height``)에서만 정한다.
  - 통합검색은 대상마다 ``search=2``의 뜻이 달라(별표는 '해당 법령명') 검색범위
    콤보를 두지 않고 늘 1로 부른다.
- **남은 일**: 조문 팝업 캐시(``# law 캐시/조문``)는 키에 법령명이 없어 파일명이
  여전히 ``009419_002100_…`` 꼴이다. 이름을 넣으려면 캐시 키 자체를 바꿔야 해서
  이번에는 두었다. `LAW_REFERENCE_CACHE_SCHEMA`를 4로 올려 기존 파일은 어차피
  다시 만들어진다.
- **검증**: 건축법 시행령 제2조를 실제 API로 조회해 ``<개정 2009. 7. 16., …
  2024. 12. 17.>``이 법제처 표기 그대로 복원되는 것을 확인했다. 별표·서식 전체
  검색은 세 API(법령 13건ㆍ행정규칙 70건ㆍ자치법규 22건)가 실제로 응답하는 것을
  확인했다. 캐시 이름 이전은 실제 저장 폴더를 복사해 돌려 74건 전부가 제목
  이름으로 옮겨지고 목록 화면이 같은 74건을 읽는 것을 확인했다. 신규 회귀
  테스트 23개를 더해 전체 722개가 통과한다.

## 2026-09-03 · 법령 본문 표·도면 이미지 적용 및 선문자 대체표 제거

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 법령 조문의 ``img`` 태그에서 ``flSeq`` 또는 ``id``를 읽어 법제처 원본
  표·도면을 백그라운드에서 내려받고, 전문 본문·인용 조문 팝업·문서 팝업·조문
  즐겨찾기에 같은 위치로 표시한다. ``<img>…</img>`` 안에 함께 오던 ``┌─│└``
  선문자 대체표는 유효한 이미지 번호가 확인된 경우에만 통째로 제거한다. 이미지
  번호가 없는 태그의 대체 텍스트는 정보 유실을 막기 위해 그대로 둔다. 이미지가
  들어 있는 구버전 저장본은 처음 다시 열 때 한 번만 API로 갱신하며, 조문
  즐겨찾기에서 시작했다면 갱신 후 원래 조문으로 돌아간다. 본문 HTML·인용 팝업
  캐시 버전을 각각 13과 3으로 올렸다.
- **왜**: ``건축법 시행령 제3조의3``처럼 법령 API가 실제 GIF와 텍스트 대체표를
  동시에 주는 경우 기존 파서는 태그만 지워 선문자 표를 본문 상자로 노출하고
  실제 이미지는 버렸다.
- **주의**: 개별 이미지 다운로드가 실패해도 법령 본문은 계속 연다. 이때 선문자
  대체표 대신 국가법령정보센터 원문 이미지 링크가 표시되며, 사용자가 API 갱신을
  누르면 이미지를 다시 내려받는다. 이미지가 없는 기존 저장본은 재조회하지 않는다.
- **검증**: ``건축법 시행령`` 실제 JSON에서 제3조의3 선문자 0개·이미지 위치 1개를
  확인했고, 원본 이미지 2건(``22909013``, ``22909337``)을 실제 다운로드하여 모두
  data URI로 내장되는 것을 확인했다. 신규 회귀 테스트 6개, 전체 테스트 706개,
  Ruff F·E9, ``git diff --check``, Impeccable 수동 탐지기를 통과했다.

## 2026-09-03 · 법령 본문 하단에 별표·서식 링크 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 법률·시행령·시행규칙 본문 API의 ``법령 → 별표 → 별표단위``를
  읽어, 조문이 모두 끝난 본문 하단에 별표·서식 목록을 표시한다. 각 항목에는
  PDF를 앱 안에서 여는 ``미리보기``와 ``원본 다운로드``, ``PDF 다운로드``
  링크를 붙였다. 링크는 Tab 키로도 접근할 수 있고, 조문 목차 마지막의
  ``별표·서식`` 항목으로 하단 목록까지 바로 이동한다. 별표·서식 전용 검색
  탭은 그대로 유지한다. 기존 저장 본문도 이 구성을 다시 그리도록
  ``LAW_RENDER_SNAPSHOT_VERSION``을 12로 올렸다.
- **왜**: 본문 API 응답에 별표 제목과 다운로드 링크가 이미 함께 오는데도
  파서가 조문만 읽어 버려, 사용자가 별표·서식 탭에서 같은 법령을 다시
  검색해야 했다. 본문을 읽는 흐름을 가리지 않도록 요청대로 목록을 위쪽이
  아니라 조문 뒤에 배치했다.
- **주의**: 미리보기는 PDF 링크가 있는 항목에만 표시한다. 원본 링크와 PDF
  링크 중 하나만 제공되는 응답도 있으므로 각각 독립적으로 렌더링해야 한다.
  별표 가지번호와 별지서식 가지번호의 어순은 서로 다르다(``별표 2의3``,
  ``별지 제2호의3서식``).
- **검증**: 신규 회귀 테스트 2개, 관련 테스트 35개, 전체 테스트 700개,
  Ruff F·E9, ``git diff --check`` 통과. Qt 오프스크린 렌더링과 Impeccable
  레이아웃 점검에서 본문 뒤 배치·좁은 폭 줄바꿈·키보드 접근·간격을 확인했다.

## 2026-09-02 · 본문 글꼴 정리와 캐시 본문 머리글ㆍ폐지 강조 보완

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 본문 글꼴 지정을 한곳으로 모았다. ``DETAIL_FONT_FAMILY``와
  본문 HTML용 ``DETAIL_FONT_CSS_FAMILY``를 ``utils/constants.py``에 두고,
  ``DETAIL_DOCUMENT_STYLE``에 다섯 군데 하드코딩돼 있던 ``'Malgun Gothic'``
  을 그 상수로 바꿔 한 곳만 고치면 본문 전체가 함께 바뀌게 했다.
  이 구조로 ``돋움``을 시험해 봤다가 ``맑은고딕``으로 되돌렸다. 법제처
  본문 CSS(``/LSW/css/lsw/common.css``)도 글꼴 선언 8건 중 6건이 ``맑은고딕``
  이고, ``doutm``(돋움)은 ``Gulim, doutm, tahoma`` 목록의 2순위 대체 글꼴일
  뿐이라 본문 글꼴로 쓰는 값이 아니었다. 붙박이 머리글 라벨도 본문과 같은
  맑은고딕으로 맞췄다(그 전에는 화면 UI 글꼴인 Pretendard였다).
  제목 옆 약칭은 제목(21px)의 절반인 11px로 줄였다. 저장 본문(캐시)으로 여는
  법령에도 붙박이 머리글이 뜨도록 했고, 폐지ㆍ연혁 안내는 본문ㆍ팝업 모두
  붉은 굵은 글씨로 강조했다.
- **왜**: 시행령을 저장 본문으로 열면 ``_set_detail_document``를 거치지 않아
  붙박이 머리글이 비어 있었다. 폐지 안내는 기본정보 한 줄이라 눈에 띄지
  않았다.
- **주의**: 저장 본문 경로(``_open_cached_law_document``)는 그려 둔 HTML을
  되살리므로 ``_set_detail_document``를 타지 않는다. 머리글처럼 본문 밖
  위젯에 그리는 것은 이 경로에도 따로 넣어야 한다. 본문 글꼴을 되돌리려면
  ``utils/constants.py``의 두 상수만 고치면 된다.
- **검증**: 전체 테스트 691개, Ruff FㆍE9, ``git diff --check`` 통과. 폐지
  법령(``제14회아시아경기대회지원법``)을 실제 API로 조회해 ``현행연혁코드``가
  ``연혁``으로 오고 안내가 붙는지 확인했다.

## 2026-09-02 · 개정 표기ㆍ연속 인용ㆍ폐지 표시와 빈 결과 안내 보완

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 다섯 가지를 함께 손봤다.
  (1) 조문 끝 ``조문참고자료``(``[전문개정 2009. 2. 6.]``)를 읽어 법제처처럼
  조문 끝 별도 줄에 표시한다. 지금까지는 이 필드를 아무 데서도 읽지 않아
  화면과 AI 평문 모두에서 통째로 빠져 있었다.
  (2) 개정 표기 안의 날짜를 ``2011.4.14`` 에서 법제처 표기 ``2011. 4. 14.``
  로 맞춘다.
  (3) ``제2조제1호 및 제2호``처럼 조를 한 번만 적고 호만 잇는 열거에서 뒤의
  호도 앞 조를 이어받아 링크가 된다.
  (4) 폐지ㆍ연혁 법령을 인용 팝업으로 열면 기본정보 맨 앞에 그 사실을 적는다.
  (5) 검색 결과가 0건이면 표 한가운데에도 안내를 띄운다(아래 상태줄은 유지).
  붙박이 제목 줄은 문서 탭마다 따로 저장해 시행령 탭에서 앞서 보던 법률
  머리글이 남지 않게 했고, 굵기도 보통으로 낮췄다.
- **왜**: 시행령 탭인데 붙박이 줄에 법률 머리글이 그대로 남았다. 개정 표기는
  아예 안 나오거나 ``2011.4.14``처럼 원본 그대로였다. 3단비교에서 ``및 제2호``
  는 링크가 안 걸려 그 호로 못 갔다. 이미 폐지된 법을 인용으로 열어도 현행과
  구분되지 않았다. 0건 검색은 빈 표만 남아 검색이 됐는지 알기 어려웠다.
- **주의**: 날짜 정규화는 ``<개정 …>``ㆍ``[전문개정 …]`` 같은 표기 안에서만
  한다. 본문 전체에 걸면 ``100.5제곱미터``나 ``제44조의3제2항`` 같은 값까지
  건드린다. 조 없는 호 인용은 기본이 평문이며(같은 조문 안을 가리키므로),
  바로 앞 열거에서 조를 밝힌 경우에만 링크한다. 결과 표 이벤트 필터는
  ``__init__`` 앞부분에서 걸리므로 ``eventFilter``가 아직 없는 ``detail_view``
  를 참조하면 안 된다(실제로 48개 테스트가 이 때문에 깨졌다).
- **검증**: 개정 표기ㆍ연속 인용ㆍ폐지 표시 회귀 테스트 6개를 더해 이 파일
  테스트 14개, 전체 테스트 691개, Ruff FㆍE9, ``git diff --check`` 통과.

## 2026-09-02 · 법령 본문 머리글에 약칭ㆍ시행일과 붙박이 제목 줄 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 법령 본문 제목 옆에 법제처 공식 약칭을 ``( 약칭: 국토계획법 )``
  으로 붙이고, 그 아래에 ``[시행 2026. 7. 1.] [법률 제21447호, 2026. 3. 5.,
  타법개정]`` 줄을 국가법령정보센터와 같은 표기로 넣었다. 같은 내용을 본문
  위쪽 붙박이 라벨(``pinnedDocumentHeadline``)에도 띄워, 아래로 굴려도 어느
  법령의 어느 시행일자를 보고 있는지 계속 보이게 했다.
- **왜**: 긴 법령을 굴리면 화면에 조문만 남아 어떤 법령ㆍ시행일자인지
  확인하려면 맨 위로 되돌아가야 했다. 약칭도 본문에 없어서 ``국토계획법``
  처럼 실무에서 쓰는 이름과 대조가 안 됐다.
- **주의**: 본문은 ``QTextBrowser``(QTextDocument)라 CSS ``position:sticky``
  가 동작하지 않는다. 고정 머리글은 반드시 본문 밖 위젯이어야 한다.
  ``법종구분``은 ``{"content": "법률"}``처럼 감싸서 오므로 그대로 문자열로
  쓰면 안 된다. ``_parse_law_detail``이 돌려주는 ``title``은 조항호목 탭
  이름(``f"{title} {unit_label}"``)에도 쓰이므로 약칭을 제목 문자열에 합치지
  말고 머리글 렌더링에서만 붙인다.
- **검증**: 국계법 실제 응답에서 약칭ㆍ시행일 줄이 법제처 표기와 같게
  나오는지, 값이 없는 법령ㆍ행정규칙 응답에서는 빈 값이 되는지 회귀 테스트로
  확인했다.

## 2026-09-02 · 3단비교에서 호를 짚지 않은 항은 한 칸으로 묶기

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 3단비교 표의 법률 열을 블록마다 한 행으로 두던 것을,
  그 항 안의 호ㆍ목을 위임 근거로 삼은 하위법령 조문이 하나도 없을 때만
  항과 딸린 호를 한 칸으로 합치도록 했다(``_merge_law_blocks_for_display``).
  호를 짚어 인용한 조문이 하나라도 있으면 지금처럼 호마다 나눠 그 자리에
  붙인다.
- **왜**: 국계법 제144조는 시행령 제134조가 ``법 제144조제1항``까지만 짚어
  전체가 ① 행에 붙는데, 표는 좌우 행 높이를 맞추므로 법률 열에서 ①과 1호
  사이가 시행령 높이만큼 벌어졌다. 제144조 기준 11행 → 3행이 되어 간격이
  사라진다.
- **주의**: 병합은 근거 목록(``anchor_units``)을 먼저 모은 뒤에 해야 한다.
  합친 블록은 머리 항의 ``hang``만 남고 ``ho``가 비므로, 호를 짚은 근거가
  있는 항을 합치면 그 조문이 갈 자리를 잃는다. 시행규칙 조각도 마지막
  잔여 배치에서 법률 블록을 대상으로 매칭하므로 근거 수집에 함께 넣는다.
- **검증**: 항까지만 인용한 경우 6블록 → 2행, 호(①제2호)를 짚은 경우
  ① 묶음은 유지되고 ② 묶음만 합쳐져 4행이 되는지 회귀 테스트 8개를
  추가했다. 전체 테스트 685개, Ruff FㆍE9, ``git diff --check`` 통과.

## 2026-09-02 · 중첩형 행정규칙 조문 본문 표시

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.3
- **무엇을**: 행정규칙 본문과 부칙이 최상위 문자열로 오는 형식뿐 아니라
  `조문 → 조문내용` 및 `부칙 → 부칙내용` 배열로 오는 형식도 합쳐서
  본문 팝업ㆍ저장 평문에 표시하도록 했다. 중첩 본문 안의 이미지 ID도 기존과
  같이 찾아 내려받도록 이미지 탐색을 재귀화했다.
- **왜**: `훈령ㆍ예규 등의 발령 및 관리에 관한 규정`은 목록과 기본정보는
  정상 조회되지만 실제 조문이 `AdmRulService.조문.조문내용`에 있어,
  `AdmRulService.조문내용`만 읽던 화면에는 본문이 비어 보였다.
- **주의**: 법제처 행정규칙 상세 JSON은 규칙마다 평문형과 법령식 중첩형이
  섞여 있다. 한 형식으로 대체하지 말고 평문형을 먼저 읽은 뒤 없을 때
  중첩형을 읽어야 기존 지침ㆍ고시 본문과 이미지 위치가 유지된다.
- **검증**: 실제 API 응답에서 해당 규칙의 조문 4,742자와 부칙 850자를
  추출했다. 새 회귀 테스트 3개, 관련 테스트 123개 및 전체 테스트 684개,
  Ruff F/E9, `git diff --check`를 통과했다.

## 2026-09-02 · 법령 인용 제목의 행정규칙 팝업 연결

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.3
- **무엇을**: 본문에서 법령 링크로 만들어진 제목을 현행 법령과 연혁법령에서
  찾지 못하면 같은 이름의 행정규칙을 다시 검색해 기존 참조 팝업에 본문을
  표시하도록 했다. 행정규칙으로 전환된 팝업에는 행정규칙 새로고침 요청을
  저장해 이후에도 법령 API로 잘못 돌아가지 않게 했다.
- **왜**: `훈령ㆍ예규 등의 발령 및 관리에 관한 규정`은 대통령훈령인
  행정규칙인데, 따옴표 안 제목을 일괄 생성한 `lawref:` 링크가 법령ㆍ연혁법령만
  조회하여 이미지는 정상 표시돼도 해당 제목 팝업은 열리지 않았다.
- **주의**: 원문은 가운뎃점(`·`, U+00B7), 법제처 등록명은 아래아점(`ㆍ`,
  U+318D)을 쓰는 경우가 있어 행정규칙명 비교 때 이 두 구분점과 공백만
  제거한다. 다른 문장부호까지 무시해 동명 규칙 오탐 범위를 넓히지 않는다.
- **검증**: 실제 법제처 API에서 해당 규칙이 행정규칙 일련번호
  `2200000078285`로 해석됨을 확인했다. 새 회귀 테스트 3개, 관련 테스트
  31개 및 전체 테스트 681개, Ruff F/E9, `git diff --check`를 통과했다.

## 2026-09-02 · 행정규칙 본문 표 이미지 누락 보정

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.3
- **무엇을**: 행정규칙 ``조문내용``에 들어 있는 ``<img id="...">`` 위치를
  내부 표식으로 보존하고, 법제처 ``flDownload.do?flSeq=...``에서 이미지를
  백그라운드로 받아 본문·행정규칙 팝업·연관검색 조문에 표시하게 했다.
  이미지가 일부 실패하면 본문은 계속 열고 해당 위치에 공식 원문 이미지
  링크를 표시한다. 이미지 ID를 이미 잃은 구버전 행정규칙 저장본은 처음
  다시 열 때 API로 갱신되도록 파싱 버전을 30으로 올렸다.
- **왜**: ``조달수수료 고시``의 요율표·면제표·감경표가 모두 이미지 태그로
  제공되는데 기존 ``json_text``가 HTML 태그와 함께 이미지 ID까지 지워,
  표 6개가 빠지고 제목과 주석만 붙어서 표시됐다.
- **주의**: 법제처 행정규칙 JSON은 이미지 URL이 아니라 파일 일련번호만
  준다. 일반 HTML 태그 제거 전에 위치를 보존해야 하며, 화면 스레드에서
  직접 다운로드하면 UI가 멈추므로 조회 작업 스레드에서 처리해야 한다.
- **검증**: 누락 사례 회귀 테스트 4개와 관련 테스트 79개, 전체 테스트
  678개, Ruff F/E9 및 ``git diff --check``를 통과했다.

## 2026-09-01 · OPEN API 활용가이드 응답 확인용 단독 스크립트 추가

- **커밋**: de74a98
- **버전**: 1.3.3
- **무엇을**: ``test_open_api_guide.py``를 새로 추가했다. 법제처
  ``open.law.go.kr/LSO/openApi/guideResult.do``가 어떤 형태로 내려오는지
  확인하고, 그 가이드에 적힌 요청 URL로 실제 API까지 호출해
  JSON/XML 응답 구조를 요약해 보여주는 단독 실행 스크립트다.
  ``list``/``guide``/``raw``/``call`` 4개 명령과 인자 없는 대화형 모드를 둔다.
- **왜**: 새 target을 붙이기 전에 요청 변수와 응답 필드가 실제로 어떻게
  오는지 프로그램 본체와 분리해서 확인할 수단이 필요했다.
- **주의**: 가이드 목록 페이지는 링크에 URL이 없고 JS
  ``openApiGuide('htmlName')`` 호출만 있어서 htmlName을 정규식으로 긁는다.
  ``guideResult.do``는 원래 POST 폼이지만 GET 쿼리스트링으로도 동일하게
  응답한다. 응답 헤더의 charset 추정이 틀려서 UTF-8로 고정 디코딩해야 한다.
  가이드 섹션 구성은 API마다 달라(샘플 URL이 제목 없는 표로만 있는 경우 등)
  dt/table을 등장 순서대로 훑는다.
- **검증**: 실제 호출로 가이드 195건 목록, ``admrulListGuide`` 가이드 파싱,
  ``target=admrul`` JSON/XML 호출(OC=test)까지 확인했다. 프로그램 본체는
  건드리지 않았다.

## 2026-09-01 · 통합검색 연관 조문 범위와 최근검색어 폭 보정

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.3
- **무엇을**: 통합검색이 연관검색 API의 법령 조문(``search=0``)과
  행정규칙 조문(``search=1``)을 병렬로 조회해 두 결과를 모두 조문 행으로
  표시하게 했다. 최근검색어 버튼은 최대 104px의 칩으로 제한하고 남은 폭은
  오른쪽 여백에 배정하되, 검색어가 많거나 창이 좁으면 함께 줄어들게 했다.
- **왜**: 통합검색은 연관검색을 ``search=1``로만 호출해 행정규칙 조문만
  보였고, 최근검색어가 하나뿐이면 버튼 하나가 남은 한 줄 전체를 차지했다.
- **주의**: 같은 ``aiRltLs`` target을 범위만 달리해 요청하므로 target을
  사전 키로 쓰면 한 응답이 덮인다. 요청 순번을 키로 보존하는
  ``search_agency_scopes``를 사용한다. 최근검색어는 104px 고정이 아니라
  최대 폭이므로 좁은 화면에서는 10개가 한 줄에 들어가도록 축소된다.
- **검증**: 실제 법제처 API에서 ``공간재구조화``가 법령 조문 10건과
  행정규칙 조문 10건을 각각 반환함을 확인했다. 관련 집중 테스트 21개와
  전체 테스트 674개, Ruff F/E9, Impeccable 레이아웃 탐지와 렌더링 확인이
  통과했다.

## 2026-09-01 · 로컬 EXE와 릴리스의 명시 요청 원칙 기록

- **커밋**: (이 항목과 같은 커밋 · 코드 변경 없음)
- **버전**: 1.3.3
- **무엇을**: 프로젝트 ``AGENTS.md``에 일반 소스 수정 때 로컬 EXE를
  빌드하지 않고, 버전 상승ㆍ태그ㆍGitHub Release는 사용자가 명시적으로
  릴리스를 요청할 때만 수행한다고 기록했다. 릴리스도 기본적으로 GitHub
  Actions 빌드만 사용하며 로컬 EXE는 별도 요청이 있을 때만 만든다.
- **왜**: 기존 지침은 동작 변경 뒤 로컬 EXE 재빌드를 요구했고 릴리스 실행
  조건은 적지 않아, 사용자의 실제 작업 방식보다 빌드ㆍ배포 범위가 넓었다.
- **주의**: 커밋ㆍ푸시 요청은 릴리스 요청이 아니다. 릴리스 요청도 로컬 EXE
  생성 요청을 포함하지 않는다.

## 2026-09-01 · 통합검색 연관 행정규칙도 선택 조문만 표시

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.3
- **무엇을**: 통합검색에 합쳐진 연관검색ㆍ직접검색의 행정규칙 결과는
  본문 전문에서 검색결과가 가리킨 ``제N조(의M)``만 잘라 표시하고 저장한다.
  단독 연관검색도 같은 공용 조문 추출 함수를 사용하게 했으며, 동일한
  행정규칙의 제2조와 제3조는 조문 코드가 포함된 서로 다른 캐시 키를 쓴다.
- **왜**: 1.3.2에서 잘못된 행정규칙 ID를 본문용 일련번호로 바꿔 조회 오류는
  해결했지만, 통합검색은 받은 전문을 그대로 표시해 단독 연관검색처럼
  선택한 제2조만 보여 주지 않았다. 조문별 캐시를 나누지 않으면 먼저 저장한
  제2조가 같은 규칙의 제3조를 열 때도 재사용될 수 있었다.
- **주의**: 통합검색의 일반 법령 키워드 결과는 기존 설계대로 저장된 전문을
  열고 해당 조문으로 이동한다. 이번 조문 단위 저장은 본문 API가 전문만
  제공하는 행정규칙 키워드 결과에 한정한다.
- **검증**: 제1조부터 제3조와 부칙이 든 응답에서 제2조 결과만 남는지,
  단독 연관검색도 같은 결과인지, 제2조ㆍ제3조 캐시 키가 다른지 회귀 테스트를
  추가했다. 집중 테스트 29개와 전체 테스트 671개, Ruff FㆍE9 및
  ``git diff --check``가 통과했다.

## 2026-09-01 · 통합검색 연관 행정규칙의 본문 ID 변환

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 통합검색에 합쳐진 연관검색ㆍ직접검색의 행정규칙 결과는
  ``행정규칙ID``를 곧바로 본문 API에 보내지 않고, 행정규칙 목록에서 같은
  이름과 IDㆍ발령일자ㆍ발령번호를 대조해 ``행정규칙일련번호``를 찾은 뒤
  본문을 조회하도록 바꿨다. 단독 연관검색도 같은 공용 변환을 사용한다.
- **왜**: 키워드 API의 ``행정규칙ID``와 행정규칙 본문 API가 요구하는
  ``행정규칙일련번호``는 서로 다른 값이다. 통합검색에서
  ``공간재구조화계획 수립 등에 관한 지침`` 제2조를 열면 ID ``46796``을
  본문 API에 그대로 보내 빈 응답을 받고 "행정규칙 본문을 찾지 못했습니다"
  오류를 표시했다.
- **주의**: 키워드 행정규칙 ID를 목록검색의 일련번호와 같은 값으로 취급하지
  않는다. 동명 규칙과 개정 이력이 있을 수 있으므로 이름만 보지 말고 원래
  키워드 결과의 IDㆍ발령일자ㆍ발령번호도 후보 선택에 사용한다.
- **검증**: 재현 회귀 테스트에서 ID ``46796``을 본문용 일련번호로 변환한
  뒤에만 본문 API가 호출되는지 확인했다. 실제 법제처 API에서도 일련번호
  ``2100000245296``으로 변환되고 조문 본문이 반환됐다. 키워드 관련 테스트
  42개와 전체 테스트 668개, Ruff FㆍE9 및 ``git diff --check``가 통과했다.

## 2026-09-01 · 바깥 AI의 표 포함 답변 하단 여백 제거

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 대화의 사용자 글ㆍAI 답변ㆍ도구 기록ㆍ인용 확인에 쓰는
  줄바꿈 QLabel을 현재 배치 폭으로 높이를 계산하는 전용 라벨로 바꿨다.
  실제 Claude 답변처럼 제목ㆍ표ㆍ구분선ㆍ여러 조문 링크가 섞인 긴 응답의
  마지막 인용문과 스크롤 끝이 정상 하단 여백 안에서 맞도록 했다.
- **왜**: Qt QLabel의 기본 ``sizeHint``가 넓은 바깥 AI에서도 더 좁은 임시
  폭으로 리치텍스트 높이를 계산해 두었다. 첫 수정에서 레이아웃 stretch는
  제거됐지만 이 과대 높이가 콘텐츠 위젯의 최소 높이로 남아, 답이 끝난 뒤
  아래로 더 스크롤하면 대화가 사라질 만큼 빈 공간이 생겼다.
- **주의**: 말풍선 컨테이너의 끝만 측정하면 컨테이너 자체가 잘못 늘어난
  경우도 정상으로 보인다. 하단 여백 회귀 테스트는 마지막 실제 콘텐츠인
  인용 확인 라벨의 끝과 스크롤 viewport의 끝을 비교해야 한다.
- **검증**: 표 6개와 여러 조문 링크를 포함한 완료 답변을 스트리밍한 뒤
  실제 인용 라벨 하단을 확인하는 회귀 테스트를 추가했다. AI 패널 테스트
  49개와 전체 테스트 667개, Ruff FㆍE9, ``git diff --check`` 및
  Impeccable 검사가 모두 통과했다.

## 2026-09-01 · 즐겨찾기 이동 뒤 폴더 접힘 상태 유지

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 즐겨찾기 트리를 다시 그리기 전에 폴더 ID별 펼침 상태를
  기억하고, 일반 갱신 뒤 같은 폴더에 복원하도록 바꿨다. 최초 표시와 검색
  중에는 기존처럼 필요한 내용을 모두 펼친다.
- **왜**: 즐겨찾기를 폴더에 드래그하면 배치 저장이 캐시 변경 신호를 보내
  전체 목록이 다시 그려졌고, 마지막의 ``expandAll`` 때문에 사용자가 접어
  둔 다른 폴더까지 전부 열렸다.
- **주의**: 화면 행의 위치나 폴더 이름이 아니라 변경되지 않는 폴더 ID로
  상태를 복원해야 이름 변경ㆍ순서 이동ㆍ하위 폴더에도 안전하다.
- **검증**: 접힌 폴더 두 개 중 하나에 즐겨찾기를 넣고 캐시 변경으로 목록이
  재생성된 뒤에도 두 폴더가 접힌 상태인지 확인하는 회귀 테스트를 추가했다.
  관련 테스트 12개와 전체 테스트 666개, Ruff FㆍE9,
  ``git diff --check`` 및 Impeccable 검사가 모두 통과했다.

## 2026-09-01 · 바깥 AI와 본문 AI의 열린 채팅을 실시간 동기화

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 채팅 저장ㆍ이름 변경ㆍ고정ㆍ삭제 때 제공자와 채팅 번호를
  다른 AI 패널에 알리는 신호를 추가했다. 반대편 패널은 저장 목록을 다시
  읽고 같은 채팅을 열고 있으면 메시지ㆍ본문 문맥ㆍ모델을 즉시 갱신한다.
  바깥 AI 대화창의 하단 stretch를 없애고 내용을 위쪽 정렬로 바꿔 마지막
  말풍선 뒤에 이전 대화 높이만큼 빈 스크롤 공간이 남지 않게 했다.
- **왜**: 바깥 AI와 본문 AI는 같은 QSettings 저장소를 사용하지만 메모리의
  현재 대화는 각각 보관했다. 따라서 본문 AI에서 이어 쓴 내용이 저장돼도
  이미 열린 바깥 AI에는 다시 들어갈 계기가 없었고, 대화 레이아웃의 가변
  spacer는 높이 재계산 뒤에도 불필요한 하단 스크롤 범위를 만들 수 있었다.
- **주의**: 채팅 변경 신호를 보내기 전에 QSettings를 ``sync``해야 패널마다
  별도 QSettings 객체를 쓰는 경우에도 방금 저장한 내용을 즉시 읽는다.
  외부 갱신 슬롯에서는 다시 변경 신호를 보내지 않아 패널 간 순환 호출을
  만들지 않는다.
- **검증**: 패널별 QSettings 객체를 사용한 양방향 열린 채팅 동기화와 마지막
  말풍선 하단 스크롤 범위 회귀 테스트를 추가했다. AI 패널 집중 테스트 48개,
  전체 테스트 665개, 변경 파일 Ruff FㆍE9와 ``git diff --check``가 통과했다.
  Impeccable 검사 경고 3건은 변경하지 않은 기존 본문 카드 강조선이다.

## 2026-09-01 · 3단비교 목 번호를 허목까지 확장

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 법령 목 번호의 공통 순서를 ``가``부터 ``허``까지 명시하고,
  본문 줄바꿈ㆍ조문 인용ㆍ3단비교 행 정렬ㆍAI 답변 링크가 모두 그 순서를
  사용하도록 통일했다. 국토계획법 제28조 3단비교에서 시행령 제22조제7항
  제3호의 ``파. 하수도``부터 ``너. 그 밖에 국토교통부령으로 정하는 시설``
  까지가 각각 줄바꿈되고, 시행규칙 제2조의2가 ``너.`` 행에 놓이며
  ``국토교통부령`` 링크가 연결되는 회귀 테스트를 추가했다.
- **왜**: 기존 정규식은 목 번호를 ``가``부터 ``하``까지만 허용했다.
  그래서 ``거ㆍ너``를 목으로 읽지 못했고, ``영 제22조제7항제3호너목``도
  제3호까지만 추출되어 시행규칙의 행 배치와 링크 생성이 함께 어긋났다.
- **주의**: 유니코드 범위식 ``[가-하]``는 ``각`` 같은 일반 음절까지
  포함한다. 목 번호를 추가할 때는 범위식을 쓰지 말고
  ``KOREAN_ITEM_MARKERS``의 실제 법령 순번을 사용해야 한다.
- **검증**: 제28조 재현 테스트를 포함한 3단비교 테스트 45개, 관련 정규식ㆍ
  링크 회귀 테스트 166개, 전체 테스트 663개가 통과했다. 변경 파일 Ruff
  FㆍE9 검사도 통과했다.

## 2026-08-31 · Impeccable UI 비평 보고서를 보관

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 현재 PySide6 화면의 900px·1440px 캡처를 Impeccable 기준으로
  검토한 비평 보고서를 ``.impeccable/critique``에 보관했다.
  비평 이후 준비하던 개선 코드 변경은 모두 되돌렸다.
- **왜**: 토큰 제한으로 이번 작업 범위를 비평 보고서 생성까지로 확정했으며,
  후속 개선을 시작할 때 우선순위와 근거를 다시 활용할 수 있도록 남겼다.
- **주의**: 보고서의 P2·P3 권고사항은 분석 결과일 뿐 아직 구현되지 않았다.
- **남은 일**: 빈 결과 온보딩, 선택지 단순화, 용어 설명, 페이지 제목 계층
  개선은 다음 UI 개선 작업에서 개별 검증 후 적용한다.

## 2026-08-31 · 대량 검색 결과의 저장 여부 확인을 일괄 처리

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: ``LawDocumentCache.saved_keys_for_rows``를 추가해 검색 결과에
  해당하는 저장 파일명을 폴더에서 한 번만 모으고, 통합검색 결과 정렬·표
  생성·체크 표시 갱신이 그 키 집합을 재사용하도록 바꿨다. 법령 외 판례·
  행정규칙 등의 상세 화면은 파일명이 있어도 실제 스냅샷 형식이 정상인지
  기존처럼 검증한다. 정상 법령, 누락 법령, 정상 스냅샷, 손상 스냅샷을
  함께 확인하는 회귀 테스트를 추가했다.
- **왜**: 1,000행 결과표를 그릴 때 저장 체크 한 칸마다 ``Path.is_file``을
  호출해 최대 1,000회의 파일 상태 조회가 발생했다. 프로파일에서 이 경로가
  결과표 생성 시간의 큰 부분을 차지했다. 같은 시점의 저장 폴더 상태는 한
  번의 디렉터리 스캔으로 충분하므로 행별 시스템 호출을 없앴다.
- **주의**: 법령 파일은 기존 ``has``와 같이 파일 존재 여부를 저장 상태로
  보지만, 법령 외 결과는 ``load_snapshot`` 검증을 생략하면 손상 JSON도
  체크된 것으로 표시된다. 일괄 조회를 다른 검색 탭으로 넓힐 때도 이 구분을
  유지한다.
- **검증**: 동일한 ``perf_baseline.py`` 측정에서 통합검색 1,000행 표 생성
  중앙값이 304.68ms에서 224.78ms로 약 26% 줄었다. 전체 테스트 660개와
  변경 파일 Ruff F·E9, ``git diff --check``가 통과했다.

## 2026-08-31 · UI 기술 감사를 반영해 접근성·적응형 화면·AI 시작 경로를 정비

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: ``impeccable`` 기술 감사 기준으로 제품의 North Star와
  반응형·접근성 계약을 ``DESIGN.md``와 ``.impeccable/design.json``에
  고정했다. 창 폭이 1100px 미만이면 136px 왼쪽 메뉴 대신 헤더의 화면
  선택기를 쓰게 하고 최소 창 폭을 900px로 낮췄다. 작업대 핵심 색상을
  ``WORKBENCH_COLORS``로 모아 본문·하단 링크 대비와 키보드 포커스를
  보강했으며, 닫기·도움말·즐겨찾기·3단비교·색상표 등 작은 동작의
  클릭 영역과 접근성 이름을 키웠다. 조문 별표를 24px로 키운 만큼 제목
  왼쪽 여백도 22px로 맞춰 본문과 겹치지 않게 했다.

  AI 패널은 CLI 상태 확인과 Gemini 모델 목록을 앱 전체 조정자에서 한 번만
  공유한다. 숨겨진 패널은 실제로 보일 때까지 CLI·모델 조회를 시작하지
  않으며, 모델 키 검증과 목록 갱신은 같은 API 응답으로 끝낸다. CLI
  subprocess는 취소 신호를 0.2초 간격으로 확인해 창 종료 시 자식 프로세스
  트리까지 정리한다. ``DeferredWrapTextBrowser``의 다음 틱 스크롤 복원은
  수신 객체를 지정한 타이머로 바꿔, 닫힌 위젯을 뒤늦게 호출하던 Qt 접근
  위반도 없앴다.
- **왜**: 900px 창에서 왼쪽 메뉴가 본문 폭을 과도하게 차지했고, 작은
  아이콘 동작 다수가 Tab 포커스를 받지 않거나 14~22px에 머물렀다. 또한
  생성만 된 AI 패널들이 저장된 키로 같은 네트워크·CLI 확인을 반복해 앱
  시작과 테스트 종료를 늦추고 ``QThread: Destroyed while thread is still
  running`` 경고를 만들었다. 전체 테스트의 Qt 접근 위반은 줄바꿈 복원
  ``singleShot``이 파괴된 본문 위젯보다 오래 살아남는 것이 원인이었다.
- **주의**: 공용 AI 조정자는 패널이 아니라 ``QApplication`` 수명에 속한다.
  개별 패널 ``shutdown``에서는 답변·설치 작업만 멈추고, 공용 CLI·모델
  조정자는 ``LawSearchWindow.closeEvent``와 앱 종료 신호에서 정리한다.
  저장된 Gemini 키가 있어도 숨은 패널에서는 목록을 받지 않으므로, 자동
  모델 갱신을 다시 생성자로 옮기지 않는다. 객체 메서드를 다음 틱에 부르는
  새 ``QTimer.singleShot``도 가능한 한 수신 객체 overload를 사용한다.
- **검증**: 전체 테스트 658개가 단일 실행으로 통과했고 변경 Python 파일의
  Ruff F·E9 및 ``git diff --check``가 통과했다. 독립 프로세스에서 900×700,
  1440×860 화면을 다시 렌더링해 좁은 창 선택기 전환, 넓은 창 왼쪽 메뉴,
  결과표·하단 링크의 잘림과 대비를 눈으로 확인했다.

## 2026-08-31 · 세 검색 화면의 본문 머리글 조절부를 공용화

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 법령검색, 연관·직접검색, 자료검색에 각각 복제되어 있던
  ``본문 / 글자 / 크기 조절`` 위젯을 ``build_detail_header_controls``로
  추출했다. 7.0~18.0pt 범위, 0.5pt 증감, 라벨·입력 폭도 의미 있는 공용
  상수와 정규화 함수로 모았다. 기존 QSS ``objectName``과 각 화면의 신호
  연결·저장 키는 유지했다. 공용 UI 체계와 추출 기준을
  ``docs/UI_DESIGN_SYSTEM.md``에 기록하고 위젯 계약 테스트를 추가했다.
- **왜**: 같은 의도와 규격의 조절부가 세 화면에서 따로 관리되어 한 화면만
  범위·툴팁·폭이 달라질 위험이 있었다. 이미 있는 ``ui/widgets.py``와
  ``ui/theme.py``의 역할을 확장해 새 디자인 시스템을 중복해서 만들지 않고
  반복되는 규격만 한곳에서 관리하려는 변경이다.
- **주의**: 더블클릭 크게 보기와 글자 크기 저장은 화면마다 수명주기와 저장
  방식이 다르므로 공용 빌더가 신호까지 소유하지 않는다. 새 검색 화면에서
  이 조절부를 쓸 때 반환된 ``title``과 ``font_spin``에 화면별 슬롯을 직접
  연결해야 한다.
- **검증**: 공용 위젯 계약, 글자 크기 범위·증감 정규화, 세 실제 탭 생성과
  레이아웃을 포함한 집중 테스트 13개가 통과했다. 변경 파일 Ruff F·E9와
  ``git diff --check``도 통과했다. 자료검색 탭 종료 시 테스트 결과와 별개로
  기존 ``QThread: Destroyed while thread is still running`` 경고는 남는다.

## 2026-08-31 · 대형 법령 본문의 조문 링크·여백 서식을 한 번에 적용

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.2
- **무엇을**: 저장된 법령 전문을 열 때 조문마다 따로 실행하던
  ``QTextCursor.mergeCharFormat``과 ``setBlockFormat``을 각각 하나의
  ``beginEditBlock``/``endEditBlock`` 구간으로 묶었다. 링크 범위와 조문별
  여백은 그대로 유지하되 Qt 문서 레이아웃은 묶음 끝에서 한 번만
  확정한다. 12개 조문에 링크를 보강해도 ``contentsChanged``가 최대
  3번만 발생하고 링크 12개가 모두 남는 회귀 테스트를 추가했다.
- **왜**: 실제 저장본(국토계획법, 조문 182개)을 프로파일링하니 첫 표시
  470ms 가운데 ``_set_three_stage_articles``가 328ms였다. 그 안에서
  위임 링크 서식 223회의 ``mergeCharFormat``이 113ms, 조문 여백 182회의
  ``setBlockFormat``이 85ms를 썼다. 서식 계산 자체보다 각 호출 뒤의
  반복 재배치가 병목이었다.
- **주의**: 편집 블록은 링크와 문단 서식을 없애는 최적화가 아니다.
  같은 커서를 범위마다 옮겨 기존 서식을 모두 적용한 뒤 알림과 레이아웃만
  합친다. 조문별 링크 개수나 별표 버튼 개수를 줄이지 않는다.
- **검증**: 같은 저장본의 첫 표시가 469.82ms에서 293.34~297.11ms로
  줄어 약 37% 빨라졌다. 조문 링크·스냅샷 관련 테스트 58개와 새 회귀
  테스트가 통과했고, 변경 파일 Ruff F·E9 및 ``git diff --check``가
  통과했다. ``test_document_tab_favorite.py``의 18개 단언도 모두
  통과했지만 종료 때 기존 백그라운드 ``QThread`` 경고로 프로세스 코드는
  1이었다. 전체 스위트는 변경 전부터 있는 ``test_ai_cli_status_check``의
  ``object.__setattr__`` TypeError와 별도 Qt 접근 위반 때문에 완주하지
  못했다.

## 2026-08-31 · (미해결) 본문 여는 첫 클릭 지연 — 조사 기록

- **커밋**: (이 항목과 같은 커밋 · 코드 변경 없음)
- **버전**: 1.3.2 · 원인 미확정
- **무엇을**: 코드는 고치지 않았다. 재현 절차와 지금까지 재 본 숫자,
  그리고 **아닌 것으로 밝혀진 것**을 남긴다. 다음에 이어서 볼 때 같은
  길을 다시 파지 않기 위해서다.
- **제보한 증상**: 즐겨찾기로 국토계획법을 연 뒤, 법령검색에서 같은
  법령을 검색해 열면 딜레이가 있다. 그냥 열 때는 바로 열린다.
- **재현 못 함**: 헤드리스로 같은 순서를 그대로 밟으면 1) 즐겨찾기
  265ms → 2) 법령검색 17ms로, 오히려 두 번째가 빠르다. 실제 화면에서만
  나는 현상이거나 네트워크 상태에 달린 것으로 보인다.
- **아닌 것으로 확인한 것**:
  - 이번에 넣은 저장 화면 완결성 검사(``_snapshot_covers_payload``)는
    1.08ms다(조문단위 202개 · 본문 98,084자 기준). 지연과 무관하다.
  - CLI 상태 확인(``AiCliCheckWorker``)은 ``moveToThread``로 별도
    스레드에서 돈다(``ai_chat_panel.py:1422``). 화면을 막지 않는다.
    다만 ``cli_version`` 수정 전에는 NameError로 즉시 죽어 아무 일도
    하지 않았고, 이제는 ``subprocess`` 8회를 실제로 돈다. 화면을 막지는
    않지만 켤 때 일이 늘어난 것은 사실이다.
- **의심하지만 확인 못 한 것**:
  - ``ai_chat_panel.py:1380``의 ``QTimer.singleShot(0, self._reload_models)``
    는 메인 스레드에서 ``validate_api_key``ㆍ``fetch_models``를 동기로
    부른다. 저장된 Gemini 키가 있으면 창이 뜬 직후 UI가 멎는다. 측정상
    ``fetch_models``가 **4회** 불렸다(패널마다 따로 받는다). 다만 이것은
    켠 직후에만 나는 시간 의존 현상이라, 제보한 순서 의존 증상과는
    맞지 않는다.
  - 본문을 열면 3단비교 링크 보강이 ``get_three_stage_comparison`` API를
    부른다. 여기 이미 "이 요청이 몇 초씩 걸리면서 마치 문서 여는 것
    자체가 느려진 것처럼 보였다"는 주석이 있다(``resource_search.py:3266``).
    응답은 ``_three_stage_payload_cache``에 담겨 다음부터는 빠르다.
- **다음에 할 일**: 인터넷을 끊고 같은 순서를 재현해 본다. 오프라인에서도
  똑같이 느리면 네트워크(3단비교ㆍGemini)가 아니라 렌더링 쪽이다. 그때는
  본문 여는 구간마다 시간을 찍는 임시 로그를 넣어 실제 화면에서 잰다.
- **참고 숫자**: 창을 켜고 6초 기다린 뒤 저장본 열기는 307ms, 같은 것을
  다시 열면 23ms다. 307ms의 내역은 ``setBlockFormat`` 182회 48ms,
  ``_set_three_stage_articles`` 110ms, ``setHtml`` 43ms다. 지연과 별개로
  줄일 여지가 있는 자리다.


## 2026-08-31 · 저장 화면이 자기 신원을 달고 다니게 한다

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: ``_law_render_snapshot_from_state``가 만드는 화면에
  ``rendered_for``를 넣어 그 화면이 어느 문서의 것인지 스스로 밝히게
  했다. 신원은 저장소가 파일 이름을 정할 때 쓰는 ``LawDocumentCache.
  _cache_key``를 그대로 빌린다. 화면을 저장하는 세 자리
  (``_show_detail``의 새 저장, 저장본을 열며 갱신하는 자리, 3단비교 링크
  보강) 모두에서 ``_snapshot_belongs_to``로 대조하고, 어긋나면 쓰지
  않는다. 새 저장에서 어긋나면 원문만 저장하고 화면은 다음에 열 때 다시
  그린다.
- **왜**: 앞 항목에서 넣은 조문 수 대조는 결과물을 보고 걸러내는
  안전망이지 원인이 아니었다. 진짜 원인은 "이건 어느 문서인가"를 두 번
  따로 계산한 것이다. 화면(``snapshot``)은 ``state``에서 나오는데 저장
  위치는 ``request["item_id"]``에서 따로 나왔다. 특히 3단비교는 법령
  단위 자료가 필요해 ``_three_stage_link_row``가 조항호목 탭일 때
  **일부러** 원본 법령 행으로 바꿔치기한다. 그래서 "링크를 붙일 화면"과
  "저장할 파일"이 서로 다른 문서를 가리키는 것이 정상 동작인데, 둘이
  어긋나는지 확인하는 자리가 없었다.
- **주의**: 신원을 못 밝히는 화면(``rendered_for``가 없는 예전 저장본)은
  막지 않는다. 막으면 멀쩡한 저장까지 사라진다. 그런 화면은 조문 수를
  보는 ``_snapshot_covers_payload``가 계속 받친다. 신원 규칙을 두 벌
  두면 언젠가 어긋나고 그 순간 이 검사가 무력해지므로, 저장소의
  ``_cache_key``를 빌려 쓰는 것을 유지한다. 조항호목 탭은 신원이
  ``law_article_...``이라 법령 전문 파일(``law_009294``)과 절대 겹치지
  않는다.
- **검증**: 전체 644개 테스트 통과(새 회귀 8개 포함), Ruff F·E9 통과.
  실제 사고 모양(조문 화면 → 법령 전문 파일)이 막히고, 같은 파일끼리는
  통과하는 것을 확인했다. 즐겨찾기에서 여는 경로는 조문 185개ㆍ98,087자로
  정상이고, 저장 파일에 ``rendered_for: law_009294``가 남는 것을 확인했다.


## 2026-08-31 · 조문 하나짜리 화면이 법령 전문 저장본을 덮던 문제

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 저장 화면이 그 법령의 조문을 대부분 담고 있는지 재는
  ``_snapshot_covers_payload``를 넣고, 되살릴 때(``_restore_cached_law_render``)와
  3단비교 링크 보강이 저장할 때(``_apply_three_stage_links``) 양쪽에서
  확인한다. 원문 조문이 4개 미만이면 판단하지 않고, 저장 화면이 원문
  조문의 절반 미만만 담고 있으면 믿지 않는다.
- **왜**: 즐겨찾기에서 국토계획법을 열면 제1조만 떴다. 저장 파일을 열어
  보니 ``payload``에는 조문단위가 202개 그대로인데 ``rendered_html``은
  1,531자, ``rendered_plain_text``는 47자였고 ``rendered_toc_entries``는
  ``[[4, "제1조(목적)", "toc-0-0"]]`` 하나뿐이었다. 3단비교 링크 보강이
  조문 하나만 담긴 화면을 법령 전문 저장 파일에 덮어쓴 것이다. 거기
  이미 "다른 법령 화면이 덮으면 안 된다"는 방어가 있었지만 법령ID만
  비교했고, 조문 단위 화면도 법령ID가 같아 그대로 통과했다. 되살리는
  쪽도 첫 줄이 법령 이름과 맞는지만 봐서, 첫 줄이 여전히 "국토의 계획 및
  이용에 관한 법률"이라 검사를 통과했다. 그래서 한 번 망가지면 스스로
  낫지 않고 열 때마다 제1조만 떴다.
- **주의**: 절반이라는 선은 아주 너그럽게 잡은 것이다. 멀쩡한 저장
  화면을 잘못 버리면 켤 때마다 원문에서 다시 그려 오히려 느려진다.
  조문 수가 적은 법령은 비교가 의미 없으므로 그냥 통과시킨다. 가지번호
  (제3조의2)는 제3조와 다른 조문으로 센다. 저장 쪽 검사는 이미 저장된
  기록의 ``payload``와 견주므로, 원문이 아직 없는 첫 저장은 막지 않는다.
- **검증**: 전체 636개 테스트 통과(새 회귀 7개 포함), Ruff F·E9 통과.
  실제로 망가져 있던 사용자 저장 파일에 대해 원문 조문 182개 대비 저장
  화면 조문 1개로 검사가 걸리는 것을 확인했고, 즐겨찾기에서 여는 경로가
  47자·조문 1개에서 98,087자·조문 185개로 복구되는 것을 확인했다.
  이전 검사(``_snapshot_matches_row``)만으로는 이 화면이 통과했음을
  회귀 테스트에 함께 남겼다.


## 2026-08-30 · 질의회신ㆍ해석례ㆍ판례 본문을 표시줄에서 닫기

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 열린 본문 표시줄에서 중앙부처 질의회신ㆍ법령해석례ㆍ판례
  항목에도 닫기 ×를 그리고, 누르면 그 화면의 본문 칸을 비우도록
  ``AiLawSearchTab.close_open_document``를 추가했다. 닫을 수 있는
  항목은 ``_CLOSABLE_DOCUMENT_TABS``로 한곳에 모았다.
- **왜**: 법령 본문은 화면 안에 탭이 여러 개라 탭 하나를 지우면 되지만,
  이 세 화면은 화면 하나에 본문 칸이 붙어 있는 구조여서 "닫을 대상이
  없다"고 보고 ×를 아예 그리지 않았다. 그래서 한 번 연 본문이 새 검색을
  할 때까지 표시줄에 계속 남아 자리를 차지했다.
- **주의**: 연관검색ㆍ직접검색은 뺐다. 검색어를 바꾸면 결과가 통째로
  바뀌는 화면이라 "이 본문 하나를 닫는다"는 뜻이 서지 않는다. 필요하면
  ``_CLOSABLE_DOCUMENT_TABS``에 줄만 더하면 된다. 본문 상태를 비우는
  것으로 충분하다 — ``_collect_open_documents``가 본문 글자가 없는
  화면을 다음 갱신에서 알아서 뺀다. 다만 본문 탭 쪽과 달리 화면이 갱신을
  예약해 주지 않으므로 ``_schedule_open_documents_refresh``를 직접 부른다.
- **남은 일**: 즐겨찾기에서 연 국토계획법이 제1조만 보였다는 제보가
  있었다. 저장 파일(202개 조문단위)과 여는 경로는 정상이고 재현되지
  않는다. ``_restore_cached_law_render``가 저장 화면을 믿을지 판단할 때
  첫 줄이 법령 이름과 맞는지만 보고 **본문이 온전한지는 확인하지 않는
  것**이 유력한 원인이다. 잘린 화면이 한 번 저장되면 계속 그것을
  되살린다. 조문 수 대조 같은 완결성 검사를 넣을지는 재현 사례를 한 번
  더 받고 정한다.
- **검증**: 전체 629개 테스트 통과(새 회귀 5개 포함), Ruff F·E9 통과.


## 2026-08-30 · 저장 목록 색인과 CLI 상태 확인 NameError 수정

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 창을 켤 때 저장 폴더를 훑던 목록 화면을 본문 없는 가벼운
  항목(``list_entries``ㆍ``favorite_entries``)으로 바꾸고, 수정 시각과
  크기로 검증하는 색인 파일 ``_목록색인.json``을 저장 폴더에 둔다.
  ``load``가 파일마다 부르던 ``directory.resolve()``를 한 번만 부른다.
  ``ai_chat_panel.py``에서 가져오지 않은 ``cli_version``과 없는
  ``_NEWLINE``을 고쳤다. ``utils/parsing.py``에서 뒤 정의에 가려 죽어
  있던 ``extract_law_article``과 그것만 쓰던 ``_law_units_for_jo``를
  지웠다. 행정규칙 정규화가 매치마다 부르던 리터럴 정규식을 컴파일
  상수로 옮기고, 본문 꼬리를 잘라 넘기던 자리를 ``match(text, pos)``로
  바꿨다. 스타일시트는 창을 다 만든 뒤가 아니라 만들기 전에 건다.
- **왜**: 목록은 이름ㆍ구분ㆍ날짜ㆍ즐겨찾기 표시만 쓰는데 본문이 함께
  들어 있는 기록을 통째로 파싱했다. 저장 62건(10MB)에서 창을 켤 때마다
  같은 파일을 세 번씩, 모두 29.9MB를 읽었다. 저장 건수에 그대로 비례해
  느려지는 자리다. ``cli_version``은 정의가 없어 확인 스레드가 늘
  NameError로 끝났고, 그 예외를 ``except Exception``이 삼켜 멀쩡히 깔린
  CLI가 계속 "미연결"로 보였다. 정규식은 리터럴로 두면 호출마다 캐시를
  뒤지고, 꼬리 슬라이스는 본문 길이에 제곱으로 늘어난다.
- **주의**: 색인 파일도 저장 폴더의 ``*.json``이므로 목록에서 반드시
  뺀다(``LIST_INDEX_NAME``). ``list_entries``가 돌려주는 항목에는 본문이
  없다. 본문이 필요한 자리는 ``load``로 그 파일만 다시 읽는다 —
  ``_open_path``가 이미 그렇게 한다. ``list_records``ㆍ``list_favorites``
  는 본문까지 필요한 곳(3단비교 기록 등)이 있어 그대로 두었다. 색인
  형식을 바꾸면 ``LIST_INDEX_VERSION``을 올린다. 정규식은 패턴 문자열을
  그대로 옮겼을 뿐 적용 범위를 바꾸지 않았다. 다만
  ``_CIRCLED_REFERENCE_TAIL_PATTERN``의 앞 ``^``는 ``match(text, pos)``와
  함께 쓸 수 없어 뺐다 — 이 패턴은 ``match``로만 쓰므로 시작 위치는
  이미 고정된다.
- **검증**: 전체 624개 테스트 통과(기존 611 + 새 회귀 13), Ruff의
  F·E9 규칙 통과, ``git diff --check`` 통과. 표본 행정규칙 95,925자에
  대해 ``insert_admin_clause_breaks``ㆍ``normalize_admin_rule_text``와
  줄 단위 목록 분리 세 함수의 출력이 수정 전과 해시까지 같음을 확인했다.
  창 생성은 666.94ms에서 347.88ms로 약 48% 줄었고, 창 생성 RSS 증가는
  78.71MB에서 53.62MB로, 살아 있는 객체 수는 103,886개에서 81,746개로
  줄었다. 저장 목록 만들기는 본문까지 읽는 ``list_records`` 57.33ms에
  견줘 색인이 있는 ``list_entries``가 2.24ms다. 96KB 행정규칙 정규화는
  38.31ms에서 36.03ms, 3배 길이 본문에서는 129.2ms에서 115.1ms로 줄었다.
  스타일시트 적용 시간은 창 하나에 73.4ms에서 10.3ms로 줄었다.


## 2026-08-30 · 대량 결과 표 가속과 즐겨찾기 본문 복귀 보정

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 검색 결과 셀을 대량으로 넣는 동안 표 신호ㆍ중간 페인트와
  ``ResizeToContents`` 자동 폭 계산을 멈추고, 삽입이 끝난 뒤 열 크기
  모드를 한 번만 복원한다. 행 높이도 실제 값이 달라질 때만 바꾼다.
  즐겨찾기에서 연 법령의 크게 보기 중 상단 열린 본문 탭을 이동해도
  최초 복귀 목적지인 즐겨찾기를 유지한다. 크게 보기의 창 바깥 여백은
  일반 화면의 기본 여백과 같게 맞췄다. 별표ㆍ서식 전용 세 카테고리의
  ``본문 조회`` 버튼은 숨기고 통합검색의 공용 버튼만 유지했다.
- **왜**: 자동 폭 열은 셀을 하나 넣을 때마다 기존 행을 다시 훑어
  대량 결과에서 사실상 제곱 비례로 느려졌다. 또한 상단 본문 탭 이동이
  즐겨찾기 복귀 콜백을 현재 메뉴인 법령검색으로 덮어썼고, 크게 보기만
  창 여백을 6px로 줄여 일반 화면의 외곽선과 맞지 않았다.
- **주의**: 일괄 갱신은 기존 열별 ``Fixed``ㆍ``Stretch``ㆍ
  ``ResizeToContents`` 설정과 진입 전 신호ㆍ업데이트 상태를 그대로
  복원해야 한다. 크게 보기 중 열린 본문을 바꿀 때 이미 저장된 복귀
  콜백이 있으면 새 콜백으로 덮어쓰지 않는다. 별표ㆍ서식 원문은 결과
  더블클릭과 링크 경로로 계속 열 수 있다.
- **검증**: 관련 UI 테스트 58개와 전체 611개 테스트 통과, Ruff 치명
  오류 검사 및 ``git diff --check`` 통과. 1,000행 결과 표 렌더링은
  최적화 전 150초 안에 측정이 끝나지 않아 중단됐고, 최적화 후 중앙값
  123.69ms(500행 62.38ms, 100행 24.56ms)로 측정됐다.

## 2026-08-30 · 탭 전환 시 열린 본문 표시줄 재구축 제거

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 전역 열린 본문 표시줄의 토큰ㆍ제목ㆍ전체 이름으로
  표시 시그니처를 만들고, 목록이 이전과 같으면 모든 탭을 삭제하고
  다시 만드는 대신 활성 탭 표시만 갱신한다. 30개 열린 본문을 이용한
  동일 갱신과 강제 재구축 측정을 ``perf_baseline.py``에 추가하고,
  동일 목록에서 ``removeTab``이 호출되지 않는 회귀 테스트를 넣었다.
- **왜**: 메인 메뉴와 키워드 탭의 ``currentChanged``는 열린 본문 목록이
  바뀌지 않아도 갱신을 예약했다. 기존 구현은 그때마다 탭 위젯을 전부
  제거ㆍ생성하고 제목ㆍ도움말ㆍ스크롤 배치를 다시 계산해 화면 전환
  지연과 불필요한 페인트를 만들었다.
- **주의**: 본문 내용만 달라지는 것은 표시줄 재구축 사유가 아니다.
  반면 토큰, 짧은 제목, 전체 이름 또는 사용자가 끌어 바꾼 순서가
  달라지면 시그니처가 바뀌어 기존 전체 갱신 경로를 실행해야 한다.
- **검증**: 열린 본문ㆍ키워드 화면 관련 23개와 전체 610개 테스트 통과,
  Ruff 치명 오류 검사 및 ``git diff --check`` 통과. 열린 본문 30개에서
  동일 목록 갱신 중앙값 0.14ms, 강제 전체 재구축 중앙값 24.82ms로 측정.

## 2026-08-30 · 3단 비교 정렬 회귀 복구와 계산 모듈 분리

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 3단 비교 표 테스트가 외곽선용 빈 셀까지 데이터 열로
  세던 판독 오류를 고쳐 실제 ``comparison-cell``만 검사하게 했다.
  ``ResourceSearchTab`` 안의 항ㆍ호ㆍ목 HTML 분해, 항 묶음 구성, 인용
  단위의 행 선택 계산을 Qt와 무관한 ``utils/three_stage_alignment.py``로
  옮기고 중복된 클래스 내부 구현을 제거했다.
- **왜**: 앞선 표 외곽선 변경으로 행마다 ``edge``ㆍ``divider`` 셀이
  추가됐지만 테스트는 여전히 전체 ``td``가 3개라고 가정해 실제 정렬
  결과를 읽기 전에 실패했다. 정렬 계산이 8천 줄짜리 화면 클래스에
  묻혀 있어 오류와 렌더링 문제를 구분하기도 어려웠다.
- **주의**: 이번 세 실패는 실제 사용자 화면의 오정렬이 아니라 낡은
  테스트 판독기의 오류였다. 항ㆍ호ㆍ목 표지 정규식과 매칭 순서는
  적용 범위를 바꾸지 않고 그대로 이동했다. 표에 장식 셀을 추가해도
  테스트는 클래스가 ``comparison-cell``인 데이터 셀만 읽어야 한다.
- **검증**: 3단 비교 관련 테스트 48개 통과, 전체 609개 테스트 통과,
  ``git diff --check`` 통과.

## 2026-08-30 · 행정규칙 렌더링·열린 본문·PDF 초기화 최적화

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 이미 정규화한 행정규칙 섹션은 ``body_to_html``에서
  같은 정규식을 다시 실행하지 않게 했다. 본문 글자 선택만 바뀔 때는
  전역 열린 본문 탭을 다시 만들지 않는다. PDF 미리보기를 팝업으로
  옮긴 뒤에도 시작 시 생성되던 옛 인라인 ``QPdfView``ㆍ
  ``QPdfDocument``와 도달 불가능한 다운로드 코드를 제거했다. 제품
  코드의 미사용 Qt import 5개도 정리하고 성능 측정 스크립트가 실제
  패키지 전체 줄 수와 현재 행정규칙 경로를 재도록 고쳤다.
- **왜**: 9.6만 자 행정규칙에서 정규화가 두 번 실행되어 변환 시간의
  큰 부분을 차지했다. ``selectionChanged``는 열린 문서 목록이나 활성
  문서를 바꾸지 않는데도 탭 전체를 재구축했다. 옛 인라인 PDF 구현은
  여는 경로가 없지만 무거운 Qt PDF 객체를 매번 만들었다.
- **주의**: ``administrative_rule_normalized=True``는 바로 앞에서
  ``normalize_admin_rule_text``를 적용한 값에만 준다. 저장된 구버전
  원문은 계속 기본 경로로 재정규화해야 한다. PDF는
  ``PdfPreviewPopup`` 경로를 유지하며 인라인 PDF 위젯을 되살리지 않는다.
- **남은 일**: 전체 609개 중 606개 통과. 기존
  ``tests/test_three_stage_article_merge.py`` 정렬 테스트 3개는 이번
  변경 전과 동일하게 단독 실행에서도 실패하므로 별도 파싱 수정이
  필요하다. ``resource_search.py``의 추가 분리는 기능별 테스트 seam을
  먼저 만든 뒤 진행한다.
- **검증**: 행정규칙ㆍPDFㆍ별표ㆍ열린 본문 관련 테스트 86개 통과,
  Ruff 미사용 심볼 검사 통과, Vulture의 도달 불가능 코드 지적 제거.
  95,925자 행정규칙 정규화+HTML 변환 중앙값은 약 122.6ms에서
  56.6ms로 약 54% 감소했고 창 생성 RSS 증가는 약 72.4MB에서
  68.7MB로 약 3.7MB 감소했다.

## 2026-08-30 · 키워드 탭 크게 보기 상태와 팝업 신호 누적 정리

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.1
- **무엇을**: 법령검색 안에서 직접검색과 연관검색을 바꿀 때 이전
  화면의 크게 보기 상태를 먼저 해제해 공용 메뉴ㆍ카테고리 바가 숨은
  채 남지 않게 했다. 법령 인용 팝업의 ``refreshRequested`` 연결은
  크게 보기 전환 때마다 추가하지 않고 탭 생성 시 한 번만 연결한다.
  두 경로의 회귀 테스트를 추가했다.
- **왜**: 같은 메인 메뉴 줄 안의 카테고리 전환은
  ``navigation.currentRowChanged``가 다시 발생하지 않아 기존 정리
  함수가 실행되지 않았다. 또한 크게 보기를 반복하면 팝업 갱신 슬롯이
  누적되어 한 번의 갱신 클릭으로 같은 작업을 여러 번 수행했다.
- **주의**: 키워드 카테고리 전환은 메인 메뉴 이동과 달리 같은
  ``tabs`` 페이지 안에서 일어난다. 다른 키워드 화면으로 바꿀 때는
  ``_reset_reading_modes_for_page_change``를 건너뛰지 않는다. Qt 신호
  연결을 반복 호출되는 상태 전환 함수 안에 두지 않는다.
- **남은 일**: 전체 테스트 607개 중 이번 변경 관련 항목을 포함한
  604개는 통과했다. ``tests/test_three_stage_article_merge.py``의 기존
  3단 비교 정렬 테스트 3개는 단독 실행에서도 실패하므로 별도 파싱
  작업에서 원인을 확인한다.
- **검증**: 관련 테스트 19개 통과. 키워드 카테고리 60회 반복 전환은
  총 262.86ms(평균 4.38ms)였고, 종료 뒤 모든 크게 보기 플래그와 공용
  UI 가시성이 정상으로 복원됐다.

## 2026-08-28 · 본문 조회 숨김·최근검색어 한 줄·즐겨찾기 본문

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 법령ㆍ행정규칙ㆍ자치법규 목록의 「본문 조회」 단추를
  숨겼다. 최근 검색어는 제목·항목·초기화를 한 줄에 두고 10개가
  들어가게 했다. 즐겨찾기에서 본문을 열 때 직전이 연관ㆍ직접검색이면
  키워드 화면이 본문을 가리지 않게 법령 목록 페이지로 돌린다.
- **왜**: 더블클릭이 바로 크게 보기라 조회 단추가 중복이다. 최근
  검색어는 5개씩 두 줄이라 한 줄에 10개가 안 들어갔다. 직접검색을
  보다가 즐겨찾기로 법령을 열면 헤더에만 열린 본문이 있고 가운데는
  키워드 검색 화면이 남았다.
- **주의**: 카테고리를 바꿀 때 ``currentChanged``를 그대로 타면 열어
  둔 본문을 지운다. ``ensure_body_page_for_target``은 신호를 막고
  스택만 바꾼다. 별표·서식·통합검색의 본문 조회 단추는 남긴다.
- **검증**: 법령·행정규칙·자치법규에서 단추가 숨고 별표는 보이는지,
  최근검색어 10개가 한 줄인지, 직접검색 뒤에 즐겨찾기 본문이
  ``resource_body``로 열리는지 확인.

## 2026-08-28 · API 설정 창·원래화면 제거·검색머리글 공유

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 헤더의 법제처 인증키 칸을 「API 설정」 단추로 바꾸고
  입력은 대화상자에서 한다. 크게 보기의 「원래화면」 단추는 없애
  돌아가기는 ◀만 쓴다. 검색 결과 「0건」 배지와 머리글은
  ``build_count_badge`` / ``build_search_result_head``로 한곳에서
  만든다. 크게 보기에서도 헤더의 열린 본문 띠는 남기고 왼쪽 메뉴만
  접는다. 인증키가 없으면 경고창 없이 설정 창을 연다.
- **왜**: 제목·인증키 한 줄이 헤더를 키웠고, 물음표를 여백에 겹치는
  방식은 거절됐다. 같은 UI를 화면마다 따로 만들면 0건 배지 크기가
  어긋난다. 연관·직접검색은 크게 보기에서 열린 본문이 사라져
  법령검색과 달랐다. 「원래화면」과 ◀가 같은 일을 두 곳에서 했다.
- **주의**: ``open_oc_api_settings``는 ``exec()``라 테스트에서 단추
  클릭으로 열면 멈춘다. 대화상자는 ``show()``로만 검사한다.
  크게 보기에서 ``header_card.setVisible(False)``를 다시 넣지 않는다.
  법령검색 내부 본문 탭 띠는 크게 보기에서 숨긴다. 헤더와 겹친다.
  검색머리글은 각 탭에서 다시 조립하지 말고 ``ui/widgets.py``를 쓴다.
- **검증**: 헤더에 인증키 칸이 없고 「API 설정」이 있는지, 크게 보기에서
  「원래화면」이 숨기고 ◀로 목록이 돌아오는지, 0건 배지 sizeHint가
  법령·별표·연관·직접에서 같은지, 키워드 크게 보기에서 헤더와 열린
  본문이 보이는지 확인.

## 2026-08-28 · 상단바 제목 제거·물음표는 여백에 겹침

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 헤더에서 「국가법령정보 통합검색」 글자를 빼고 로고를
  36px로 줄여 높이를 법제처 API 인증키 한 줄에 맞췄다. 물음표
  안내 단추는 인증키 박스 레이아웃에서 빼 헤더 오른쪽 여백에만
  겹친다. 박스 안 40px 자리 비우기는 없앴다.
- **왜**: 제목 한 줄 때문에 헤더가 인증키 칸보다 높았고, 물음표
  자리 40px가 열린 본문 줄을 왼쪽으로 밀었다.
- **주의**: 물음표를 레이아웃에 다시 넣거나 인증키 박스
  ``contentsMargins`` 오른쪽만 키우지 않는다. 창 제목줄 글자는
  그대로 둔다.
- **검증**: 헤더에 제목 라벨이 없는지, 헤더 높이가 인증키 칸+여백
  안인지, 물음표가 레이아웃에 없고 저장 체크 오른쪽인지 확인.

## 2026-08-28 · 연관·직접검색 안쪽 여백을 법령과 같게

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 법령검색 탭에 끼운 연관검색ㆍ직접검색의 바깥 12px
  여백을 없앴다. 법령ㆍ별표서식은 탭의 12px만 쓰므로, 키워드
  화면이 한 겹 더 들어가면 검색칸이 작아 보였다.
- **왜**: 같은 카테고리 바 아래인데 안쪽 UI만 좁았다.
- **주의**: 크게 보기에서 나올 때 키워드 화면 여백을 12로 되돌리면
  다시 어긋난다. 바깥 탭 ``root_layout`` 12px는 유지한다.
- **검증**: 법령ㆍ별표서식ㆍ연관검색ㆍ직접검색의 검색칸 왼쪽과 폭이
  같은지, 검색칸 높이도 같은지 확인.

## 2026-08-28 · 크게 보기 × 제거, 원래화면으로 닫기

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 직접검색ㆍ연관검색ㆍ중앙부처ㆍ해석례ㆍ판례 본문 우상단의
  × 닫기 단추를 없앴다. 크게 보기에서 목록으로 돌아가는 길은
  「원래화면」과 ◀이다. × 자리를 비우던 22px 여백과 전용 스타일도
  지웠다.
- **왜**: 더블클릭이 바로 크게 보기라 ×와 「원래화면」이 같은 일을
  두 곳에서 했다. 닫기는 머리글 단추 하나로 충분하다.
- **주의**: 새 검색이 본문 칸을 닫을 때는 여전히
  `_hide_detail_split`을 쓴다. 스타일 이름 ``caseDetailCloseButton``을
  다시 만들지 않는다.
- **검증**: 크게 보기에서 「원래화면」을 누르면 본문 칸이 닫히고
  목록이 돌아오는지, × 속성이 없는지를 확인.

## 2026-08-28 · 검색결과 표 명칭열이 남는 폭을 채움

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 검색결과 표의 마지막 열 Stretch를 쓰지 않고, 명칭(법령검색)ㆍ
  조문+법령명(연관ㆍ직접검색)ㆍ안건명/사건명(중앙부처ㆍ해석례ㆍ판례)
  열만 Stretch로 두어 표 오른쪽 빈칸을 채우게 했다.
- **왜**: 헤더는 컨테이너 폭을 쓰는데 본문 열은 고정 폭이라 오른쪽에
  빈칸이 남았다. 관련법령·날짜 같은 짧은 열까지 늘리면 읽기 어렵다.
- **주의**: ``setStretchLastSection(True)``로 되돌리지 않는다. 숨긴
  열은 폭 합산에서 빼야 한다.
- **검증**: 명칭/조문/안건명 열이 Stretch인지, 보이는 열 폭 합이
  뷰포트 폭 이상인지 확인.

## 2026-08-28 · 검색 더블클릭은 바로 크게 보기

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 직접검색ㆍ연관검색ㆍ중앙부처ㆍ해석례ㆍ판례에서 검색결과
  더블클릭이 목록+본문 반반 분할을 거치지 않고 바로 크게 보기로
  들어가게 했다. 저장 본문도 같다. 크게 보기에서 ◀로 나오면 목록
  화면으로 돌아간다.
- **왜**: 반반 분할은 목록이 좁아 본문을 읽기 불편했고, 법령검색
  전문은 이미 더블클릭이 크게 보기라 화면마다 동작이 달랐다.
- **주의**: `_show_detail_split`은 시험용으로 남아 있다.
  사용자 열기 경로에서는 쓰지 않는다. 크게 보기에서 나오면 분할
  비율을 복원하지 않고 본문 칸을 닫는다. 닫기는 ◀ 또는
  「원래화면」이다.
- **검증**: 더블클릭과 저장본문이 `_reading_mode`이고 왼쪽 목록 폭이
  0인지, ◀ 뒤에는 본문 칸이 닫히는지 확인.

## 2026-08-28 · Windows 제목줄을 어두운 테마로

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 메인 창의 Windows 네이티브 제목줄에 DWM immersive
  dark mode를 켠다. 앱 본문 색은 바꾸지 않는다.
- **왜**: 창 제목줄만 흰색이라 왼쪽 남색 메뉴와 어색했다. Qt
  ``ColorScheme.Dark``는 본문까지 어두워져 쓰지 않았다.
- **주의**: Windows가 아니면 아무 것도 하지 않는다. 창이 실제로
  보인 뒤(``showEvent``)에 적용해야 ``winId``가 생긴다.
- **검증**: 검색 범위 칸과 검색칸 한 줄 테스트와 함께 창 생성 회귀.

## 2026-08-28 · 검색 범위 칸과 검색어 칸을 한 줄로

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 직접검색ㆍ연관검색의 조문 범위 목록과 검색칸, 중앙부처ㆍ
  해석례ㆍ판례의 범위(및 중앙부처 기관) 목록과 검색칸을 한 줄에 두었다.
  법령검색 통합검색은 원래 한 줄이라 그대로 둔다.
- **왜**: 범위 목록이 검색칸 위에 따로 있어 같은 검색 줄이 두 단이 되었다.
- **검증**: 연관ㆍ직접검색과 중앙부처ㆍ해석례ㆍ판례에서 범위 칸과 검색칸의
  세로 중심이 같고, 왼쪽에서 오른쪽으로 이어지는지 확인.

## 2026-08-28 · 본문 AI 선택칸·키 단추 폭 축소

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 본문 옆 AI 패널의 제공자 목록을 178px 고정에서
  가장 긴 이름(Gemini/Claude/Codex) 폭 + 화살표 여백으로 줄였다.
  API 키 단추는 좌우 패딩을 10px에서 6px로, 글자 옆 여백을 12에서
  8로 줄였다.
- **왜**: 선택칸은 "Gemini"보다 빈칸이 훨씬 넓었고, 키 단추도
  문구보다 좌우가 남아 좁은 패널에서 자리를 낭비했다.
- **주의**: 제공자 이름은 세 개 중 가장 긴 쪽에 고정해야 탭을
  바꿔도 헤더·대화 영역이 흔들리지 않는다. 키 단추 패딩을 더
  줄이면 "확인 필요"가 잘릴 수 있다.
- **검증**: 콤보 폭이 글자+32이고 178보다 작은지, 키 단추 폭이
  가장 긴 문구+8인지, 제공자를 바꿔도 헤더 위치가 같은지 확인.

## 2026-08-28 · 항호목 여백 2px 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 일반 법령 항ㆍ호ㆍ목ㆍ세목 왼쪽 여백을 각각 2px 늘렸다
  (12→14, 24→26, 40→42, 64→66). 조 제목 여백(14px)과 수립지침 여백은
  그대로 둔다. 렌더 스냅샷 버전을 11로 올렸다.
- **왜**: 직전 12px 증가 뒤에도 단계가 조금 더 필요했다.
- **주의**: 저장된 본문은 스냅샷 버전이 바뀌어야 여백이 다시 그려진다.
  항을 14px로 두면 조 제목 왼쪽 여백과 같아진다.
- **검증**: 항14ㆍ호26ㆍ목42ㆍ세목66, 법령형 행정규칙 호 26px, 수립지침
  40px 회귀.

## 2026-08-28 · 통합검색 키워드 결과를 조문 단위로 유지

- **커밋**: 89d69bd
- **버전**: 1.3.0
- **무엇을**: 통합검색에 합치는 연관검색ㆍ직접검색 행을 법령 단위로
  접지 않고, 단독 검색과 같이 조문마다 남긴다. 명칭 열은 제N조 제목을
  쓰고 관련법령 열은 법령명으로 둔다. 저장된 본문을 열면 해당 조로
  스크롤한다. `AGENTS.md`의 접기 안내도 맞췄다.
- **왜**: 같은 법령의 다른 조문은 각각 검색결과인데, 법령 ID로 처음
  것만 남기면 통합검색에서 조문이 사라졌다. 목록검색과 겹친다고 빼면
  어느 조를 찾았는지도 보이지 않았다.
- **주의**: 별표ㆍ서식 키워드 건은 본문 일련번호가 없어 계속 제외한다.
  내부 저장ㆍ본문 조회는 `name`에 법령명을 두고, 목록만 `display_name`을
  쓴다.
- **검증**: 같은 법령 두 조문이 두 행으로 남고, 목록검색에 이미 있는
  법령의 키워드 조문도 버리지 않는지 확인.

## 2026-08-28 · 항호목 여백 12px 추가와 Gemini 키 단추 폭

- **커밋**: 89d69bd
- **버전**: 1.3.0
- **무엇을**: 일반 법령 본문의 항ㆍ호ㆍ목ㆍ세목 왼쪽 여백을 각각 12px
  늘렸다(0→12, 12→24, 28→40, 52→64). 수립지침 여백은 그대로 둔다.
  저장 HTML이 옛 여백을 쓰지 않게 렌더 스냅샷 버전을 10으로 올렸다.
  본문 안 AI 패널의 `Gemini API 키 확인됨` 단추는 CLI 연결 줄 폭을
  따라가지 않고, 가장 긴 키 상태 문구 폭에 맞춘다.
- **왜**: 조문 별표를 왼쪽으로 당긴 뒤 항호가 조보다 앞에 있어 단계가
  12px씩 더 필요했다. 키 단추는 CLI 배지+확인 단추 합 너비에 맞춰 두어
  글자보다 빈칸이 넓었다.
- **주의**: 저장된 본문은 스냅샷 버전이 바뀌어야 여백이 다시 그려진다.
  키 단추와 CLI 줄 폭을 다시 같게 맞추면 본문 패널에서 빈칸이 되살아난다.
- **검증**: 항12ㆍ호24ㆍ목40ㆍ세목64 여백 테스트, 수립지침 40px 회귀,
  본문 AI 헤더에서 키 단추가 문구 폭을 넘지 않고 탭을 옮겨도 헤더ㆍ대화
  영역 위치가 같은지 확인.

## 2026-08-28 · 본문 조문 별표 여백과 저장본문 재열기 지연

- **커밋**: 89d69bd
- **버전**: 1.3.0
- **무엇을**: 법령 전문 `제N조` 왼쪽 즐겨찾기 별의 자리(왼쪽 여백 24px,
  별 18px, 글자와의 틈 4px)를 14px·16px·2px로 줄였다. 저장 본문을 다시
  열 때 조문 수만큼 저장 JSON을 다시 파싱하던 별표 상태 조회는 법령당
  한 번만 읽고, 같은 파일은 mtime 기준으로 기억한다. 조문 제목 여백을
  넣을 때는 `contentsChanged`를 잠시 끊는다.
- **왜**: 별 자리만 `제N조`에 들여쓰기를 줘서 ①·1. 항호가 조보다 앞에
  있는 것처럼 보였다. 저장된 국토계획법 같은 큰 JSON을 조문마다 다시
  읽으면, API를 안 타도 더블클릭이 늦게 열린 것처럼 느껴졌다.
- **주의**: 조문 제목 왼쪽 여백을 문서 전체 `leftMargin`으로 바꾸면
  법령명·기본정보까지 함께 밀린다. 별표는 조문 첫 블록에만 자리를 낸다.
- **검증**: 별이 제목 왼쪽에 붙고 제목 여백이 14px인지, 다섯 조 본문의
  별표 새로고침이 `article_favorites`를 한 번만 부르는지, 같은 저장
  파일을 연속 조회해도 디스크 읽기가 늘지 않는지 확인.

## 2026-08-28 · 본문 AI 패널 최소 폭ㆍ자동 접힘 제거

- **커밋**: 962ed88
- **버전**: 1.3.0
- **무엇을**: 법령ㆍ직접검색ㆍ연관검색ㆍ중앙부처 질의회신ㆍ법령해석례ㆍ
  판례 본문 옆 AI 패널의 내부 최소 폭을 0으로 낮췄다. 분할 손잡이로 폭을
  줄일 때는 패널을 접을 수 없게 하고, 완전히 닫는 동작은 기존 `×`로만
  수행하게 했다.
- **왜**: 내부 단추와 콤보박스의 최소 크기에 도달하면 QSplitter가 다음
  드래그 구간을 곧바로 0폭 접힘으로 처리해, 사용자가 원하는 좁은 폭을
  선택할 수 없었기 때문이다.
- **주의**: `setCollapsible(2, False)`는 손잡이 드래그의 자동 접힘만 막는다.
  패널 `×`는 위젯을 `hide()`하므로 기존처럼 정상적으로 0폭까지 닫힌다.
- **검증**: 세 본문 계열에서 AI 패널 칸이 접기 불가인지 확인하고 40px까지
  줄여도 0폭이 되지 않는 테스트, AI 열기ㆍ닫기ㆍ크게 보기 전환 회귀와 문법
  검사 포함 20개 통과.

## 2026-08-28 · 즐겨찾기 조항호목 크게 보기 실행본 갱신

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 즐겨찾기의 조항호목을 더블클릭하면 법령 전문과 같은 전체
  크게 보기로 열리고 왼쪽 위 `◀`로 즐겨찾기 화면에 돌아오는 실제 창 경로를
  통합 테스트로 고정했다. 8월 27일자였던 `dist/국가법령정보 통합검색.exe`를
  8월 28일 현재 소스로 다시 빌드했다.
- **왜**: 현재 소스에서는 조항호목의 크게 보기ㆍ복귀가 정상 동작했지만,
  사용 중인 배포 실행 파일은 관련 UI 수정 전 빌드라 공용 머리말과 메뉴만
  사라지고 법령검색 목록이 남으며 복귀 단추도 없는 이전 동작을 보였다.
- **주의**: `dist/`와 `build/`는 빌드 산출물이므로 커밋하지 않는다. UI 변경을
  EXE로 확인할 때는 소스 테스트 통과와 별개로 spec 재빌드 시각도 확인한다.
- **검증**: 즐겨찾기 화면에서 제77조 조항호목을 열어 대상 화면 전환, 전체
  크게 보기, 검색 결과ㆍ상태줄 숨김, 본문ㆍ복귀 단추 표시, `AI 에이전트`
  단추 전환, `◀` 클릭 후 즐겨찾기 복귀를 확인했다. 같은 조항호목을 다시
  열어 본문 탭 `×`를 누를 때도 빈 안내 화면 없이 즐겨찾기로 복귀하고 해당
  본문 탭이 닫히는 경로까지 테스트했다. PyInstaller onefile 빌드 완료.

## 2026-08-28 · 크게 보기 본문 닫기 시 이전 화면 복귀

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 법령 본문을 크게 보는 중 활성 본문 탭의 `×`를 누르면 크게
  보기를 먼저 종료한 뒤 법령검색 결과 목록 또는 본문을 열기 전에 보던
  저장내역ㆍ즐겨찾기ㆍAI 화면으로 돌아가게 했다.
- **왜**: 마지막 활성 본문을 닫은 뒤에도 크게 보기가 유지되어 내부의 빈
  미리보기 `안내`가 전체 화면에 나타나고, 사용자가 원래 작업하던 화면으로
  직접 돌아가야 했기 때문이다.
- **주의**: 활성 본문을 크게 보기에서 닫을 때만 복귀한다. 여러 본문 가운데
  현재 읽지 않는 비활성 탭을 닫을 때는 읽던 본문과 크게 보기를 유지한다.
- **검증**: 크게 보기에서 활성 본문 닫기, 복귀 콜백 실행 시점, 검색 결과
  화면 복원, 마지막 본문 탭 제거를 확인하는 회귀 테스트와 기존 법령검색
  크게 보기 전환 테스트 통과.

## 2026-08-28 · 직접검색ㆍ연관검색도 목록부터 분할 본문

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 법령검색 안의 직접검색ㆍ연관검색을 처음 열거나 새 검색할 때
  검색 조건과 결과 목록만 전체 폭으로 표시한다. 한 번 클릭은 선택만 하고,
  결과를 더블클릭하면 오른쪽 분할 본문을 연다. 기존 `본문 조회`를 없애고
  카드 우상단 `×`, `크게 보기` 오른쪽의 `AI 에이전트`를 추가했다.
- **왜**: 질의회신ㆍ해석례ㆍ판례와 달리 키워드 검색만 검색 전부터 빈 본문
  도구와 영역을 차지하고, 결과 더블클릭 시 곧바로 크게 보기로 들어가 검색
  결과와 본문을 함께 비교하기 어려웠기 때문이다.
- **주의**: 연관검색은 첫 행 자동 선택만으로 조문 API를 호출하지 않는다.
  더블클릭ㆍ저장 체크ㆍ즐겨찾기처럼 본문이 실제로 필요한 동작에서만 조회한다.
  키워드 AI 패널도 시작 시 만들지 않고 처음 열 때 지연 생성하며 앱 종료 시
  생성된 패널만 정리한다. 크게 보기에서는 바깥 법령검색 카테고리 바와
  안내문까지 함께 접는다.
- **검증**: 직접검색ㆍ연관검색의 목록 전용 시작, 한 번 클릭, 더블클릭 분할
  열기, 우상단 닫기, 새 검색 닫기, 저장 본문 열기, AI 컨텍스트ㆍ종료,
  바깥 카테고리 크게 보기 전환 및 기존 사례ㆍ조문 링크ㆍ3단비교ㆍ정렬 회귀
  테스트 37개 통과.

## 2026-08-28 · 메뉴 전환 뒤 법령검색 화면 겹침 수정

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 메인 메뉴의 법령검색ㆍ질의회신ㆍ해석례ㆍ판례 또는 별도
  즐겨찾기ㆍAI 에이전트ㆍ저장내역 화면으로 이동할 때, 각 검색 탭에 남은
  크게 보기 상태와 되돌아가기 콜백을 먼저 정리한다.
- **왜**: 한 본문 화면의 크게 보기가 남은 채 다른 화면이 공용 머리말과
  메뉴를 다시 표시하면, 법령검색으로 돌아왔을 때 바깥 `ResourceSearchTab`은
  크게 보기이고 안쪽 직접검색은 일반 화면인 상태가 겹쳤다. 그 결과 상단
  카테고리 바는 사라지고 빈 본문 카드가 오른쪽에 펼쳐졌다.
- **주의**: 저장내역이나 열린 본문에서 크게 보기로 들어가는 경로는 먼저
  메뉴를 이동한 뒤 새 크게 보기를 설정하므로 영향을 받지 않는다. 명시적인
  메인 화면 이동에서만 이전 크게 보기 상태를 버린다.
- **검증**: 직접검색을 고른 법령검색에 남은 크게 보기 상태를 만든 뒤
  질의회신을 거쳐 다시 법령검색으로 돌아오는 재현 테스트 추가. 카테고리 바
  복원ㆍ빈 본문 카드 닫힘을 포함한 법령검색ㆍ사례검색 테스트 19개 통과.

## 2026-08-28 · 사례 본문 우상단 닫기와 AI 에이전트

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 사례 검색 분할 본문의 `×`를 머리말 도구줄에서 빼 본문 카드
  오른쪽ㆍ위쪽 테두리에서 각각 5px 떨어진 모서리에 고정했다. `크게 보기`
  바로 오른쪽에는 `AI 에이전트` 단추를 추가해 현재 본문 또는 드래그한
  부분을 근거로 대화하는 오른쪽 패널을 열고 닫게 했다.
- **왜**: 닫기 단추는 일반 기능 단추와 나란히 있기보다 창 모서리에 있어야
  찾기 쉽고, 사례를 읽는 중에도 화면을 떠나지 않고 AI에게 물을 수 있어야
  하기 때문이다.
- **주의**: 사례용 AI 패널은 세 검색 탭마다 시작 시 만들지 않고 처음 열 때
  지연 생성한다. 앱 종료 때 생성된 패널만 `shutdown()`하고, 분할 본문을
  닫으면 AI 패널도 함께 숨긴다. 법령 본문ㆍ독립 AI 화면과 저장 채팅 비우기
  신호를 서로 연결해 목록 상태가 어긋나지 않게 한다.
- **검증**: 사례 검색 3종의 기존 분할 전환과 우상단 `×` 좌표, 도구 단추
  비겹침, AI 단추 위치ㆍ열기ㆍ닫기ㆍ본문 컨텍스트ㆍ종료 정리 테스트 11개
  통과. 전체 창 생성ㆍ종료 회귀 테스트 본문은 1개 통과했으며, 프로세스
  종료코드 1은 기존 기록에 남아 있는 Qt 네이티브 종료 충돌과 같은 현상이다.

## 2026-08-28 · 사례 검색 분할 본문 닫기 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 중앙부처 질의회신ㆍ법령해석례ㆍ판례 검색 결과를 더블클릭해
  여는 오른쪽 분할 본문에서 `본문 조회` 단추를 없애고 22px 크기의 작은
  `×` 단추로 바꿨다. `×`를 누르면 본문 칸을 닫고 검색 결과 목록을 다시
  전체 폭으로 표시한다. 본문 조회 안내도 결과 더블클릭 기준으로 고쳤다.
- **왜**: 본문을 이미 더블클릭으로 여는 흐름에서는 조회 단추가 중복되고,
  열린 분할창을 사용자가 직접 닫을 방법이 없었기 때문이다.
- **주의**: 이 변경은 사례 검색 세 화면이 공유하는 `LawSearchTab`에만
  적용한다. 법령검색ㆍ키워드검색 등 조회 단추가 필요한 다른 화면의
  `본문 조회`는 유지한다.
- **검증**: 사례 검색 3종의 목록 전용 시작, 더블클릭 열기, 작은 닫기 단추,
  새 검색 닫기, 저장 본문 열기 회귀 테스트 10개 통과.

## 2026-08-27 · 제미나이 안내 재조정ㆍ물음표 단추ㆍ그림 WebP 전환

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: 제미나이 안내를 새 그림에 맞춰 5단계로 다시 짜고, Gemini API
  설정 대화상자의 `발급` 옆에 물음표 단추를 달아 그 안내를 연다. 안내
  그림 9장을 JPG에서 WebP로 바꿔 426KB → 223KB로 줄였다.
- **왜**: 그림 이름이 바뀌었는데 HTML은 옛 이름을 가리켜 4단계 그림이
  깨져 있었고, 제미나이 안내는 만들어만 두고 어디서도 열 수 없었다.
- **주의**: **안내 그림은 HTML과 같은 폴더에 파일 이름 그대로 있어야 하고,
  spec의 `datas`에도 있어야 한다.** 제미나이 안내 6개가 spec에서 통째로
  빠져 있었다. 원인은 번들 검사 테스트가 "인증키 안내가 참조하는 파일"만
  보도록 좁혀져 있었던 것이라, `메뉴얼/*.html` 전부를 보도록 되돌렸다.
  WebP는 브라우저로 여는 안내에만 쓴다. Qt 위젯에 직접 그리는 자산은
  아니라서 문제가 없다.

## 2026-08-27 · Codex 연결이 끊기면 스스로 다시 연결

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.3.0
- **무엇을**: `CodexConnectionLost` 예외를 따로 두고, 턴이 끊기면 세션을
  닫고 같은 질문을 **한 번만** 다시 보낸다.
- **왜**: 질문하고 다른 AI 탭에 갔다 오면 app-server 프로세스가 사라져
  있어서, 다음 질문마다 "Codex 연결이 예기치 않게 끝났습니다"만 나왔다.
  사용자는 같은 질문을 손으로 다시 넣어야 했다.
- **주의**: 탭 전환 자체는 세션을 닫지 않는다(가짜 세션을 붙여 재현해 보니
  `close`ㆍ`cancel` 호출이 0회였다). 프로세스가 쉬는 동안 사라지는 것이라
  UI 쪽을 고쳐도 낫지 않는다. **답 글자가 한 자라도 나온 뒤 끊긴 것은 다시
  묻지 않는다** — 같은 답이 화면에 두 번 쌓인다. 인증ㆍ한도 실패는 다시
  물어도 똑같이 실패하므로, 연결 끊김만 골라내려고 예외 종류를 나눴다.
- **남은 일**: 프로세스가 왜 쉬는 동안 죽는지는 아직 모른다. 턴 사이에
  stdout을 아무도 읽지 않아 파이프가 차는 것이 의심되지만 확인하지 못했다.

## 2026-08-27 · 법령 전문 조문 왼쪽에 즐겨찾기 별표 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 법령 전문의 `제1조`, `제2조` 같은 각 조문 제목 왼쪽에 작은
  즐겨찾기 별표를 표시했다. 빈 별을 누르면 해당 조문만 조항호목
  즐겨찾기에 추가되고, 채워진 별을 다시 누르면 해제된다. 즐겨찾기 화면이나
  인용 조문 팝업에서 상태를 바꿔도 열려 있는 본문의 별이 즉시 따라간다.
- **왜**: 기존에는 본문 속 링크로 연 인용 조문 팝업과 AI 답변에서만 조문
  즐겨찾기를 추가할 수 있어, 현재 읽고 있는 법령 전문의 조문을 바로 저장할
  수 없었기 때문이다.
- **주의**: 별표는 본문 HTML에 넣지 않고 기존 조문 앵커 위치에 화면 단추로
  얹는다. 조문 첫 블록에만 왼쪽 여백을 주므로 법령 제목ㆍ기본정보와 저장된
  원문 HTML은 바뀌지 않는다. 법령 전문과 조항호목 탭의 조문에만 표시하며
  편ㆍ장ㆍ절ㆍ관 제목에는 표시하지 않는다.
- **검증**: 본문 조문 별표 생성ㆍ제목 왼쪽 배치ㆍ추가ㆍ해제ㆍ다른 조문 상태
  분리 테스트를 포함해 본문 탭ㆍ조문 링크ㆍ3단비교 연관 테스트 77개 통과,
  오프스크린 화면에서 조문별 왼쪽 배치 확인. 기존 3단비교 정렬 테스트 실패
  3건은 이 변경과 무관하며 1.2.3 릴리스 기록에 남긴 기존 항목이다.

## 2026-08-27 · 제미나이 API 키 발급 안내 HTML 추가

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: `메뉴얼/제미나이 api 메뉴얼.pptx`의 5개 슬라이드를 모두
  확인해 Google AI Studio 접속, 새 키 생성, 생성된 키 복사, 프로그램 입력의
  4단계 HTML 안내서와 단계별 화면 이미지 4개를 `메뉴얼` 폴더에 추가했다.
- **왜**: PPT를 따로 열지 않고도 브라우저에서 발급 순서와 프로그램 입력
  위치를 바로 확인할 수 있게 하기 위해서다.
- **주의**: PPT의 API 키 세부정보 화면에는 전체 비밀키가 보여 결과물에서
  제외했다. 안내서에는 키가 일부만 표시된 목록 화면만 사용했으며 실제 API
  키를 문서나 화면 캡처에 공개하지 말라는 보안 안내를 넣었다. 원본 PPTX는
  참고 자료이므로 수정하거나 커밋하지 않았다.
- **검증**: PPTX의 5개 슬라이드 확인, HTML의 4개 단계ㆍ4개 이미지 참조와
  HTTPS 링크 3개 검사, 참조 이미지 존재 및 비밀키 형태 문자열 미포함 확인.
  연결된 미리보기 브라우저가 없어 자동 렌더링 캡처 검증은 수행하지 못했다.

## 2026-08-27 · 인증키 도움말 물음표를 박스 안쪽으로 이동

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 인증키 발급 안내 물음표 단추의 부모를 머리말 카드에서
  `apiCompact` 인증키 박스로 바꾸고, 박스 내부 우상단에 배치했다. 오른쪽에
  단추 전용 여백을 두어 저장 체크박스와 겹치지 않게 했다.
- **왜**: 물음표가 인증키 박스의 위쪽ㆍ오른쪽 경계 밖에 걸쳐 보여 별도의
  떠 있는 단추처럼 보였기 때문이다.
- **주의**: 단추는 인증키 박스 레이아웃에 넣지 않고 자식으로만 둔다. 박스
  크기가 바뀌면 Resize 이벤트에서 위치를 다시 잡으며, 오른쪽 레이아웃
  여백은 단추와 저장 체크박스가 겹치지 않게 확보한 자리다.
- **검증**: 인증키 안내 단추 테스트 5개 통과, 박스 내부 포함ㆍ저장 체크박스
  비겹침 좌표 및 오프스크린 렌더링 확인.

## 2026-08-27 · 사례 검색은 목록부터, 더블클릭하면 분할 본문

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 중앙부처 질의회신ㆍ법령해석례ㆍ판례 검색은 처음에 검색 조건과
  결과 목록만 전체 폭으로 표시하고, 결과를 더블클릭했을 때 기존 목록/본문
  분할 화면을 열도록 바꿨다. 새 검색을 시작하면 목록 전용 화면으로 돌아가며,
  저장내역에서 사례 본문을 열면 분할 화면으로 표시한다. 법령검색의 결과
  더블클릭은 기존처럼 본문 크게 보기를 유지한다.
- **왜**: 검색 결과를 고르기 전부터 빈 본문 검색ㆍ색상ㆍ메모 입력 도구가
  보여 검색 화면에서 무엇을 먼저 해야 하는지 헷갈렸기 때문이다.
- **주의**: 목록 전용 화면에서 한 번 누르기는 선택 표시만 바꾼다. 분할
  화면을 연 뒤에는 한 번 누르기로 저장 본문이나 조회 안내를 바꾸는 기존
  동작을 유지한다. 본문을 제공하지 않는 결과는 분할 화면을 열지 않는다.
- **검증**: 신규 전환 테스트와 기존 법령검색ㆍ크게 보기 회귀 테스트
  16개 통과, 목록 전용/분할 화면 오프스크린 렌더링 확인.

## 2026-08-27 · 1.2.3 릴리스

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.2.3
- **무엇을**: `APP_VERSION`과 `버전기록.md`를 1.2.3으로 갱신하고,
  파일명 변경 뒤 남아 있던 `버전_기록.md` 참조를 바로잡았다. 1.2.2 이후
  변경을 검증한 뒤 공개 `main`에 푸시하고 `v1.2.3` 태그로 배포한다.
- **왜**: 자동 업데이트 개선, CLI 오류 한글 안내, API 인증키 발급 도움말 등
  1.2.2 이후 완료된 변경을 정식 배포하기 위해서다.
- **검증**: 릴리스 관련 테스트 67개가 통과했고 PyInstaller onefile EXE가
  정상 빌드됐다. 결과물에서 1.2.3 버전ㆍ고정 자산명ㆍ필수 라이선스를
  확인했다.
- **주의**: 태그는 이 릴리스 커밋이 공개 원격 `main`에 올라간 뒤 같은
  커밋을 가리키도록 만들고, Actions가 생성한 네 개 자산을 확인한다. 전체
  pytest 일괄 실행은 Qt 네이티브 종료 충돌이 있었으나 해당 파일 단독 실행은
  모두 통과했다. 1.2.2 이후 바뀌지 않은 3단 비교 테스트 3건 실패와 기존
  회귀ㆍ스모크 스크립트 문제는 이번 릴리스 변경에서 생긴 회귀가 아니다.
- **남은 일**: Qt 테스트 종료 충돌, 3단 비교 기존 실패 3건, 회귀 골든 차이
  6건과 스모크 스크립트 정지를 별도 점검한다.

## 2026-08-27 · 기존 작업 기록 통합 및 작성자 구분 제거

- **커밋**: (이 항목과 같은 커밋)
- **버전**: 1.2.3
- **무엇을**: `버그수정_기록_2026-08-21.md`의 내용을 이 파일에 통합하고,
  항목 형식과 기존 기록에서 작성 주체 항목을 없앴다. `AGENTS.md`와
  `CLAUDE.md`의 안내도 변경 내용 중심으로 고쳤다.
- **왜**: 작업 기록을 한 파일에서 시간순으로 확인하고, 작성 주체를 따로
  구분하지 않기 위해서다.

## 2026-08-27 · 공동활용 법령종류 전체선택 안내

- **커밋**: 3367400
- **버전**: 1.2.3
- **무엇을**: API 인증키 발급 안내에서 공동활용 법령종류를 반드시
  전체선택하도록 설명을 고쳤다.
- **왜**: 일부만 고르면 법령ㆍ행정규칙ㆍ자치법규ㆍ별표서식 또는
  판례ㆍ해석례ㆍ질의회신 검색이 빠질 수 있기 때문이다.

## 2026-08-27 · 문의 이메일 오타 수정

- **커밋**: 03bc741
- **버전**: 1.2.3
- **무엇을**: 프로그램 정보 화면의 문의 주소를 `hakang17@naver.com`에서
  `hgkang17@naver.com`으로 고쳤다.
- **주의**: 같은 주소가 `docs/QT_SOURCE_OFFER.md`의 LGPL 소스 제공 요청처
  에도 적혀 있다. 한쪽만 고치면 배포판에서 연락이 닿지 않는다. 이 주소는
  LGPL v3가 요구하는 소스 제공 창구라 지우거나 틀리게 두면 안 된다.

## 2026-08-27 · 인증키 발급 안내 메뉴얼과 물음표 단추

- **커밋**: 0f7ecd5
- **버전**: 1.2.3
- **무엇을**: `메뉴얼/API인증키 발급안내.html`을 만들고, 머리말 카드 오른쪽
  위에 작은 물음표 단추를 얹어 기본 웹 브라우저로 열게 했다.
- **왜**: 인증키를 어디서 어떻게 받는지 프로그램 안에 안내가 없었다.
- **주의**: 물음표 단추는 **레이아웃에 넣지 않고** 머리말 카드의 자식으로만
  두고 `move`로 모서리에 붙인다. 레이아웃에 넣으면 그만큼 인증키 칸이
  밀린다. 위치는 머리말의 Resize 이벤트에서 다시 잡는다.
  `setFixedSize(20, 20)`만으로는 작아지지 않는다 — 전역 `QPushButton`의
  `min-height`가 이겨서 QSS에서 min/max를 함께 못 박아야 한다.
  HTML과 그림은 같은 폴더에 있어야 하며 spec의 `datas`에 다섯 파일을 모두
  넣었다. 빠지면 개발 중에만 보이고 EXE에서는 안 열린다.

## 2026-08-27 · CLI 오류 한글 설명과 이 수정기록 파일 도입

- **커밋**: c70abb8
- **버전**: 1.2.3
- **무엇을**: `llm/cli_errors.py`를 새로 만들어 ClaudeㆍCodex CLI가 남긴 영문
  오류와 종료 코드를 한글 설명으로 바꾼다. 두 CLI의 실패 경로에 연결했다.
  이 `수정기록.md`를 만들고 `AGENTS.md`ㆍ`CLAUDE.md`에서 반드시 읽고 쓰도록
  걸었다.
- **왜**: 실패하면 "종료 코드 1"이나 영문 stderr만 보여 무엇을 해야 할지
  알 수 없었다. 또한 작업 판단을 이어서 확인할 기록이 없었다.
- **주의**: 두 CLI 다 **성공 0, 실패 비0**만 쓰고 값마다 뜻을 정해 두지
  않았다(공식 문서 확인). 그래서 종료 코드로는 운영체제가 정한 것만
  알아보고 나머지는 stderr 문구로 가린다. 규칙은 좁은 것부터 차례로 맞추므로
  `_MESSAGE_RULES`에 새 규칙을 넣을 때 순서를 지켜야 한다.

## 2026-08-27 · 업데이트 실패 안내를 위치별로 나누고 법령검색 안내문 제거

- **커밋**: c090f3f
- **버전**: 1.2.3
- **무엇을**: 내려받기 전에 EXE 폴더에 쓸 수 있는지 먼저 확인하고, 안 되면
  받지 않고 위치와 조치를 알린다. 교체 실패 사유에 실제 경로와 운영체제
  오류를 담는다. 관리자 전용 폴더인지에 따라 안내를 나눈다. 법령검색 탭
  위쪽 안내 배너를 없앴다.
- **왜**: `C:\`나 `Program Files`에 둔 사람은 자동 업데이트가 늘 실패하는데
  "쓰기 권한을 확인해 주세요" 한 줄뿐이라 무엇을 해야 할지 알 수 없었다.
- **주의**: 교체 실패 사유는 교체 도우미가 **명령줄 인자**로 넘긴다. 줄바꿈을
  넣으면 인자가 쪼개진다. 사람이 읽을 여러 줄 안내는 받는 쪽(`main.py`)에서
  경로를 보고 덧붙인다. 임시 파일 정리(`unlink`)도 실패할 수 있어서 그
  예외가 원래 실패 사유를 가리지 않도록 `_discard_candidate`로 감쌌다.

## 2026-08-27 · 배포 EXE의 MCP 서버가 mcp 2.x로 죽던 문제

- **커밋**: 0f6a56e
- **버전**: 1.2.2
- **무엇을**: `requirements.txt`의 `mcp[cli]`에 `<2` 상한을 두고, MCP 서버
  시작 실패 시 영문 예외 상자 대신 표준오류에 한 줄만 남기게 했다.
- **왜**: mcp 2.x가 `FastMCP`를 `MCPServer`로 바꾸면서 `mcp.server.fastmcp`가
  사라졌다. AI 에이전트가 같은 EXE를 `--mcp-server`로 띄울 때 죽었다.
- **주의**: **로컬에서는 재현되지 않는다.** 개발 PC에는 옛 버전이 깔려 있고,
  GitHub Actions만 매번 최신을 새로 받는다. 의존성에 상한을 열어 두면
  배포 EXE에서만 터지는 사고가 또 난다.

## 2026-08-27 · 업데이트 안내창의 영어 단추와 잘린 이름

- **커밋**: 6a2284c
- **버전**: 1.2.1
- **무엇을**: `setDetailedText`가 만드는 단추를 "자세히"로 바꾸고, 창 최소
  폭을 확보했다. 안내 문구는 새 버전ㆍ현재 버전을 함께 알리던 원래대로 두고
  단추만 확인ㆍ취소로 두었다.
- **주의**: 그 단추는 Qt가 직접 붙여 이름을 정할 수 없다. 역할이
  `ActionRole`인 단추를 찾아 바꾼다. `QMessageBox`는 글 폭에만 맞춰 창을
  잡아서, 단추가 늘면 이름이 잘린다. 격자 맨 아래에 빈 칸을 깔아 폭을 준다.

## 2026-08-27 · 키워드검색을 법령검색 탭 카테고리로 합침

- **커밋**: 8a0875c
- **버전**: 1.2.0
- **무엇을**: 연관검색ㆍ직접검색을 왼쪽 주 메뉴에서 빼고 법령검색 탭의
  카테고리 바로 옮겼다. 통합검색이 목록 검색과 두 결과를 한 목록으로
  합쳐 보여 준다.
- **주의**: 주 메뉴 줄 번호와 `self.tabs` 페이지 번호가 **1:1로 묶여 있다.**
  메뉴를 늘리거나 줄이면 하드코딩된 페이지 번호(`setCurrentIndex`)도 함께
  고쳐야 한다. 화면 자체는 `AiLawSearchTab` 두 개를 그대로 쓰므로 저장내역
  복원 경로가 유지된다. 두 API는 조문 단위로 답해서 통합검색에 넣을 때는
  법령 단위로 접고 겹치는 것과 별표ㆍ서식을 뺀다.

## 2026-08-27 · 인증키 가리기와 공개 저장소 첫 배포 정리

- **커밋**: 2087410, aed18ae, eff99b3, f0b0293, f6875cd
- **버전**: 1.0.0 → 1.1.0
- **무엇을**: 공개 저장소로 전환하며 버전을 1.0.0부터 다시 셌다. 배포 자산
  이름을 `law_info_search.*`로 통일했다. API 인증키를 기본으로 가리고
  표시ㆍ숨김 단추를 붙였다.
- **주의**: **GitHub은 Release 자산 이름에서 한글과 공백을 잘라낸다.**
  `국가법령정보 통합검색.exe`를 올리면 `default.exe`가 된다. 앱은
  `UPDATE_ASSET_NAME`과 정확히 같은 이름만 찾으므로 자동 업데이트가 조용히
  실패한다. 자산 이름에 한글ㆍ공백을 쓰지 않는다.

## 2026-08-21 · 화면·본문 파싱·기능 오류 일괄 수정

- **커밋**: 공개 전 내부 이력
- **버전**: 공개 전 내부 단계
- **무엇을**: 화면 밀림과 깜빡임, 수립지침류 본문 파싱, 즐겨찾기,
  메모, 하위법령 자동 링크, 성능 및 기타 UI 오류를 수정했다.
- **왜**: 당시 발견한 오류와 해결 과정, 회귀 시 주의할 조건을 한데
  보존하기 위해 상세 내역을 아래에 남긴다.

### 1. 화면 밀림·깜빡임

- **하단 조항 탭바(reference_tabs) 밀림**: 탭이 없을 때 `hide()`, 생기면 `show()`로 처리해 처음 조항을 열 때마다 본문이 줄어들며 밀렸음. 고정 높이(44px)로 항상 공간을 확보하도록 수정.
- **API 호출 시 하단 진행바(progress) 표시로 본문이 줄어듦**: `hide()`/`show()` 대신 항상 공간을 확보하고 투명도만 조절하도록 수정.
- **본문검색칸 포커스 시 스크롤이 계속 밀림**: 근본 원인은 검색창이 포커스를 받으면 `border` 두께가 얇은 기본값에서 `2px`로 바뀌면서 레이아웃 폭 자체가 변해 본문이 리플로우된 것. 포커스 여부와 무관하게 항상 `2px` 테두리를 쓰도록 CSS 수정.
- **문서를 처음 열 때 / 탭 전환 시 줄바꿈이 잠깐 바뀌며 깜빡임**: 3단비교 버튼 자리를 위한 본문 오른쪽 여백(`setRightMargin`)을 목차를 채운 **뒤에** 적용하고 있어, 여백 없이 넓게 한 번 그려졌다가 다시 좁게 그려지는 게 보였음. 여백 설정을 맨 앞으로 당기고, `setUpdatesEnabled(False)`로 갱신을 잠갔다가 레이아웃을 강제로 확정한 뒤에만 다시 그리도록 수정 (`_set_detail_document`, `_restore_document_state` 둘 다 동일하게 적용).
- **탭 전환 때 가로 스크롤바가 잠깐 나타났다 사라짐**: 모든 본문 viewport 크기 변화를 실제 창 크기 조절로 간주해 이전 폭을 140ms 동안 `FixedPixelWidth`로 유지했기 때문에, 목차 표시·탭 전환으로 새 viewport가 좁아지는 동안 가로 스크롤바가 생겼음 → 최상위 창 크기가 실제로 변할 때만 지연 줄바꿈을 사용하고, 지연 중에는 가로 스크롤바를 숨겼다가 최종 `WidgetWidth` 줄바꿈 뒤 원래 정책을 복원.

### 2. 수립지침류(행정규칙) 본문 파싱

원문 API가 문장 중간에 줄바꿈을 잘못 넣어 보내는 경우가 여러 패턴으로 있었음. 모두 `body_to_html` 전처리 단계에서 복원하도록 수정.

- **하이픈에서 줄바꿈**: `1-5-2-1.`처럼 하이픈이 많은 번호가 `1-` / `5-2-1.` 두 줄로 쪼개져 옴 → 하이픈으로만 끝나는 줄은 다음 줄과 합침.
- **"(1)(2)(3)..." 항목이 줄바꿈 없이 붙어서 옴**: `(1) 내용(2) 내용(3) 내용`처럼 한 줄에 이어 붙는 경우, `(1)`로 시작해 번호가 정확히 순서대로 이어질 때만 각 항목을 줄로 분리.
- **표제+첫 항목이 붙어서 옴**: `7-3-2-2. 지형(1) 기존...` 처럼 짧은 표제 뒤에 `(1)`만 붙어 있고 `(2)` 이후는 이미 줄이 분리된 경우, 다음 줄이 실제로 `(2)`로 시작할 때만 `(1)` 앞에서 분리.
- **완결된 문장 뒤에 다음 항목 번호가 붙어서 옴**: `...대체하려는 경우(2) 환경성 검토를...`처럼 문장이 끝나는 어미(다/경우/함/음/것 등) 바로 뒤에 번호가 붙는 경우 분리.
- **다른 항목을 인용하는 문구가 조각조각 끊겨서 옴**: `①(1)의①부터⑥까지의 어느 하나에...`가 `①` / `(1)의` / `①부터` / `⑥까지의...` 네 줄로 쪼개져 옴 → 표식만 있고 실질 내용이 없는(조사만 남는) 줄은 실질 내용이 나올 때까지 계속 다음 줄과 합침.
- **다른 지침 항목 번호를 그대로 인용하는 문장**: `...변경 등\n1-6-2-1.의 각 검토가...`처럼 참조하는 번호가 자기 번호 뒤에 공백 없이 조사가 붙어 오는 경우(정상 항목은 번호 뒤에 공백이 있음), 새 항목이 아니라 앞 문장이 이어지는 것으로 보고 병합.
- **인용된 법령 조문이 지침 자체 조문처럼 굵게 표시·줄바뀜**: `제25조(...)` 같은 인용문이 지침 문서 안에서 법령 문서처럼 굵은 독립 표제로 뽑혀 나오고 줄이 내려갔음. 지침류(`administrative_rule`)에서는 `제OO조` 패턴을 구조적 표제로 취급하지 않고 일반 문단으로 흘려보내도록 수정.
- **인용된 법령 조문이 지침 자체 목차에 섞여 나옴**: 위와 같은 인용 조문이 좌측 TOC 트리에도 항목으로 추가되고 있었음. 지침류에서는 목차에 넣지 않도록 수정(본문 표시와 앵커는 유지).
- **번호에 특수 하이픈 사용**: 줄바꿈 방지를 위해 일반 하이픈(-)을 줄바꿈 없는 특수 하이픈(‑, U+2011)으로 치환하고 있었는데, 복사해서 실제 법령 사이트에서 검색이 안 됨 → 일반 하이픈으로 되돌림(대신 마커 폭을 실측해 줄바꿈되지 않게 처리).
- **한 항목 안의 참조번호가 별도 항목으로 분리됨**: 원문의 `⑩ ①ㆍ②ㆍ⑤ㆍ⑥ㆍ⑧ 및 ⑨의 규정…`과 `⑦ 상기 ① 및 ②의 규정…`에서 문장 안의 ①·② 등을 모두 새 줄로 나눠 빈 항목처럼 표시했음 → 번호 뒤가 가운데점·접속사·조사인 경우 참조번호로 판별하여 같은 항목으로 유지하고, 이미 잘못 저장된 평문도 열 때 다시 병합하도록 수정.
- **⑯~⑳ 번호가 사라짐**: 일부 지침 API 원문의 `<16>`~`<20>` 표기를 HTML 태그로 오인해 번호만 제거하고 내용을 앞 항목에 합쳤음 → HTML 태그 제거 전에 ⑯~⑳으로 복원. 새 행정규칙 저장본에는 정규화된 원문 섹션과 파싱 버전을 함께 저장하여 QTextDocument 평문을 다시 원문처럼 재파싱하지 않도록 수정.
- **상위 괄호 항목이 앞 문장에 붙음**: `⑮ … 기반시설(4) 재해취약성분석…`처럼 동그라미 항목 직후의 `(4)`가 줄바꿈되지 않았음 → 수립지침류에서만 완성된 동그라미 항목 뒤의 괄호 번호를 상위 항목으로 분리. 일반 법령 파싱에는 적용하지 않음.

### 3. 즐겨찾기

- **즐겨찾기 해제 기능 소실**: `_remove_selected_favorite` 해제 함수 자체는 있었지만 우클릭 메뉴가 폴더에만 동작하도록 막혀 있고 단축키도 연결이 안 되어 있던 죽은 코드였음. 항목 우클릭 메뉴, Delete 키, 그리고 항목 왼쪽 별표(★) 클릭으로 바로 해제할 수 있게 복구.
- **즐겨찾기를 폴더로 옮겨도 재실행하면 밖으로 나가 있음**: Qt 트리위젯 드래그앤드롭이 같은 부모 안에서 순서만 바꿀 때는 `rowsMoved`가 뜨지만, 다른 폴더 "안으로" 옮길 때(부모가 바뀔 때)는 내부적으로 제거 후 재삽입으로 처리되어 `rowsMoved`가 발생하지 않아 저장이 안 됐음. `rowsInserted`도 함께 감지하도록 추가.
- **즐겨찾기 선택 음영이 별표를 덮지 못함**: 별표 자리를 위해 줄인 영역 기준으로 선택 음영을 그리고 있어 음영이 별표 앞에서 잘려 보였음. 원래 전체 영역 기준으로 그리도록 수정.

### 4. 메모

- **메모 버튼이 눌러도 반응 없음 (당일 회귀)**: 오른쪽 띠지 클릭 시 팝업 위치 지정 기능을 추가하며 `_edit_selection_memo`에 파라미터를 붙였는데, `QPushButton.clicked` 시그널이 넘기는 `bool` 값이 그 파라미터로 잘못 들어가 매번 조용히 실패했음. 연결 방식을 람다로 감싸고, 인자 타입도 `isinstance` 체크로 방어.
- **메모 밑줄이 링크 색과 겹쳐 구분 안 됨**: 파란 계열 밑줄 → 빨간색(`#d9362e`)으로 변경.
- **저장된 메모를 다시 열면 바로 편집 상태로 뜸**: 읽기 전용으로 열리고 "수정"을 눌러야 편집 가능하도록, "수정"/"메모 삭제"/"닫기(취소)" 버튼으로 구조 변경.
- **최초 메모 생성 때 본문 전체가 다시 줄바꿈됨**: 메모가 없을 때 오른쪽 띠지를 `hide()`했다가 첫 메모에서 `show()`하여 본문 폭이 14px 줄어들었음 → 세 상세화면 모두 메모 유무와 관계없이 14px 전용 띠지 칸을 처음부터 확보하고 표식만 표시·제거하도록 수정.

### 5. 대통령령·하위법령 자동 링크

- **본문 안 "대통령령" 텍스트가 일반 조문 인용 링크와 색·밑줄이 다름**: 별도 코드(`_apply_inline_subordinate_links`)가 그리는 색(`#1768aa`, 밑줄 없음)이 일반 링크 색(`#006dcc`, 밑줄 있음)과 달랐음 → 통일.
- **일부 항·호(예: 법 제8조③, ④4호)의 "대통령령" 링크가 안 열리고 "하위법령 링크 없음" 표시**: 두 가지 원인.
  1. 시행령 조문 하나가 법률의 여러 항·호를 한꺼번에 위임 근거로 나열해도(`_law_source_units_referenced_by_decree`), 첫 번째 매칭 직후 `break`로 탐색을 끝내 나머지 항·호에는 링크가 안 붙었음 → `break` 제거, 전부 수집.
  2. 매칭되는 링크가 0건이어도 빈 배열을 인코딩한 `lawsub://open?data=W10` 형태의 "그럴듯한" 링크를 만들어 클릭 가능한 것처럼 밑줄을 쳤음 → 링크 후보가 없으면 애초에 href를 만들지 않도록 수정.

### 6. 성능

- **"대통령령" 링크 매칭 로직이 O(N²)**: 언급이 나올 때마다 그 앞부분 전체를 매번 처음부터 다시 훑는 방식이라 언급이 많은 긴 조문에서 느려짐 → 항·호 표식 위치를 한 번만 훑어 표로 만들고 이분 탐색(bisect)으로 찾도록 변경. 결과 동일함을 합성 테스트로 확인.
- **저장된 법령을 열 때마다 "대통령령" 링크 자동 연결용 3단비교 API를 다시 호출하며 검색·조회 버튼을 잠그고 진행바를 띄움**: 일반 검색과 같은 블로킹 방식(`_start_worker`)을 쓰고 있어, API 응답이 오는 몇 초~몇십 초 동안 화면이 멈춘 것처럼 보였음 → 버튼 잠금·진행바 없이 조용히 백그라운드에서 실행하는 전용 경로(`_start_background_worker`)로 분리.
- **3단비교 API 응답을 기다리는 데 최대 22초, 앱을 재시작하면 매번 다시 기다려야 함**: 3단비교 결과(원문 API 응답)를 저장 파일에 함께 저장해, 한 번 확인한 법령은 다음에 열 때 API를 다시 부르지 않고 바로 링크가 연결되도록 함. "API 갱신"을 직접 누르면 저장된 3단비교 결과를 지우고 새로 확인.

### 7. 기타 UI

- **PDF 미리보기 영역이 좁음**: 별표·서식 PDF 미리보기 시 위쪽 메타정보 영역이 너무 넓어 미리보기가 작았음 → 메타정보 영역을 구분선 손잡이 수준(1px)까지 접고 PDF가 나머지 공간을 거의 다 차지하도록 변경(드래그로 다시 펼칠 수 있음).
- **최근 검색어 클릭 시 검색창만 채워지고 검색은 안 됨**: 클릭 시 검색까지 바로 실행되도록 수정.
- **메타정보(구분/공포일자 등) 박스가 본문과 잘 구분 안 됨**: 라벨을 더 굵게, 안팎 여백을 더 넓게 조정.
- **즐겨찾기 화면 상단 설명 문구가 불필요하게 김**: "저장 위치: ..." 줄만 남기고 위 설명 문장은 제거.

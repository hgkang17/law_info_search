# 수정기록

이 저장소는 **여러 AI 도구와 사람이 번갈아 가며** 고친다. 서로 무엇을 왜
바꿨는지 모르면 같은 자리를 되돌리거나 이미 고친 버그를 다시 만든다.
그래서 모든 작업은 이 파일을 거친다.

## 지켜야 할 것

1. **작업을 시작하기 전에 이 파일의 최근 10개 항목을 읽는다.**
   특히 "주의" 줄은 같은 실수를 막으려고 남긴 것이다.
2. **작업을 끝내면 맨 위(최신이 위)에 항목을 하나 추가한다.**
   커밋과 함께 올린다. 커밋을 나눠도 항목은 하나로 묶어도 된다.
3. **되돌린 작업도 적는다.** 왜 되돌렸는지가 다음 사람에게 가장 중요하다.
4. 이 파일은 `버전_기록.md`와 다르다. 저기는 **사용자에게 보이는 버전별
   변경 요약**이고, 여기는 **고친 사람이 다음 사람에게 남기는 작업 기록**
   이다. 코드에 드러나지 않는 판단ㆍ함정ㆍ보류한 일을 여기 적는다.

## 항목 형식

```markdown
## YYYY-MM-DD · 한 줄 제목

- **작업자**: (Claude Code / Codex / Gemini / 사람 이름)
- **커밋**: 짧은 해시 (여러 개면 쉼표로)
- **버전**: 이 작업이 들어간 APP_VERSION (릴리즈 안 했으면 "미배포")
- **무엇을**: 바꾼 내용
- **왜**: 그렇게 한 이유
- **주의**: 다음 사람이 밟기 쉬운 함정 (없으면 줄째 생략)
- **남은 일**: 하다 만 것ㆍ보류한 것 (없으면 줄째 생략)
```

---

## 2026-08-27 · 문의 이메일 오타 수정

- **작업자**: Claude Code
- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: 프로그램 정보 화면의 문의 주소를 `hakang17@naver.com`에서
  `hgkang17@naver.com`으로 고쳤다.
- **주의**: 같은 주소가 `docs/QT_SOURCE_OFFER.md`의 LGPL 소스 제공 요청처
  에도 적혀 있다. 한쪽만 고치면 배포판에서 연락이 닿지 않는다. 이 주소는
  LGPL v3가 요구하는 소스 제공 창구라 지우거나 틀리게 두면 안 된다.

## 2026-08-27 · 인증키 발급 안내 메뉴얼과 물음표 단추

- **작업자**: Claude Code
- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: `메뉴얼/API인증키 발급안내.html`을 만들고, 머리말 카드 오른쪽
  위에 작은 물음표 단추를 얹어 기본 웹 브라우저로 열게 했다.
- **왜**: 인증키를 어디서 어떻게 받는지 프로그램 안에 안내가 없었다.
- **주의**: 물음표 단추는 **레이아웃에 넣지 않고** 머리말 카드의 자식으로만
  두고 `move`로 모서리에 붙인다. 레이아웃에 넣으면 그만큼 인증키 칸이
  밀린다. 위치는 머리말의 Resize 이벤트에서 다시 잡는다.
  `setFixedSize(20, 20)`만으로는 작아지지 않는다 — 전역 `QPushButton`의
  `min-height`가 이겨서 QSS에서 min/max를 함께 못 박아야 한다.
  HTML과 그림은 같은 폴더에 있어야 하며 spec의 `datas`에 다섯 파일을 모두
  넣었다. 빠지면 개발 중에만 보이고 EXE에서는 안 열린다.

## 2026-08-27 · CLI 오류 한글 설명과 이 수정기록 파일 도입

- **작업자**: Claude Code
- **커밋**: (이 항목과 같은 커밋)
- **버전**: 미배포
- **무엇을**: `llm/cli_errors.py`를 새로 만들어 ClaudeㆍCodex CLI가 남긴 영문
  오류와 종료 코드를 한글 설명으로 바꾼다. 두 CLI의 실패 경로에 연결했다.
  이 `수정기록.md`를 만들고 `AGENTS.md`ㆍ`CLAUDE.md`에서 반드시 읽고 쓰도록
  걸었다.
- **왜**: 실패하면 "종료 코드 1"이나 영문 stderr만 보여 무엇을 해야 할지
  알 수 없었다. 그리고 여러 AI가 이 저장소를 함께 고치는데 서로의 판단이
  남지 않았다.
- **주의**: 두 CLI 다 **성공 0, 실패 비0**만 쓰고 값마다 뜻을 정해 두지
  않았다(공식 문서 확인). 그래서 종료 코드로는 운영체제가 정한 것만
  알아보고 나머지는 stderr 문구로 가린다. 규칙은 좁은 것부터 차례로 맞추므로
  `_MESSAGE_RULES`에 새 규칙을 넣을 때 순서를 지켜야 한다.

## 2026-08-27 · 업데이트 실패 안내를 위치별로 나누고 법령검색 안내문 제거

- **작업자**: Claude Code
- **커밋**: c090f3f
- **버전**: 미배포
- **무엇을**: 내려받기 전에 EXE 폴더에 쓸 수 있는지 먼저 확인하고, 안 되면
  받지 않고 위치와 조치를 알린다. 교체 실패 사유에 실제 경로와 운영체제
  오류를 담는다. 관리자 전용 폴더인지에 따라 안내를 나눈다. 법령검색 탭
  위쪽 안내 배너를 없앴다.
- **왜**: `C:\`나 `Program Files`에 둔 사람은 자동 업데이트가 늘 실패하는데
  "쓰기 권한을 확인해 주세요" 한 줄뿐이라 무엇을 해야 할지 알 수 없었다.
- **주의**: 교체 실패 사유는 교체 도우미가 **명령줄 인자**로 넘긴다. 줄바꿈을
  넣으면 인자가 쪼개진다. 사람이 읽을 여러 줄 안내는 받는 쪽(`main.py`)에서
  경로를 보고 덧붙인다. 임시 파일 정리(`unlink`)도 실패할 수 있어서 그
  예외가 원래 실패 사유를 가리지 않도록 `_discard_candidate`로 감쌌다.

## 2026-08-27 · 배포 EXE의 MCP 서버가 mcp 2.x로 죽던 문제

- **작업자**: Claude Code
- **커밋**: 0f6a56e
- **버전**: 1.2.2
- **무엇을**: `requirements.txt`의 `mcp[cli]`에 `<2` 상한을 두고, MCP 서버
  시작 실패 시 영문 예외 상자 대신 표준오류에 한 줄만 남기게 했다.
- **왜**: mcp 2.x가 `FastMCP`를 `MCPServer`로 바꾸면서 `mcp.server.fastmcp`가
  사라졌다. AI 에이전트가 같은 EXE를 `--mcp-server`로 띄울 때 죽었다.
- **주의**: **로컬에서는 재현되지 않는다.** 개발 PC에는 옛 버전이 깔려 있고,
  GitHub Actions만 매번 최신을 새로 받는다. 의존성에 상한을 열어 두면
  배포 EXE에서만 터지는 사고가 또 난다.

## 2026-08-27 · 업데이트 안내창의 영어 단추와 잘린 이름

- **작업자**: Claude Code
- **커밋**: 6a2284c
- **버전**: 1.2.1
- **무엇을**: `setDetailedText`가 만드는 단추를 "자세히"로 바꾸고, 창 최소
  폭을 확보했다. 안내 문구는 새 버전ㆍ현재 버전을 함께 알리던 원래대로 두고
  단추만 확인ㆍ취소로 두었다.
- **주의**: 그 단추는 Qt가 직접 붙여 이름을 정할 수 없다. 역할이
  `ActionRole`인 단추를 찾아 바꾼다. `QMessageBox`는 글 폭에만 맞춰 창을
  잡아서, 단추가 늘면 이름이 잘린다. 격자 맨 아래에 빈 칸을 깔아 폭을 준다.

## 2026-08-27 · 키워드검색을 법령검색 탭 카테고리로 합침

- **작업자**: Claude Code
- **커밋**: 8a0875c
- **버전**: 1.2.0
- **무엇을**: 연관검색ㆍ직접검색을 왼쪽 주 메뉴에서 빼고 법령검색 탭의
  카테고리 바로 옮겼다. 통합검색이 목록 검색과 두 결과를 한 목록으로
  합쳐 보여 준다.
- **주의**: 주 메뉴 줄 번호와 `self.tabs` 페이지 번호가 **1:1로 묶여 있다.**
  메뉴를 늘리거나 줄이면 하드코딩된 페이지 번호(`setCurrentIndex`)도 함께
  고쳐야 한다. 화면 자체는 `AiLawSearchTab` 두 개를 그대로 쓰므로 저장내역
  복원 경로가 유지된다. 두 API는 조문 단위로 답해서 통합검색에 넣을 때는
  법령 단위로 접고 겹치는 것과 별표ㆍ서식을 뺀다.

## 2026-08-27 · 인증키 가리기와 공개 저장소 첫 배포 정리

- **작업자**: Claude Code
- **커밋**: 2087410, aed18ae, eff99b3, f0b0293, f6875cd
- **버전**: 1.0.0 → 1.1.0
- **무엇을**: 공개 저장소로 전환하며 버전을 1.0.0부터 다시 셌다. 배포 자산
  이름을 `law_info_search.*`로 통일했다. API 인증키를 기본으로 가리고
  표시ㆍ숨김 단추를 붙였다.
- **주의**: **GitHub은 Release 자산 이름에서 한글과 공백을 잘라낸다.**
  `국가법령정보 통합검색.exe`를 올리면 `default.exe`가 된다. 앱은
  `UPDATE_ASSET_NAME`과 정확히 같은 이름만 찾으므로 자동 업데이트가 조용히
  실패한다. 자산 이름에 한글ㆍ공백을 쓰지 않는다.

## 이전 기록

이 파일을 만들기 전의 이력은 `git log`와 `버그수정_기록_2026-08-21.md`,
`버전_기록.md`에 있다.

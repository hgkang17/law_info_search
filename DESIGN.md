---
name: 국가법령정보 통합검색 UI
description: 빠르게 찾고, 비교하고, 근거를 읽는 정돈된 법률 작업대
platform: PySide6 desktop
tokens: ui/theme.py::WORKBENCH_COLORS
styles: ui/main_window.py::_apply_style
components: ui/widgets.py
responsive_breakpoint: 1100px
minimum_window: 900x640
---

# North Star

이 제품은 **정돈된 법률 작업대**다. 사용자는 장식적인 포털을 구경하는 사람이
아니라 법령·판례·해석례를 찾아 근거를 대조하는 실무자다. 화면은 공공 서비스의
신뢰감, 데스크톱 도구의 밀도, 긴 문서를 읽는 차분함을 동시에 제공해야 한다.

# Product Context

핵심 흐름은 검색 조건 입력 → 결과 탐색 → 본문 열기 → 조문 비교·인용 → 저장 또는
AI 검토다. 검색과 읽기가 같은 창에서 반복되므로 현재 위치, 열린 본문, 선택 상태를
항상 잃지 않아야 한다. 한국어 법률 용어와 긴 법령명이 기본 콘텐츠다.

# Design Principles

- 탐색 상태를 숨기지 않는다. 선택된 업무 영역과 열린 본문을 항상 식별할 수 있게 한다.
- 본문이 주인공이다. 색과 장식은 검색·선택·상태 전달에만 사용한다.
- 조밀하되 답답하지 않게 한다. 반복 행은 촘촘하게, 주요 동작은 28px 이상으로 둔다.
- 기다림을 설명한다. 네트워크·AI·CLI 작업은 GUI 밖에서 실행하고 상태와 취소 경로를 둔다.
- 마우스 전용 동작을 만들지 않는다. 포커스, 접근성 이름, 툴팁을 같은 기능 계약으로 본다.

# Visual Language

캔버스는 옅은 청회색, 작업 면은 흰색, 주 내비게이션은 짙은 남색을 사용한다. 청록색은
현재 선택과 주요 동작에만 쓴다. 그라디언트와 과도한 그림자는 쓰지 않으며 3–6px의 작은
모서리와 선명한 1px 경계로 문서 도구다운 구조를 만든다. 실제 색상 값은
`ui/theme.py`의 `WORKBENCH_COLORS`가 단일 기준이다.

기본 글꼴은 맑은 고딕이며, 구획 제목에는 Pretendard를 보조적으로 사용한다. 본문은 사용자가
7–18pt 범위에서 조절할 수 있다. 문장형 레이블을 우선하고 아이콘만 있는 동작에는 접근성
이름과 툴팁을 반드시 제공한다.

# Layout and Responsive Behavior

1440px 기준으로 헤더, 좌측 업무 내비게이션, 우측 작업 영역을 구성한다. 창 폭이 1100px보다
작아지면 좌측 내비게이션을 숨기고 헤더의 `compactNavigation` 콤보로 동일한 목적지를 제공한다.
최소 창 크기는 900×640이며 가로 스크롤로 전체 앱 구조를 버티지 않는다. 크게 보기에서는
헤더만 남기고 내비게이션을 접어 본문 폭을 최대로 확보한다.

# Components and Interaction

- 헤더: 로고, 좁은 창 화면 선택기, 열린 본문 탭, API 상태·설정, 정보·업데이트 동작.
- 업무 내비게이션: 넓은 창의 그룹 목록과 좁은 창의 콤보가 같은 페이지 상태를 양방향 동기화한다.
- 검색 도구막대: 입력, 상세 조건, 실행 버튼 순으로 읽히며 Enter와 Esc 흐름을 보존한다.
- 본문 탭: 즐겨찾기와 닫기 동작은 22px 이상이고 키보드 포커스를 받는다.
- 본문 작업: 3단 비교, 글자 크기, 음영, 메모, 복사 동작은 본문 위계를 침범하지 않는다.
- AI 패널: 제공자별 대화와 상태를 분리하고 모델·CLI 조회는 앱 전체에서 중복 실행하지 않는다.

# Accessibility and Content

일반 텍스트는 WCAG AA 대비를 목표로 하며 짙은 헤더의 보조 링크도 밝은 텍스트를 사용한다.
모든 실행 단추는 Tab으로 도달 가능해야 하고 `NoFocus`는 순수 장식에만 허용한다. 포커스는
청록색 2px 윤곽으로 표시한다. 오류는 원인과 다음 행동을 한국어로 설명하며, 비어 있는 상태는
무엇이 없는지와 시작 방법을 함께 알려 준다.

# Implementation Contract

색상 역할은 `WORKBENCH_COLORS`, 공용 위젯 구조는 `ui/widgets.py`, objectName 기반 스타일은
`LawSearchWindow._apply_style`에서 관리한다. 같은 의도의 UI가 세 곳 이상 반복되면 공용 위젯이나
토큰으로 승격한다. 새 화면은 900px와 1440px에서 시각 검증하고, 키보드 포커스·접근성 이름·종료
시 백그라운드 작업 정리를 회귀 테스트에 포함한다.

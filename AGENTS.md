# 법령프로그램 작업 지침

## 시작하기 전에

이 저장소의 변경 배경과 주의사항은 `수정기록.md`에 이어서 남긴다.

1. **작업 전에 [`수정기록.md`](수정기록.md)의 최근 항목을 읽는다.** 앞선
   작업에서 무엇을 왜 바꿨는지, 어떤 함정이 있었는지가 거기 있다.
2. **작업이 끝나면 `수정기록.md` 맨 위에 항목을 추가하고 함께 커밋한다.**
   형식은 그 파일 안에 적어 두었다.
3. 코드만 보고는 알 수 없는 판단ㆍ함정ㆍ보류한 일을 반드시 남긴다.

## 정규식 수정 원칙

- 정규식을 추가하거나 수정할 때는 해당 사례만 고치고 기존 정규식의 적용 범위를 불필요하게 넓히지 않는다.
- 앞뒤 문맥, 번호 위계, 조사, 구분기호 등 확실한 조건을 함께 사용해 다른 본문 형식에 미치는 영향을 최소화한다.
- 변경 전에 관련 기존 정규식과 처리 순서를 확인한다. 새 규칙이 기존 규칙보다 먼저 또는 나중에 실행될 때 생기는 재분리·재결합·오탐 가능성도 확인한다.
- 수정한 사례의 재현 테스트를 추가하고, 기존 정규식 관련 회귀 테스트도 함께 실행한다.
- 기존 테스트가 깨지면 단순히 기대값을 바꾸지 말고 실제 동작 변화가 의도된 것인지 먼저 확인한다.
- 수립지침 번호, 괄호 번호, 동그라미 번호, 목 번호, 날짜, 소수, 연도 자리표시자, 조항 인용 등 기존에 구분해 둔 형식이 새 규칙에 잘못 걸리지 않는지 확인한다.
- 파싱 결과가 화면 변환 과정에서 다시 분리되거나 합쳐질 수 있으므로, 가능하면 정규화된 평문과 최종 HTML 표시를 모두 검증한다.
- 저장된 구버전 캐시에도 적용되어야 하는 변경이면 캐시 재정규화 동작을 확인하고 파싱 버전을 갱신한다.

## 실행 파일(exe) 취급 원칙

- `dist/`의 실행 파일은 빌드 산출물이다. **직접 수정하지 않는다.** 바이너리를
  손대면 소스와 어긋나고 다음 빌드에서 그대로 사라진다.
- 일반적인 소스 수정ㆍ테스트에서는 **로컬 EXE를 빌드하지 않는다.** 사용자가
  로컬 EXE 생성 또는 로컬 빌드 검증을 명시적으로 요청한 경우에만
  `국가법령정보 통합검색.spec`으로 빌드한다.

  ```
  python -m PyInstaller --noconfirm --clean "국가법령정보 통합검색.spec"
  ```

- `APP_VERSION` 상승, 릴리스 태그 생성ㆍ푸시, GitHub Release 발행은 사용자가
  **명시적으로 릴리스를 요청한 경우에만** 수행한다. 일반적인 수정 요청이나
  커밋ㆍ푸시 요청을 릴리스 요청으로 확대 해석하지 않는다.
- 릴리스 요청을 받으면 기본적으로 태그 푸시로 GitHub Actions의 Windows
  Release 빌드를 사용한다. 이 경우에도 로컬 EXE는 따로 만들지 않으며,
  사용자가 로컬 EXE까지 명시적으로 요청했을 때만 로컬 빌드를 추가한다.

- 새 자산(아이콘·글꼴 등)을 추가하면 spec의 `datas`에도 함께 넣는다. 넣지
  않으면 개발 중에는 보이고 exe에서만 빠진다.
- 아이콘을 바꾸면 `molit_law_logo.svg`와 `molit_law_logo.ico`를 함께 갱신한다.
  exe는 `.ico`만 쓴다.
- 캐시 폴더(`# law 캐시` 안의 `저장내역`·`조문`·`검색목록`)는 exe가 있는 자리에
  생긴다.
- 배포는 실행 파일 하나(onefile)로 한다. 공개 GitHub 저장소에서 소스와
  함께 EXE를 배포하는 것 자체는 가능하다.
- onefile은 Qt DLL을 EXE 안에 담으므로 LGPL v3 제4조 (d)(1)의 교체 가능한
  공유 라이브러리 방식은 아니다. 공개 Release에서는 제4조 (d)(0)의 소스
  제공 방식에 맞춰 동일 태그의 프로그램 소스ㆍ빌드 설정ㆍ빌드 정보를 함께
  제공하고, 수정한 Qt로 다시 빌드하고 실행할 수 있는 안내를 유지한다.
- `requirements.txt`의 의존성에 상한을 열어 두지 않는다. 로컬에 옛 버전이
  깔려 있으면 개발 중에는 멀쩡하고, GitHub Actions가 새로 받아 만든 EXE
  에서만 터진다. `mcp[cli]`가 실제로 그렇게 깨졌다(2.x에서 FastMCP가
  MCPServer로 바뀜).
- 연관검색ㆍ직접검색(키워드검색)은 왼쪽 주 메뉴가 아니라 법령검색 탭의
  카테고리 바에 있다. 화면 자체는 `AiLawSearchTab` 두 개를 그대로 쓰고,
  법령검색 탭이 `attach_keyword_page`로 받아 스택에 끼운다. 주 메뉴 줄
  번호와 `self.tabs` 페이지 번호는 1:1이라 메뉴를 늘리거나 줄이면 하드코딩된
  페이지 번호도 함께 고쳐야 한다.
- 통합검색은 목록 검색 결과에 연관검색ㆍ직접검색 결과까지 합쳐 보여 준다.
  두 API는 조문 단위로 답하므로 단독 검색과 같이 조문 행을 남긴다. 목록
  검색과 같은 법령이어도 조문 검색결과는 버리지 않는다. 본문을 열 수
  없는 별표ㆍ서식 항목은 뺀다.
- 공개 저장소는 `origin` 원격의 `main` 브랜치이며 공개용 커밋만 유지한다.
  Release 태그는 반드시 공개 브랜치를 대상으로 만들고 `origin`에 푸시한다.
  비공개 개발 이력(`private` 원격)에 붙은 커밋의 태그를 공개 원격으로 보내지
  않는다. 공개 배포 버전은 `1.0.0`부터 센다.
- 저장소 공개만으로 LGPL 의무가 전부 끝나는 것은 아니다. 실제 배포에 넣은
  Qt/PySide6 버전의 완전한 대응 소스 또는 관리 가능한 서면 제공 방법도
  필요하다. `docs/QT_SOURCE_OFFER.md`의 제공 약속과 연락처를 삭제하지 않는다.
- 위 소스 제공과 재빌드 경로를 관리하기 어렵다면 폴더(onedir) 방식이 더
  단순하다. 그 경우 EXE에서 `binaries`/`datas`를 빼고
  `exclude_binaries=True`를 준 뒤 `COLLECT`를 추가하고, `ui/about.py`의
  고지도 실제 배포 형태에 맞게 갱신한다.
- 라이선스 사본(`licenses/`)은 LGPL v3와 OFL 1.1이 요구하는 것이라
  spec의 `datas`에서 빼지 않는다. LGPL v3는 GPL v3의 조건을 포함하는
  형태라 `LICENSE.GPLv3.txt`도 함께 있어야 사본이 완성된다. onefile에서는
  사본이 임시 폴더에 풀리므로 프로그램 정보 대화상자의 "라이선스 원문
  보기"가 사용자에게 보여 주는 통로다. 이 버튼을 없애지 않는다.
- 프로젝트 자체 MIT 사본인 `LICENSE`와 `licenses/LICENSE.project-MIT.txt`도
  함께 유지한다.
- 새 오픈소스를 끌어 쓰면 `ui/about.py`의 `LICENSE_NOTICES`에 고지를
  추가한다. 화면 어딘가에서 고지에 닿을 수 있어야 재배포 조건을 채운다.
- `dist/`와 `build/`는 저장소에 올리지 않는다.

# UI 개선 작업목록 2차 (2026-09-04)

1차(`UI_개선_작업목록_2026-09-04.md`) 반영분을 실제 화면에서 보고 이어서
요청한 것들. 위에서부터 차례로 처리한다.

**끝낸 항목은 제목 끝에 `(완료)`를 붙이고 `- [x]`로 바꾼다.**

# A. 먼저 고칠 결함

## 별표가 안 펼쳐진다 (버그, 우선) (완료)

- [x] 본문 안 별표 항목을 눌러도 PDF 미리보기가 펼쳐지지 않는다
  - **1차 65번이 바로 이 회귀를 고치는 항목이었는데 아직 안 된다.**
  - **테스트는 통과한다**
    (`test_annex_title_opens_constrained_inline_pdf_panel`,
    `tests/test_law_annex_links.py`). 그러니 착수하면 **테스트부터
    의심한다** — 실제 클릭 경로를 타지 않고 내부 함수를 직접 부르는
    형태라면, 화면에서 깨져도 초록불이 뜬다.
  - 확인할 것: 제목 글자를 눌렀을 때와 옆 아이콘(hwp/pdf)을 눌렀을 때가
    서로 다른 경로인지, 어느 쪽이 안 되는지 먼저 가른다.
  - **원인(확인함)**: 별표 목록(`_annex_section_entries`)은
    `_set_detail_document` 안에서만 채워지는데, **저장 본문을 열 때는 그려
    둔 HTML을 그대로 되살리므로 그 경로를 타지 않는다.** 목록이 빈 채로
    남아 화면에는 별표가 보이는데 눌러도 `_annex_entry_at`이 None을 돌려
    조용히 끝났다. 탭을 옮겨 다녀도 같은 일이 났다(목록이 탭별이 아니라
    화면 하나에 전역이었다).
  - **고친 것**: ① `open_cached_law`에서 원문으로 별표 목록을 다시 만든다.
    ② 목록을 문서 상태(`annex_entries`)에 함께 담아 탭을 되살릴 때 복원한다.
  - **테스트**: 기존 테스트가 `_toggle_annex_preview`를 직접 불러서 화면이
    깨져도 초록불이었다. **실제 클릭 경로(`_detail_link_clicked(QUrl)`)로
    시작하는** 회귀 테스트 둘을 새로 넣었다
    (`test_saved_law_keeps_annex_list_clickable_after_reopen`,
    `test_annex_list_survives_switching_document_tabs`).
- [x] 별표 목록의 줄 간격을 **1.75**로 넓힌다
  - 지금은 `line-height:1.55`다(`ui/tabs/resource_search.py`의 별표 항목
    HTML). 1차 30번에서 `padding:7px 0` → `10px 0`과 함께 넣은 값이다.
  - **주의**: 본문 줄 간격 항목과 마찬가지로 Qt가 CSS `line-height`를
    무시할 수 있다. 값만 바꾸고 끝내지 말고 화면에서 실제로 벌어지는지
    확인한다. 안 먹으면 `setLineHeight`나 문단 여백으로 처리한다.

## 글자 크기를 바꾸면 본문이 깨진다 (버그, 우선) (완료)

- [x] 조절칸으로 글자 크기를 바꾸면 본문이 거의 사라지고 조문 하나만
      남는다. 조문 옆 별(☆)만 빈 자리에 흩어져 남는다.
  - **창을 최소화했다 키우면 정상으로 돌아온다.** 즉 내용이 없어진 게
    아니라 **레이아웃이 다시 계산되지 않은 것**이다. 리사이즈가 들어오면
    그때 제자리를 찾는다.
  - **의심되는 곳**: 본문 뷰어가 `DeferredWrapTextBrowser`인데, 이름대로
    "창 크기 조절 중에만 줄바꿈을 잠시 미루는" 클래스다(`ui/widgets.py`).
    글자 크기를 바꿀 때 `_set_detail_font_size` →
    `_replace_detail_content`로 문서를 통째로 다시 넣는데
    (`ui/tabs/resource_search.py`), 이때 지연 줄바꿈 상태(`_wrap_deferred`)
    가 잘못 걸린 채 남거나 레이아웃 무효화가 빠졌을 수 있다.
  - 별(☆)만 제자리에 남는 것도 단서다. 별은 본문 위에 얹은 위젯이라
    `_schedule_three_stage_button_positions`로 따로 자리를 잡는다. **위젯
    쪽은 갱신됐는데 문서 쪽만 안 됐다**는 뜻이다.
  - 위의 "글자 크기ㆍ글꼴이 다른 본문 탭에 안 옮겨진다"와 **같은 함수**를
    건드리므로 함께 처리한다.
  - **확인한 것**: offscreen으로 큰 문서(조문 60개)를 만들어 크기를 바꿔
    보니 블록 수ㆍ글자 수ㆍ별 개수가 모두 그대로였다. **데이터는 멀쩡하고
    화면에 그려지는 것만 문제**라는 뜻이다(사용자가 창을 키우면 돌아온다고
    한 것과 맞는다).
  - **고친 것**: ① 내용을 갈아 끼우기 전에 미뤄 둔 줄바꿈을 끝내는
    `settle_wrap_now()`를 `DeferredWrapTextBrowser`에 넣고
    `_replace_detail_content` 첫머리에서 부른다. 지연 상태가 남으면 이전
    폭이 고정된 채 새 내용이 배치되고, 뒤늦은 타이머가 이전 문서 기준
    스크롤을 되살리려 든다. ② 배치가 끝난 뒤 뷰포트를 한 번 다시 그린다.
  - **회귀 테스트**: `test_settle_wrap_now_restores_widget_width_before_new_content`.
  - **남은 확인**: offscreen에서는 실제 페인팅이 없어 증상 자체를 재현할 수
    없다. 실제 화면에서 다시 봐 주셔야 한다.

## 글자 크기ㆍ글꼴이 다른 본문 탭에 안 옮겨진다 (버그) (완료)

- [x] 한 문서에서 글자 크기를 바꾼 뒤 다른 본문 탭으로 가면, 조절칸은 새
      값(11)을 보여 주는데 **글자는 옛 크기 그대로**다
  - **원인 확인함**: `_set_detail_font_size`는
    `self.detail_font_size`(조절칸이 읽는 값)를 갱신한 뒤
    **지금 화면에 떠 있는 문서 하나만** 다시 그린다
    (`ui/tabs/resource_search.py`). 열려 있는 다른 탭들의 QTextDocument는
    `_document_states`에 옛 크기 그대로 남는다. 그래서 조절칸 값과 실제
    글자가 어긋난다.
  - `_set_detail_font_family`(글꼴 콤보)도 똑같은 구조라 같은 증상이 있다.
  - **고치는 법 두 갈래**: ① 크기를 바꿀 때 열린 문서 전부에 적용한다
    (탭이 많으면 느려질 수 있다) / ② 문서 상태마다 자기 크기ㆍ글꼴을
    적어 두고, 그 탭을 **활성화할 때** 현재 값과 다르면 그때 맞춘다
    (지연 적용, 권장). `scale_document_font_sizes(html, source, target)`가
    이미 있으므로 크기 환산은 그대로 쓸 수 있다.
  - **고친 것**: 문서 상태에 글꼴 이름(`font_family`)도 함께 담고,
    그려 둔 문서를 되살릴 때 `_document_matches_font()`로 지금 설정과
    같은지 본다. 다르면 그려 둔 문서를 쓰지 않고 HTML 경로로 다시 그린다
    (지연 적용). 크기를 바꾼 직후 그 탭에 처음 갈 때 한 번만 든다.
  - **회귀 테스트**: `tests/test_detail_font_propagation.py`. 수정을 빼면
    실제로 실패하는 것까지 확인했다.

## 콤보 목록이 아직 잘린다 (완료)

- [x] 콤보를 펼쳤을 때 마지막 항목이 잘린다. 목록 높이를 더 준다
  - **1차 15번으로 이미 요청했던 것인데 고쳐지지 않았다.** 코드에 처리가
    들어간 것만 보고 완료로 표시한 것은 판정 잘못이다. 1차 목록에서
    미완료로 되돌렸다.
  - **범위**: 프로젝트 선택 콤보뿐 아니라 **중앙부처 질의회신ㆍ법령해석례ㆍ
    판례 화면의 콤보가 모두 잘린다.** 한 곳만 고치지 말고 `DropdownComboBox`
    를 쓰는 자리를 전부 확인한다.
  - 이 콤보도 `DropdownComboBox`를 쓴다(`ui/tabs/viewed_laws.py:124`).
    1차 15번에서 행 높이를 최소 28px로 잡고, 항목 수가 적으면 스크롤바를
    끄도록 이미 손봤는데(`ui/widgets.py`) 아직 모자란다.
  - **이 저장소에서 되풀이되는 함정이다.** `수정기록.md`의 2026-09-03
    "콤보 목록 잘림" 항목에 적어 두었다 — **Qt는 콤보 목록 높이를 행 높이
    합으로만 잡고 스타일시트의 안쪽 여백ㆍ테두리를 계산에 넣지 않는다.**
    행 높이만 더 키우지 말고 목록 뷰의 여백ㆍ테두리 두께까지 더해서 높이를
    잡는다.
  - **재현**: 항목 둘일 때 56px이 필요한데 실제로 글자가 그려지는 영역은
    44px이었다(**12px 모자람**). 고정 보정값 `POPUP_CHROME = 8`은 목록
    안쪽 여백만 센 값이라, 목록을 감싼 틀과 목록 자신의 테두리를 빠뜨렸다.
  - **고친 것**: 실제로 글자가 그려지는 영역(`view.viewport()`)을 재서
    모자란 만큼 더 준다. 스타일이 바뀌어도 따라간다.
  - **회귀 테스트**: `tests/test_dropdown_popup_height.py` (항목 2ㆍ3ㆍ4ㆍ12개).

# B. 글꼴과 줄 간격

## 글꼴 (본문ㆍUI를 나눠서 본다) (완료)

- [x] **본문 기본값을 굴림 10pt로 되돌린다** (위 "본문 서식 도구 줄" 참조)
- [x] **UI 글꼴의 세부 두께가 안 먹는 문제**
  - 맑은 고딕은 Windows에 Semilight(290)ㆍRegular(400)ㆍBold(700) **세 종뿐**
    이다. `font-weight`를 500ㆍ600으로 올려도 그 굵기 파일이 없어 전부
    Regular로 그려진다. 사용자가 "계속 올리는데 똑같다"고 한 것이 맞다.
  - 지금 `ui_font()`의 기본 굵기가 `QFont.Weight.Medium`(500)인데
    **실제 효과가 없다**(`ui/theme.py`).
  - 저장소에는 **Pretendard 9종 + 가변 폰트가 이미 있고**
    (`fonts/`), 등록 함수도 살아 있다
    (`register_bundled_pretendard_fonts`, `ui/theme.py`). Pretendard는
    Thin~Black 9단계라 세부 두께가 실제로 먹는다.
  - 1차에서 UI 글꼴을 Pretendard에서 맑은 고딕으로 바꾸면서 굵기 조절
    능력을 잃었다. **"UI 텍스트 느낌이 달라졌다"의 원인도 굵기가 아니라
    글꼴 교체 그 자체다.**
  - **결정: 맑은 고딕을 유지한다.**
  - **한 것**: `ui_font()`의 기본 굵기를 Medium(500)에서 Normal(400)로
    되돌렸다. 이 함수가 창 전체 기본 글꼴이라 영향이 가장 컸는데, 500은
    실제로 Regular로 그려지고 있었으므로 **화면은 그대로**다.
  - 창 스타일시트 맨 위에 이 사실을 적어 두었다(500ㆍ600은 모두 Regular로
    그려진다, 굵게 하려면 700). 스타일시트에 흩어진 500ㆍ600 61곳은
    지금도 Regular로 보이고 있어 값을 고쳐도 화면이 달라지지 않으므로
    건드리지 않았다. **굵게 보여야 하는데 안 굵은 자리가 있으면 알려
    주시면 700으로 올린다.**
  - 본문과 UI는 원래도 나뉘어 있었다(본문 굴림 / UI Pretendard).
    본문을 굴림으로 되돌리는 것과 UI 글꼴 결정은 별개로 처리한다.

## 본문 서식 도구 줄 (완료)

- [x] 글꼴 콤보("굴림")와 글자 크기 칸("10.0pt")의 높이를 같게 맞춘다
  - **원인**: 창 스타일시트의 `QLineEdit { min-height: 38px }`를 숫자 칸이
    안에 품은 입력칸을 통해 함께 받아, 그 칸에 적어 둔 max-height 28px과
    위젯 고정 높이가 모두 밀렸다(최소 38 > 최대 30). 그래서 글꼴 칸보다
    8px 높았다.
  - **고친 것**: `CompactDoubleSpinBox`가 자기 스타일시트로 높이를 못박는다
    (위젯 스타일시트가 창 스타일시트보다 세다). 테두리 2px을 뺀 안쪽
    높이를 적어야 실제 높이가 맞는다.
  - **회귀 테스트**: `tests/test_detail_header_control_heights.py`.
- [x] 본문 기본값을 **굴림 10pt**로 한다
  - `DETAIL_FONT_FAMILY`ㆍ`DETAIL_FONT_CSS_FAMILY`ㆍ`DETAIL_FONT_FAMILIES`를
    굴림 우선으로 되돌리고 `DEFAULT_DETAIL_FONT_POINT`를 10.0으로 올렸다.
  - 화면 세 곳에 하드코딩돼 있던 "Malgun Gothic"을 `DETAIL_FONT_FAMILY`로
    바꿔 한곳에서 관리하게 했다.
  - 저장본 글꼴 정규화 테스트가 맑은 고딕을 기대하고 있어 계약을 새로
    적었다(특정 글꼴 이름 대신 `DETAIL_FONT_CSS_FAMILY` 기준).
  - **1차 20번과 정반대다.** 그때 "법령을 수립지침류와 통일"하려고
    `DETAIL_FONT_FAMILY`를 굴림에서 `Malgun Gothic`으로 바꾸고 크기도
    9.5pt로 맞췄다(`utils/constants.py:18,27`). 실제 화면을 보고 굴림
    10pt로 되돌리는 것이므로, 1차 20번 항목도 함께 고쳐 적는다.
  - 되돌릴 때 확인할 것: 1차에서 `.content`의 `14px`을 `9.5pt`로 바꾼
    것은 **단위 문제**라 그대로 두고(px과 pt가 섞이면 같은 설정에서도
    법령 쪽만 커 보였다), 글꼴 이름과 기본 크기만 되돌린다.
  - 이미 저장된 본문의 글꼴을 맑은 고딕으로 정규화하는 처리가 있다
    (`test_legacy_saved_body_fonts_are_normalized_to_malgun_gothic`).
    기본값을 굴림으로 되돌리면 이 정규화도 함께 손봐야 한다.
- [x] 그 옆 박스가 잘리는 것을 본다 (색 단추ㆍ초기화ㆍ메모가 있는 칸)

## 본문 줄 간격 (완료)

- [x] 한 문단 안에서 자동 줄바꿈될 때의 줄 간격을 넓힌다
  - 단락 **사이** 간격은 괜찮다. 한 문단이 두 줄 이상으로 접힐 때만 좁다.
  - **원인으로 의심되는 것**: Qt의 QTextDocument는 CSS `line-height`를
    지원하지 않는다. 지금 본문 스타일에는 `line-height:1.75`만 있고
    (`utils/formatting.py`) `setLineHeight` 호출이 **한 곳도 없다.**
    그래서 1차 20번에서 1.45 → 1.75로 올린 것이 화면에는 반영되지 않았을
    가능성이 크다. 단락 사이는 `.paragraph { margin:0 0 12px 0 }`이라
    실제로 먹는데, 이는 "단락 간은 괜찮다"는 관찰과 정확히 맞아떨어진다.
  - **고치는 법**: `QTextBlockFormat.setLineHeight(값,
    ProportionalHeight)`를 본문 블록에 적용한다. 착수할 때 먼저 CSS만
    바꿔 보고 정말 안 먹는지 확인한 뒤 손댄다.
  - 저장된 본문(HTML로 보관)에도 적용되어야 하므로, 다시 열 때 블록
    서식을 다시 입히는 자리가 필요한지 확인한다.

## 법령과 수립지침의 실제 글자 크기가 다르다 (완료)

- [x] 같은 10pt로 맞춰도 법령 본문과 수립지침(행정규칙) 본문의 글자 크기가
      화면에서 다르게 보인다
  - **1차 20번이 고치려던 바로 그 문제인데 아직 남아 있다.** 그때는
    `.content`의 `14px`(≈10.5pt)을 `9.5pt`로 바꿔 단위를 통일했다
    (`utils/formatting.py`). 그것만으로는 부족했다는 뜻이다.
  - **다음 후보를 의심한다**: 1차에서 `normalize_detail_html`이 정규식으로
    바꾸는 것은 `font-family` **뿐이고 `font-size`는 손대지 않는다**
    (`ui/theme.py`). 행정규칙 본문은 법제처 API가 준 HTML을 그대로 쓰므로
    거기 인라인으로 박힌 `font-size`가 남아 법령 쪽과 달라질 수 있다.
  - 착수하면 두 화면의 실제 문자 서식(`QTextCharFormat.fontPointSize`)을
    찍어서 어디서 갈라지는지 먼저 확인한다. 눈대중으로 값을 맞추지 않는다.
  - 확인되면 두 본문의 글자 크기를 견주는 회귀 테스트를 붙인다.

# C. 본문 화면 다듬기

## 본문 기본정보 아래 여백 (완료)

- [x] 기본정보 상자(공포일자ㆍ시행일자ㆍ소관부처)와 첫 장("제1장 총칙")
      사이 여백을 **10 더** 준다
  - 1차 23번에서 같은 자리를 `margin-bottom: 20px → 28px`로 넓혔다
    (`utils/formatting.py`의 메타 상자). 여기서 10 더하면 38px이 된다.

## 조문 검색 머리와 법령명 줄을 한 덩어리로 (완료)

- [x] 왼쪽 "조문 검색" 머리와 오른쪽 법령명 줄의 **바탕색을 맞춘다**
- [x] **라운드를 없애** 두 칸이 붙어 보이게 한다
- [x] 두 칸의 **글꼴 스타일도 같게** 맞춘다
  - 1차 29번이 같은 목적("상단 영역 외곽선 제거 및 조문 검색 배경과 연결")
    이었고, 그때 `QLabel#pinnedDocumentHeadline`을
    `background: transparent; border: none; border-radius: 0`으로 바꿨다
    (`ui/main_window.py`). 라벨이 투명해지면서 **그 부모 칸의 배경색**이
    드러나는데, 그 색이 왼쪽 조문 검색 쪽과 달라 아직 갈라져 보인다.
  - 라벨만 보지 말고 두 칸을 감싸는 컨테이너의 배경ㆍ테두리ㆍ라운드를
    함께 맞춘다.

## 조문 옆 3단 버튼 (완료)

- [x] 버튼 너비를 줄인다
  - 1차 22번에서 글자를 "3단비교"에서 "3단"으로 줄였는데
    (`ui/tabs/resource_search.py`, `ui/tabs/ai_search.py`) 버튼 자체 너비는
    그대로다. 글자에 맞춰 좁힌다.
  - 이 버튼은 본문 위에 얹혀 조문 첫 줄 오른쪽 끝에 놓이고, 자리를 내려고
    문서 오른쪽 여백을 64px 잡아 둔다(`_set_three_stage_articles`의
    `frame_format.setRightMargin(64.0)`). 버튼을 좁히면 **그 여백도 함께
    줄여야** 본문 오른쪽에 빈 칸이 남지 않는다.

## 뒤로가기(◀) 버튼 크기 (완료)

- [x] ◀ 버튼 크기를 옆의 "본문" 글자 높이에 맞춘다
  - 지금 `restoreViewButton`은 `setFixedSize(30, 30)`이고
    (`ui/widgets.py`의 `build_restore_view_button`) 스타일시트에도 30px로
    한 번 더 고정돼 있다(`ui/main_window.py`). **두 곳을 함께 고쳐야
    한다.**
  - 1차에서 같은 줄의 서식 도구들은 28px로 맞췄는데(색 단추ㆍ초기화ㆍ메모)
    이 버튼만 30px이라 어긋나 보이는 것일 수 있다. 줄 전체 높이를 함께
    견준다.
  - **주의**: 스타일시트 주석에 적힌 대로 전역 `QPushButton`의
    `min-height: 38px`ㆍ`padding: 0 14px`를 덮어써야 ◀ 아이콘이 잘리지
    않는다. 크기를 줄일 때 아이콘이 잘리지 않는지 확인한다.

## 본문 색칠 동작 (완료)

- [x] 색을 고를 때 뜨는 "글씨를 먼저 선택하라"는 오류를 없앤다
  - 색을 콤보로 먼저 골라 두고, 그 다음 글자를 드래그하고, 단추를 누르는
    순서로도 쓸 수 있다. 지금 경고는 그 순서를 막는다.
  - 드래그하지 않은 채 색을 고르면 **아무 일도 일어나지 않으면 된다.**
    굳이 경고를 띄우지 않는다.

# D. 별표

## 별표만 힌팅 끄기 (완료)

- [x] 별표 항목만 폰트 힌팅을 끈다 (지금 안 먹는 것으로 보인다)
  - **구현은 이미 있다**: `_apply_annex_text_font`가 별표 제목을 찾아
    `QTextCharFormat`에 `PreferNoHinting`을 넣는다
    (`ui/tabs/resource_search.py`). 1차 31번으로 넣은 것이다.
  - `setFontHintingPreference` API도 PySide6 6.11.1에 **존재한다**(확인함).
    그러니 "API가 없어서 조용히 넘어가는" 경우는 아니다.
  - **의심되는 것 두 가지**:
    ① Qt가 힌팅을 폰트 엔진 수준에서 처리해서 **문자 단위 지정을 렌더링에
    반영하지 않는다**(유력).
    ② `document.find(shown)`이 표시 문자열을 못 찾아 서식이 안 걸린다.
  - 착수하면 ②부터 확인한다(찾았는지 로그로 확인). ②가 아니면 ①이므로,
    **별표 목록을 본문 문서 밖 별도 위젯으로 빼는 방법**을 검토한다.
    위젯이면 그 위젯 폰트로 힌팅을 확실히 끌 수 있다. 다만 본문 흐름
    안에 두라는 1차 요구와 충돌하므로 그때 다시 정한다.

## 별표 밑줄 (완료)

- [x] 본문 안 별표 항목의 밑줄을 없앤다
  - 별표 제목이 링크라서 밑줄이 그어진다. 색은 남기고 밑줄만 뺀다.
  - Qt 리치텍스트는 앵커 밑줄을 스타일시트로 못 없애는 경우가 있다.
    `text-decoration:none`이 안 먹으면
    `QTextCharFormat.setAnchor`/`setFontUnderline(False)`로 처리하거나,
    본문 뷰어의 `document().setDefaultStyleSheet`에 `a { ... }` 규칙을
    넣어 본다. 값만 바꾸고 끝내지 말고 화면에서 확인한다.
  - 위 "별표만 힌팅 끄기"와 같은 자리(`_apply_annex_text_font`)에서 함께
    처리할 수 있는지 본다.

# E. 화면 전반

## 하단 상태바 (완료)

- [x] 하단바 안쪽 여백을 상ㆍ하ㆍ좌ㆍ우 1px씩 준다
  - 1차 45번으로 `SharedStatusBar`에 `setContentsMargins(1, 1, 1, 1)`을
    이미 넣었다(`ui/widgets.py`). 화면에서 그대로 안 보이면 여백이 아니라
    바깥 레이아웃이나 자식 위젯 쪽이 원인일 수 있으니 거기부터 본다.
- [x] 오른쪽 "업데이트 확인"의 굵은 글꼴을 없앤다
  - **1차 46번과 정반대 요청이다.** 그때 `QPushButton#updateLinkButton`에
    `font-weight: 600`을 넣었는데(`ui/main_window.py`), 실제 화면을 보고
    빼기로 했다. 되돌릴 때 46번 항목도 함께 고쳐 적는다.

## 최근 검색어 (완료)

- [x] 검색어 박스가 잘린다. ×를 수평 정렬해서 글자를 가리지 않게 한다
  - 1차 42번으로 `RecentSearchChip`을 만들면서 ×를 칩 안에 겹쳐 놓았다
    (`ui/widgets.py`). `query_button`이 칩 전체를 차지하고 글자는 가운데
    정렬인데 ×가 오른쪽 끝에 얹혀서, 글자가 길면 ×에 깔려 잘려 보인다.
  - 겹치기를 유지하려면 ×가 차지하는 만큼 오른쪽 안쪽 여백을 주고,
    아니면 겹치지 말고 한 줄에 나란히 두는 쪽으로 바꾼다.

## 화면명 머리글 (중앙부처 질의회신ㆍ법령해석례ㆍ판례) (완료)

- [x] 상단 화면명의 파란색ㆍ볼드를 없애고 법령검색과 같은 기본 회색으로 통일
  - 1차 14번으로 넣은 `QLabel#pageHeadingLabel`이 지금
    `color: #1f57c8; font-weight: 600`이다(`ui/main_window.py`).
- [x] 법령검색 탭과 이 세 화면의 상단 높이가 미세하게 달라 탭을 옮길 때
      화면이 흔들린다
  - 1차 14번이 노린 것이 바로 "검색창 시작 높이 통일"인데 아직 어긋난다.
    `QFrame#pageHeadingTrack`의 높이ㆍ여백ㆍ아래 선(1px)까지 합쳐서
    법령검색 쪽 머리 영역과 픽셀 단위로 맞춘다.
  - 두 화면의 실제 높이를 재는 회귀 테스트를 붙여 다시 어긋나지 않게 한다.
  - **어긋남이 시작되는 자리**: 사용자가 보기에 화면명 줄뿐 아니라 그
    아래 **검색어 줄(최근 검색어 목록 포함)부터** 이미 법령검색과 살짝
    안 맞는다. 머리글만 맞추지 말고 검색 카드ㆍ최근 검색어 바까지
    위에서부터 차례로 높이를 견줘 볼 것.

## 법령검색 카테고리 바 선택 표시 (완료)

- [x] 법령ㆍ행정규칙ㆍ자치법규ㆍ별표서식도 선택했을 때 **볼드**까지
      통합검색ㆍ지능형과 같게 한다
  - 지금은 통합검색ㆍ지능형만 파란색 + 볼드이고, 부제가 붙은 나머지
    분류는 파란색만 들어간다.
  - **원인은 1차 8번과 같은 자리다.** 부제가 있는 캡슐은 `paintEvent`에서
    직접 그리기 때문에 스타일시트의 `:checked`가 자동 적용되지 않는다.
    1차에서 글자색만 `QColor("#1f57c8")`로 명시했고 굵기는 빠뜨렸다
    (`ui/widgets.py`). 같은 자리에서 굵기도 함께 지정한다.
  - 맑은 고딕은 Bold(700)가 실제로 있으므로 볼드는 정상적으로 먹는다.

## 스크롤바 라운드 (완료)

- [x] 스크롤바가 직사각형으로 보인다. 라운드 처리한다
  - 1차 44번에서 전역 스타일의 손잡이를 `border-radius: 6px`, 폭 12px로
    이미 넣었다(`ui/main_window.py`의 `_apply_style`). 그런데도 각져
    보이는 자리가 있다는 뜻이다.
  - **확인할 것**: 어느 스크롤바인지 먼저 가른다. 전역 스타일이 안 닿는
    곳이 있다 — 콤보 목록ㆍ메뉴는 따로 규칙을 두었고, 별표 PDF 패널이나
    팝업처럼 나중에 붙은 위젯은 자기 스타일시트를 가질 수 있다.
  - 손잡이가 아니라 **트랙(add-page/sub-page)이나 바깥 테두리**가 각져
    보이는 것일 수도 있으니 함께 본다.

## 열린 본문 탭의 × 가 뜨는 범위 (완료)

- [x] 지금은 마우스가 **× 바로 위**에 가야 뜬다. 탭 **오른쪽 1/4** 안에
      들어오면 뜨게 넓힌다
  - 판정은 `CornerCloseTabBar.close_spot_at(point)`이 하고, 그 결과를
    `mouseMoveEvent` → `_set_hover`가 받는다(`ui/widgets.py`). 이 판정
    범위를 탭 폭의 오른쪽 1/4로 넓힌다.
  - **누르는 판정은 지금 그대로 둔다.** 뜨는 범위만 넓히고, 실제로 닫히는
    자리는 × 아이콘 근처로 유지해야 탭을 고르려다 닫는 사고가 없다.
    (`close_spot_at`을 그리기용ㆍ클릭용으로 나눠야 할 수 있다.)
  - 최근 검색어 칩도 같은 "마우스 올렸을 때만 ×" 방식이다
    (`RecentSearchChip`). 거기도 같이 넓힐지는 작업할 때 정한다.

# F. 즐겨찾기

## 즐겨찾기 화면의 제목 상자 (완료)

- [x] 오른쪽 본문 위의 "즐겨찾기" 제목 상자를 없앤다
  - 왼쪽 메뉴에 이미 "즐겨찾기"가 있어 같은 말이 두 번 나온다.
  - `ViewedLawsTab`의 `QLabel("즐겨찾기" if favorites_only else "저장내역")`
    이다(`ui/tabs/viewed_laws.py`).
  - **확인할 것**: 저장내역 화면도 **같은 클래스**를 쓰므로 조건 없이 지우면
    "저장내역" 제목까지 사라진다. 즐겨찾기 쪽만 없앨지, 저장내역도 같이
    없앨지 정한다. (저장내역도 왼쪽 메뉴에 이름이 있어 사정은 같다.)
  - 제목을 빼면 그 자리 여백ㆍ위쪽 정렬이 달라지므로 아래 내용이 위로
    붙어 올라가지 않는지 본다.

## 조항호목 즐겨찾기로 통일 (키워드검색 즐찾 삭제) (완료)

- [x] 지능형 검색(연관ㆍ직접) 결과의 법령 조문을 **조문 즐겨찾기 공간으로
      합치고**, 지금의 키워드검색 행 단위 즐겨찾기는 없앤다
  - 조사와 결정은 `지능형검색_조문즐겨찾기_통합_2026-09-04.md`에 이미
    정리해 두었다. 그 문서의 **작업 A**가 이것이다.
  - 요지: 법령 조문은 지능형 결과의 `법령ID` + `jo_code`가 조항호목 API와
    같은 체계라 그대로 `add_article_favorite_by_id`에 넘길 수 있다.
    행정규칙 조문ㆍ별표서식 행은 이번에 합치지 않고 별표 자체를 없앤다.
  - 즐겨찾기 화면의 "키워드검색" 칸도 함께 정리한다
    (`ViewedLawsTab.FAVORITE_CATEGORIES`).
  - 기존에 키워드검색으로 걸어 둔 즐겨찾기의 이관 여부를 정해야 한다
    (저장 파일 키가 달라 그냥 두면 목록에서 사라져 보인다).

## 프로젝트 즐겨찾기 개선 (큰 건)

### 공통 목록에서 끌어다 담기

- [x] 프로젝트를 바꾸면 즐겨찾기를 **처음부터 다시 검색해서 담아야 한다.**
      이미 담아 둔 것을 다른 프로젝트로 옮겨 담기 쉽게 만든다.
- [~] **즐겨찾기된 것 전체를 모아 보여 주는 공통 목록**을 두고, 거기서
      **마우스로 끌어다** 지금 프로젝트에 넣는다.
  - **데이터 구조는 이미 있다.** 1차 53번으로 한 항목이 여러 프로젝트에
    동시에 속할 수 있게 해 두었고(`_favorite_memberships`),
    `_article_favorites(record, all_projects=True)`처럼 **프로젝트를 가리지
    않고 전부 읽는 경로도 있다**(`storage/cache.py`). 즉 새로 만들 것은
    저장 구조가 아니라 화면이다.
  - 즐겨찾기 트리 항목에 `ItemIsDragEnabled`가 이미 붙어 있어
    (`ui/tabs/viewed_laws.py`) 끌기 자체는 지금도 된다. **공통 목록 →
    현재 프로젝트**로 떨어뜨렸을 때 "옮기기"가 아니라 **"소속 추가"**가
    되도록 받는 쪽을 만든다.
  - **결정: 모든 프로젝트의 즐겨찾기 합집합으로 한다.** 어느 프로젝트에
    담았든 공통 목록에는 늘 전부 보인다. 읽는 경로는
    `all_projects=True`가 이미 있으므로 그대로 쓴다.
  - 합집합이므로 **같은 항목이 여러 프로젝트에 있어도 공통 목록에는 한
    번만** 나와야 한다. 중복 제거 기준은 저장 파일 키(`_cache_key`)와
    조문 단위 4튜플(jo/hang/ho/mok)이다.
  - 공통 목록에는 그 항목이 **어느 프로젝트에 들어 있는지** 표시해 주면
    끌어다 넣을 때 이미 있는 곳에 또 넣는 일이 준다.
  - **정할 것**: 공통 목록에서 뺐을 때 각 프로젝트의 소속도 함께 지울지,
    공통에서만 감출지.
  - 끌어다 넣은 뒤 원래 프로젝트에서도 그대로 남는지(복사이지 이동이
    아닌지) 회귀 테스트로 고정한다.
  - **저장 계층은 마쳤다(2026-09-04)**:
    `all_project_favorite_entries()`가 어느 프로젝트에 담겼든 한 번씩만
    돌려주고 `favorite_project_ids`로 어디에 들어 있는지 함께 알려 준다.
    `add_favorite_to_project()`는 옮기지 않고 소속만 더한다(조문을 담으면
    그 법령도 함께 담아 목록에서 찾아갈 수 있게 한다).
    회귀 테스트 3건은 `tests/test_article_favorites.py`에 있다.
  - **남은 것은 화면뿐이다**: 공통 목록 나무를 왼쪽에 세우고, 거기서 끌어
    프로젝트 탭이나 분류 칸에 떨어뜨리면 `add_favorite_to_project`를
    부르게 잇는다. 지금 트리는 `InternalMove`라 트리 사이 끌기를 받으려면
    `DragDrop`으로 바꾸고 받는 쪽에 `dropEvent`를 둬야 한다.

### 프로젝트 선택을 탭으로

- [x] 프로젝트 고르는 칸을 **콤보박스 대신 탭 방식**으로 바꾼다
  - 지금은 `DropdownComboBox`다(`ui/tabs/viewed_laws.py:124`). 위의 "콤보
    목록이 잘린다" 문제도 이 칸에서 났으므로, 탭으로 바꾸면 그 건은 함께
    사라진다.
  - 새로 만들기ㆍ이름 바꾸기ㆍ지우기 단추(`project_add_button` 등)를 탭
    옆 어디에 둘지 함께 정한다.
  - 프로젝트가 많아졌을 때 탭이 넘치는 경우를 어떻게 할지 정한다
    (좌우 넘김ㆍ줄바꿈ㆍ더보기).
  - **탭으로 바꾸면 드래그 대상이 하나 늘어난다**: 공통 목록에서 끌어다
    **탭 위에 떨어뜨려** 그 프로젝트에 넣는 방식도 자연스럽다. 위 항목과
    함께 설계한다.


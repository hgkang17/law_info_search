# 화면. 법령프로그램 본체와 같은 스택을 써서 나중에 옮겨 붙이기 쉽게 한다.
PySide6==6.11.1

# 법제처 OpenAPI와 Gemini REST 호출. Gemini는 CLI가 아니라 HTTPS API라
# SDK 없이 이것만으로 부른다. SDK를 쓰면 받는 사람이 따로 설치해야 하고
# exe로 묶어 배포할 때도 같이 들어가지 않아 아예 못 쓴다.
requests>=2.31.0

# 법령검색 MCP 서버 (mcp_server/server.py). Claude Desktopㆍ ChatGPT
# Desktop이 로컬 프로세스로 띄워 stdio로 대화한다.
# 2.x에서 FastMCP가 MCPServer로 바뀌면서 mcp.server.fastmcp가 사라졌다.
# 상한을 열어 두면 빌드 기계가 2.x를 받아 배포 EXE의 MCP 서버만 죽는다.
# 2.x로 옮길 때는 mcp_server/server.py의 API도 함께 고쳐야 한다.
mcp[cli]>=1.12.0,<2

# 법령 전문을 한글 문서로 내보낼 때 쓴다. 법제처 OPEN API는 별표ㆍ서식만
# 원본 파일을 주므로 전문은 우리가 만든다. .hwp(이진)는 순수 파이썬으로
# 안정적으로 만들 수 없어 개방 형식인 HWPX(OWPML)를 쓴다. 한글에서 열어
# .hwp로 다시 저장할 수 있다. 판이 올라가며 API가 바뀌면 저장 자체가
# 막히므로 판을 고정한다.
python-hwpx==6.3.0

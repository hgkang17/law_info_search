# 조문 즐겨찾기 정리 (계획, 2026-09-04)

아직 코드는 손대지 않았다. 조사와 결정만 적어 둔 문서다.

작업이 둘로 갈렸다. 둘 다 ``add_article_favorite_by_id`` 배선을 건드리므로
함께 본다.

- **작업 A** — 지능형 검색 결과를 조문 즐겨찾기로 합치기
- **작업 B** — 조문 참조 팝업의 즐겨찾기를 전문 대신 조각으로 저장하기

> 라인 번호는 적지 않는다. 이 문서를 쓰는 동안에도 ``storage/cache.py``ㆍ
> ``ui/tabs/viewed_laws.py``ㆍ``ui/tabs/resource_search.py``가 다른 작업으로
> 바뀌고 있었다(``article_favorites``가 746행에서 889행으로 밀렸다).
> 함수 이름으로 찾는다.

## 먼저 알아 둘 구조

조문 즐겨찾기는 **조항호목 API로 조문을 받아 저장하는 방식이 아니다.**
저장본 파일 안에 ``favorite_articles: [{jo, hang, ho, mok, label}]``로 위치만
기록하고, 열 때 ``extract_law_article``로 잘라 낸다
(``storage/cache.py``의 ``set_article_favorite`` /
``article_favorites``, ``ui/tabs/resource_search.py``의
``open_cached_favorite_article``). 그래서 여는 순간 API 호출이 없다.

**즐겨찾기 화면의 "조항호목" 칸은 저장본이 전문이기를 요구하지 않는다.**
``ViewedLawsTab``의 ``FAVORITE_CATEGORIES``에 ``("article", "조항호목")``이
있고, 그 칸을 채우는 루프는 저장본 레코드의 ``favorite_articles``를 훑을
뿐이다. **어떤 저장본이든 그 필드가 있으면 같은 칸에 뜬다.** 작업 B가
성립하는 근거다.

### 실측 (2026-09-04, 저장내역 84개 26MB)

| 저장본 | 크기 |
| --- | --- |
| ``국토의 계획 및 이용에 관한 법률 시행령_law_009419.json`` (전문) | 2.6MB |
| ``건축법 시행령_law_002118.json`` (전문) | 2.2MB |
| ``…제2조제1호_law_reference_1a69….json`` (조각) | 4.6KB |

조 하나를 걸려고 전문을 받으면 **566배**를 받는 셈이다.

### 항ㆍ호ㆍ목은 이미 된다

저장 계층은 진작부터 항ㆍ호ㆍ목을 지원한다. 실제 저장본에 들어 있다.

```
공공주택 특별법_law_009595.json  →  jo=002704  hang=000100
                                    "공공주택 특별법 제27조의4제1항"
```

``extract_law_article``이 ``_filter_article_hang_ho``로 항ㆍ호ㆍ목까지
잘라 내고, ``favorite_articles``의 키도 4튜플이다. **빠진 것은 UI뿐이다** —
본문 인라인 별표는 조 제목 옆에만 붙고 ``hang``을 넘기지 않는다
(``_toggle_inline_article_favorite``). 참조 팝업 별표는 넘긴다
(``_toggle_reference_favorite``). 위 저장본은 팝업에서 건 것이다.

**본문에 항 별표를 다는 방안은 폐기했다.** 조문마다 별이 수십 개로 늘고,
마우스를 올렸을 때만 나타나게 하는 것도 원치 않는다는 판단이다. 대신
작업 B로 푼다.

---

## 작업 A — 지능형 검색 결과를 조문 즐겨찾기로 합치기

### 결론: 식별자는 같다

법령 조문에 한해 지능형 검색이 돌려주는 값과 조항호목 API가 받는 값이
같은 체계다.

| | 값 |
| --- | --- |
| 조항호목 API (``eflawjosub``, ``get_law_article``) | ``ID``=법령ID, ``JO``=6자리 |
| 조문 즐겨찾기 (``set_article_favorite``) | 법령ID + ``jo`` 6자리 |
| 지능형 검색 결과 행 (``_show_search_results``) | ``법령ID`` + 조문번호ㆍ가지번호 → ``law_unit_code`` |

``law_unit_code(number, branch)``는 ``f"{조:04d}{가지:02d}"``다. 제12조의2가
``001202``. 조항호목 API가 받는 코드 규칙과 같다.

증명은 코드 안에 있다. ``RelatedArticleWorker``가 결과 행의 ``source_id``와
``jo_code``를 그대로 ``get_law_article``에 넣어 본문을 받아 온다.

### 확정한 결정

1. **법령 조문 행**: 조문 즐겨찾기 공간으로 합친다.
2. **행정규칙 조문ㆍ별표서식 행**: 이번에는 합치지 않고 **별표 버튼 자체를
   없앤다.** 지능형 탭 자체 즐겨찾기(행 단위)는 남기지 않는다.
   - 지능형 검색에서 찾은 행정규칙 조문은 당분간 즐겨찾기에 걸 수 없다.
     사용자가 이 손실을 알고 선택했다.
   - 행정규칙은 **원리상 가능하다**(아래). 이번 범위에서 뺄 뿐이다.

### 행정규칙을 미루는 이유 (정정 기록)

처음에는 "행정규칙은 조항호목 API가 없으니 합칠 방법이 없다"고 적었는데
**틀렸다.** 두 가지를 뭉뚱그린 것이다.

- 조항호목 API로 그 조만 받는 것 → 행정규칙은 안 된다.
- 조문 위치를 특정해 즐겨찾기에 기록하는 것 → **된다.** 직접검색 응답에
  ``조문번호``(예: ``0009``)와 ``조문가지번호``(``00``)가 실제로 들어 있다.

조문 즐겨찾기가 애초에 조항호목 API를 쓰지 않으므로 행정규칙도 같은
원리로 가능하다. 잘라내는 함수 ``extract_admin_rule_article``도 이미 있고
연관검색이 쓰고 있다. 그런데도 미루는 것은 아래 셋 때문이다.

1. ``_law_row``가 ``target="law"`` 하드코딩이다. 조문 즐겨찾기 배선 전체가
   이 함수를 지난다.
2. 저장본 구조가 다르다. 법령은 ``payload`` JSON에 ``extract_law_article``,
   행정규칙은 섹션 텍스트다(``_open_cached_resource_snapshot``).
   ``open_cached_favorite_article``이 ``payload`` 전용이라 분기가 필요하다.
3. 응답의 ``행정규칙ID``는 본문 API가 받는 ``행정규칙일련번호``가 아니다
   (실측 근거는 ``수정기록.md``의 해당 항목). 변환 함수
   ``administrative_rule_detail_id``가 목록 검색 API를 한 번 더 부르므로
   별 하나에 API가 2회 든다.

**별표ㆍ서식은 원리상으로도 안 된다.** ``조문번호``가 아니라
``별표서식번호``를 쓰므로 기록할 ``jo`` 자체가 없다(``_provision_label``).

### 구현 지점

1. **지능형 탭의 별표 동작 교체.** 지금은 ``FavoriteTitleDelegate``에
   ``_toggle_favorite_at_row`` / ``_is_favorite_at_row``를 물려 행 단위
   ``law_cache.set_favorite``을 쓴다(``ui/tabs/ai_search.py``). 법령 조문
   행이면 대신 이렇게 부른다.

   ```
   add_article_favorite_by_id(
       law_id=row["source_id"], jo=row["jo_code"],
       label=row["provision"], name=row["name"],
   )
   ```

   다리는 이미 있다. ``_resource_action(name, *args)``가 ``reference_tab``의
   메서드를 그대로 호출하고, AI 채팅 패널이 같은 방식으로
   ``add_article_favorite_by_id`` / ``is_article_favorite_by_id``를 쓴다.
2. **법령 조문이 아닌 행은 별을 그리지 않는다.** 판정은
   ``_three_stage_target``과 같은 기준 — ``kind``가 "행정규칙"으로 시작하지
   않고 ``source_id``와 ``jo_code``가 모두 있을 때만 법령 조문.
3. **남는 행 단위 즐겨찾기 정리.** ``_toggle_favorite_at_row`` /
   ``_is_favorite_at_row`` / ``_finalize_pending_favorite`` /
   ``_pending_favorite_row``가 안 쓰이면 걷어낸다. 다만
   **``law_cache.set_favorite`` 자체는 지우지 않는다** — 법령ㆍ질의회신ㆍ
   해석례ㆍ판례 탭이 계속 쓴다.

---

## 작업 B — 참조 팝업 즐겨찾기를 조각으로

### 지금 무슨 일이 벌어지나

참조 팝업은 **두 동작이 서로 다른 API를 쓴다.**

| 동작 | 쓰는 것 |
| --- | --- |
| 링크를 눌러 팝업을 여는 것 | 조항호목 API — 조 하나만 (약 5KB) |
| 팝업의 ☆를 누르는 것 | 법령 **전문**을 새로 받아 그 안에 위치 기록 |
| 즐겨찾기에서 다시 여는 것 | 저장된 전문에서 잘라낸 화면 (API 0회) |

즉 조문 하나를 5KB로 받아 **눈앞에 띄워 놓고, ☆를 누르는 순간 같은 조문을
담은 전문 1.9MB를 새로 받는다.** 팝업이 가진 내용은 버려진다.

### 바꿀 것

☆를 누르면 전문을 받지 말고, **팝업이 이미 받은 조각을 저장본으로 굽고**
거기에 ``favorite_articles``를 기록한다. 조항호목 칸이 저장본 종류를
가리지 않으므로 **같은 칸에 그대로 뜬다.**

| | 지금 | 바꾸면 |
| --- | --- | --- |
| ☆ 누를 때 API | 전문 1회 (1.9MB) | **0회** |
| 저장 크기 | 1.9MB | 약 5KB |
| 즐겨찾기 위치 | 조항호목 칸 | **조항호목 칸 (동일)** |
| 다시 열 때 | 전문에서 잘라냄 | 조각 그대로, API 0회 |
| 항ㆍ호ㆍ목 단위 | 팝업에서만 가능 | 동일 (본문 별표 불필요) |

동선은 그대로다. 링크를 눌러 팝업을 띄우고 거기서 ☆를 누른다.

### 구현 지점

1. **``_toggle_reference_favorite`` → ``add_article_favorite_by_id``** 경로에서
   전문을 받는 대신 조각을 저장본으로 굽는다. 지금 전문을 받는 자리는
   ``add_article_favorite_by_id`` 안의 ``_request_resource_detail``이다.
   조각 저장본은 이미 ``target=law_reference``로 만들어지고 있다(저장내역에
   실물이 있다).
2. **``open_cached_favorite_article``에 조각용 분기.** 지금은
   ``record["payload"]`` + ``extract_law_article`` 전용이다.
3. **중복 판정.** 같은 조를 본문 별표로도 걸고 팝업으로도 걸면 조항호목
   칸에 두 번 뜬다. ``(법령ID, jo, hang, ho, mok)``이 같으면 하나로 본다.

### 대가

- **앞뒤 조문으로 못 넘어가고 목차가 없다.** 조각만 있기 때문이다. 다만
  "그 조항만 보려고" 거는 즐겨찾기라면 오히려 맞는 동작이다.
- **메모ㆍ형광펜이 조각 기준이다.** ``formatting_spans``는 ``{start, end}``
  문자 위치라 문서가 달라지면 좌표가 달라진다. 나중에 같은 법령 전문을
  열어도 조각에 단 메모는 따라오지 않는다.

---

## 공통 주의사항

- **저장 파일 키가 갈린다.** ``_cache_key``가
  ``국토기본법 제6조…_ai_related_009293_000600``(지능형),
  ``국토기본법_law_009293``(전문),
  ``…_law_reference_<해시>``(조각)로 서로 다른 파일을 만든다. 이미
  지능형 탭에서 걸어 둔 즐겨찾기는 통합 후 목록에서 사라져 보인다.
- **작업 A는 전문을 받는다.** 지능형 검색은 조문 하나만 손에 든 상태라,
  별을 누르면 그 법령 전문을 받게 된다. 같은 법령의 두 번째 조문부터는
  0회다(``_row_is_saved``). 작업 B와 함께 하면 이쪽도 조각으로 통일할지
  다시 볼 것.
- 지능형 탭은 본문을 화면에 띄우면 ``save_snapshot``을 자동으로 부른다.
  열람내역용이므로 그대로 둔다.

## 미결

- [ ] 기존 지능형 탭 즐겨찾기 데이터의 이관 여부와 방법
- [ ] 작업 A도 조각 저장으로 통일할지 (작업 B를 먼저 하면 자연스럽다)
- [ ] 행정규칙 조문에서 별이 사라지는 것을 사용자에게 안내할지
- [ ] 직접검색의 행정규칙 조문 응답에 ``조문내용``이 실제로 들어오는지 확인.
      안 들어온다면 지금도 본문이 안 뜨고 "별표ㆍ서식 본문이 포함되지
      않습니다" 안내가 나온다. 이 작업과 별개인 기존 결함일 수 있다
- [ ] (후속) 행정규칙 조문 통합 — 위 세 군데를 손볼 때
- [ ] 진행 중인 즐겨찾기 개편(프로젝트 최상위 구성)과의 선후 관계.
      그 개편이 먼저면 이 작업은 새 구조 위에서 한다

## 작업할 때

- ``tests/test_article_favorites.py``, ``tests/test_document_tab_favorite.py``,
  ``tests/test_favorite_search_navigation.py``,
  ``tests/test_keyword_category_merge.py``를 함께 돌린다.
- 끝나면 ``수정기록.md`` 맨 위에 항목을 추가한다.
